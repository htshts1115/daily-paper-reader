---
title: "From RGB Generation to Dense Field Readout: Pixel-Space Dense Prediction with Text-to-Image Models"
title_zh: 从RGB生成到密集场读出：基于文本到图像模型的像素空间密集预测
authors: "Zanyi Wang, Xin Lin, Haodong Li, Dengyang Jiang, Yijiang Li"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06553v2"
tags: ["query:vfm"]
score: 9.0
evidence: 用于深度、alpha抠图等密集预测的视觉基础模型
tldr: 现有方法将密集预测转化为RGB生成任务，在VAE潜在空间解码目标，继承过多生成接口。本文发现预训练DiT通过patch-token-patch晶格组织输入，每个token对应固定输出patch。提出ReChannel，保留VAE编码器，去掉解码器，用冻结DiT加任务LoRA，通过共享token线性头直接预测像素级任务场。在六个任务上，ReChannel在无三元图抠图、KITTI深度和指代分割上达到新SOTA，性能优于同类方法且速度快2.48倍，证明密集预测可从生成预训练受益而不必使用生成输出接口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06553v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1770, \"height\": 708, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06553v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 841, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06553v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1794, \"height\": 748, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06553v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1422, \"height\": 583, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1618, \"height\": 654, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1307, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 621, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1274, \"height\": 584, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 370, \"label\": \"Table\"}]"
motivation: 现有方法将密集预测当作RGB生成任务，引入了不必要的生成接口，导致效率低且偏离密集预测需求。
method: 提出ReChannel：保留VAE编码器，冻结DiT并加入任务LoRA，每个token通过共享线性头直接映射到像素级任务场，无需目标解码器。
result: 在六个密集预测任务上，ReChannel在无三元图抠图、KITTI深度和指代分割上刷新SOTA，4B规模下比baseline准确且快2.48倍。
conclusion: 密集预测可从生成预训练中获益，但无需继承其输出接口，轻量线性头的像素场重构方法高效且精准。
---

## 摘要
大规模文本到图像模型是密集预测的有吸引力的骨干网络，因为RGB生成预训练学习了丰富的语义、结构和几何先验。现有的生成和编辑方法通过将密集预测视为目标生成来重用这些先验：诸如深度、法线、alpha抠图、掩码和热力图等注释被编码到RGB训练的VAE潜在空间中，然后解码回类似图像的目标。我们认为这继承了比密集预测所需的更多的生成输出接口：与RGB合成不同，密集预测要求在同一个图像平面上获得像素正确、任务原生场，而不是要渲染的新RGB内容。我们的关键观察是，预训练的DiT已经通过图像平面上的块到令牌到块网格来组织RGB输入，因此每个令牌索引一个固定输出块，其通道可以携带任务原生数量而不是RGB外观。我们将其实现为ReChannel：我们保留用于DiT输入分布的VAE编码器，但丢弃目标侧的解码器，用任务LoRA适配冻结的DiT，并通过共享的令牌局部线性头（约33K参数，无空间混合）将每个令牌映射到其p x p x K_t像素空间块。使用FLUX-Klein，我们在六个密集预测任务和十多个基准上进行了评估。这种最小接口在无trimap抠图、KITTI深度和指代分割上取得了新的最先进结果，并在法线、显著性和姿态上保持竞争力。在匹配的4B设置下，它比编辑加潜在解码对应方案更准确且快2.48倍——密集感知可以从生成预训练中受益而不继承其输出接口。

## Abstract
Large-scale text-to-image models are attractive backbones for dense prediction because RGB generation pretraining learns rich semantic, structural, and geometric priors. Existing generative and editing approaches reuse these priors by casting dense prediction as target generation: annotations such as depth, normals, alpha mattes, masks, and heatmaps are encoded into an RGB-trained VAE latent space and decoded back as image-like targets. We argue this inherits more of the generative output interface than dense prediction requires: unlike RGB synthesis, dense prediction asks for pixel-correct, task-native fields on the same image plane, not new RGB content to be rendered. Our key observation is that a pretrained DiT already organizes RGB inputs through a patch-to-token-to-patch lattice on the image plane, so each token indexes a fixed output patch whose channels can carry task-native quantities instead of RGB appearance. We instantiate this as ReChannel: we keep the VAE encoder for the DiT's input distribution but drop the target-side decoder, adapt the frozen DiT with task LoRA, and map each token to its p x p x K_t pixel-space patch through a shared token-local linear head--about 33K parameters, no spatial mixing. Using FLUX-Klein, we evaluate on six dense prediction tasks and over a dozen benchmarks. This minimal interface sets new state-of-the-art on trimap-free matting, KITTI depth, and referring segmentation, and stays competitive on normals, saliency, and pose. In a matched 4B setting it is more accurate and 2.48x faster than an edit-plus-latent-decode counterpart--dense perception can benefit from generative pretraining without inheriting its output interface.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **现有做法**：基于文本到图像（T2I）生成模型的密集预测方法（如深度估计、法线预测、抠图、分割等）通常将任务视为**目标生成**：把标注（深度图、掩码等）编码到RGB训练的VAE潜在空间，再通过VAE解码器还原为像素级输出。这种做法继承了生成模型的完整输出接口（VAE解码路径），但密集预测的本质是**在同一个图像平面上输出任务原生场**（如几何、光学密度、热力图），而不是渲染新RGB内容。
- **核心问题**：密集预测是否需要这种“目标侧生成接口”？论文观察发现，预训练的DiT（Diffusion Transformer）已经通过`patch → token → patch`的网格组织RGB输入，每个token天然对应一个固定位置的空间载体。因此可以直接**重新解释token的通道含义**，从RGB外观变为任务原生量（深度、法线等），无需额外VAE解码。
- **意义**：提出“读取不生成”的新范式，证明了生成预训练的强大先验可以用于密集感知，而无需继承其输出接口。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：ReChannel —— 将DiT的token场视为空间载体，通过**共享的token局部线性头**直接读出像素空间的任务场，抛弃目标侧VAE解码器。
- **关键流程**：
  1. **输入路径**：保留预训练T2I模型的VAE编码器，为DiT提供正确的输入分布（潜在表示）。
  2. **主干冻结与适配**：冻结DiT主干的全部权重，仅插入**任务特定的LoRA参数**（低秩适配）。在确定性模式（σ=0）下运行，得到空间token场 \( Z_t = F_{\theta+\Delta t}(\text{VAE}_{\text{enc}}(x); \sigma=0, c_t) \)。
  3. **线性读出**：每个token \( z^t_{ij} \) 通过共享的线性投影（权重 \( W_t \)、偏置 \( b_t \)）映射到对应的 \( p \times p \times K_t \) 像素块 \( \hat{Y}^t_{ij} \)，然后平铺得到完整预测图。**无空间混合、无多阶段解码器**，仅约33K参数。
  4. **任务实例化**：对所有任务使用相同读出形式，区别仅在于输出通道数 \( K_t \)（深度/抠图/显著性为1，法线为3，姿态为热图多通道）和监督损失（连续任务用像素空间的L1等，二值任务用掩码监督）。指代分割额外利用文本条件作为提示。
- **为什么线性头足够？**：诊断实验（图2）显示RGB输入token场维度高，任务适配后场坍缩到低维子空间（PR 1.1~4.2），说明线性读出即可捕捉任务语义，不需要高容量解码器。

### 3. 实验设计：数据集、基准、对比方法

- **几何任务（深度 & 法线）**：
  - 数据集：NYU（Eigen split）、KITTI（Garg crop）、ScanNet val；法线附加iBims。
  - 对比：判别式方法（Omnidata v2、DAv2、DSINE）、生成式方法（GeoWizard、Marigold、GenPercept、Lotus-D、Edit2Perc）。
  - 指标：深度用absRel↓/δ₁↑，法线用平均角度误差↓/11.25°准确率↑。
- **无trimap抠图**：
  - 数据集：P3M-500-P、P3M-500-NP、零样本AIM-500。
  - 对比：特殊抠图方法（SHM、HATT、MODNet、P3M-Net、ViTAE-S）以及生成式基线（GenPercept）。
  - 指标：SAD↓、MSE×10³↓、Conn↓。
- **指代分割**：
  - 数据集：RefCOCO、RefCOCO+、RefCOCOg 的标准8个split。
  - 对比：CRIS、PolyFormer、LISA-7B、GLaMM-7B、PSALM-1.3B、OMG-LLaVA-7B、Text4Seg-8B、READ-7B、CoPRS-7B、FCLM-7B等。
  - 指标：累积cIoU（%）。
- **姿态估计 & 显著性**：
  - 姿态：COCO val，AP/AP₅₀，对比HRNet-W48、TokenPose-L、ViTPose-L、RTMO-l、DynPose。
  - 显著性：DUTS-TE、ECSSD，F_max↑/MAE↓，对比ICON、MENet、VST++、VSCode-B、EVPv2。
- **消融实验**（表5）：使用FLUX-Klein 4B，512²分辨率，在法线和抠图上进行：
  - 问1：是否只需要体？→ 仅训练头部（冻结体）垮掉。
  - 问2：是否需要先验？→ 随机初始化全微调结果差。
  - 问3：给定先验，额外容量或生成接口是否有帮助？→ 4级卷积头、全微调、潜在目标、VAE冻结、编辑范式均不如ReChannel的线性头，且速度更慢。

### 4. 资源与算力

- 论文**未明确说明训练所用GPU型号、数量和训练时长**。仅在致谢中提到使用Google TPU Research Cloud (TRC) 获得Cloud TPU访问权限。
- 推理效率：在单张L40S GPU、512²图像上，ReChannel（4B）延迟47.7ms（与主干前向一致），比潜在目标变体（74.4ms，1.56×）、编辑范式（118.1ms，2.48×）快。线性头几乎无额外成本。

### 5. 实验数量与充分性

- **实验数量**：覆盖**六个密集预测任务**、超过12个标准benchmark，包含几何、抠图、分割、姿态、显著性。主实验结果在4个表中展示（表1-4），消融实验（表5）包含8种配置。
- **充分性**：
  - 每种任务均对比了该领域最强**判别式**和**生成式**方法，且使用标准协议（如深度log-space对齐、姿态检测框协议等）。
  - 消融实验设计合理，从三个核心问题（必要性、先验依赖、接口选择）做了严格解耦，并控制骨干和LoRA rank一致。
  - 实验客观性较好，未发现刻意挑选指标或隐瞒结果（如承认在NYU深度上弱于Edit2Perc）。
- **公平性**：所有生成式基线使用相同或相当的骨干（FLUX-Klein），但部分基线可能使用了不同骨干（如Edit2Perc也基于FLUX），论文未详尽列出所有基线骨干。不过消融实验在匹配条件下证明了接口优势。

### 6. 论文的主要结论与发现

- **核心发现**：密集预测可以从T2I生成预训练中受益，但**不需要目标侧VAE解码**。由DiT组织的token场本身已经提供了空间结构，通过一个极轻量（约33K参数）的token局部线性头即可直接读出任务原生场。
- **性能结果**：
  - 无trimap抠图：ReChannel-9B在P3M-500-P SAD 5.69（超越ViTAE-S 0.55），零样本AIM-500 SAD 34.90（不足GenPercept一半），表明移除VAE解码显著提升边界精度和跨域泛化。
  - KITTI深度：ReChannel-9B absRel 0.063，优于Edit2Perc（0.079）达到最佳。
  - 指代分割：ReChannel-4B（80.3 avg cIoU）已超越7B-8B LLM方法，9B（82.0）在全部8个split上最优。
  - 法线、显著性、姿态：均达到或接近SOTA。
  - 效率：在4B规模下比编辑+潜在解码方案快2.48倍，且更准确。
- **设计原则**：线性头足够、LoRA适配必要、生成先验不可少；额外容量或生成接口无益。

### 7. 优点

- **极简输出接口**：仅用共享线性头，无空间解码器、无VAE解码、无LLM分支，统一处理连续、二值、多通道热力图等多种任务。
- **效率与性能双优**：在提升精度的同时大幅降低推理延迟（47.7ms vs 118.1ms），且无精度-速度权衡。
- **诊断充分**：通过参与率（PR）分析和严格的消融实验，有力支撑了“token场已携带足够空间结构”的假设。
- **跨任务泛化**：单一配方应对几何、抠图、语言引导分割、热力图，展现强大通用性。
- **开源友好**：提供代码仓库（github.com/xmz111/ReChannel）。

### 8. 不足与局限

- **骨干范围局限**：实验仅基于FLUX-Klein（4B/9B），未验证其他T2I骨干（如Stable Diffusion、Seedream等）的适用性。方法对骨干架构（如其他非DiT的扩散模型）的泛化性未知。
- **训练资源不透明**：未报告训练所需的GPU/TPU型号、数量、时长，难以评估可重复性。
- **感受野限制**：线性头仅基于单token，对大尺度结构（如长程依赖、全景分割）可能不够，论文尚未探索是否需要全局注意力或交叉token混合。
- **消融中LoRA的影响**：虽比较了完全冻结头和全微调，但未详细探究不同LoRA rank对精度-效率的影响。
- **任务覆盖**：未包括需要像素级分类的任务（如语义分割、全景分割），该方法的适用边界未完全明确。
- **零样本能力**：抠图在AIM-500上远低于P3M-500（SAD 34.90 vs 5.69），跨域泛化虽有优势但仍有显著差距，可能受到训练数据分布限制。

（完）
