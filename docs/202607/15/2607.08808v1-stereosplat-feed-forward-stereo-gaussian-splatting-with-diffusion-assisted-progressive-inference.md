---
title: "StereoSplat+: Feed-Forward Stereo Gaussian Splatting with Diffusion-Assisted Progressive Inference"
title_zh: StereoSplat+：基于扩散辅助渐进推理的前馈立体高斯泼溅
authors: "Zihua Liu, Masatoshi Okutomi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08808v1"
tags: ["query:stereo-depth"]
score: 8.0
evidence: 前馈立体深度估计
tldr: 现有3D高斯泼溅方法依赖多视角观测，单立体对输入下因遮挡、视野有限等难以重建高质量场景。本文提出StereoSplat+，包含一个可处理可变数量立体对的前馈3DGS估计器StereoSplat，以及一种扩散增强的单次渐进推理方案：从单对立体开始，渲染新视角并用一步扩散增强，再反馈更新。在KITTI-360上，该方法在遮挡区域和强外推视角下显著提升了渲染质量和几何精度，优于现有前馈基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1829, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 251, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1722, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 875, \"height\": 523, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1741, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08808v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 377, \"label\": \"Table\"}]"
motivation: 单立体对输入下重建高质量3DGS场景因遮挡、视野有限而困难，现有前馈方法依赖多视角或非因果访问。
method: 提出StereoSplat+，含前馈估计器StereoSplat（融合代价体积与三平面分支，连续位姿编码）和扩散增强渐进推理（生成新视角并反馈更新）。
result: 在KITTI-360数据集上，StereoSplat+在遮挡区域和强外推视角下渲染质量和几何精度优于近期前馈3DGS基线。
conclusion: 该工作展示了扩散增强的单次推理方案能有效提升单立体对输入下3DGS重建质量，适用于机器人及AR等在线场景。
---

## 摘要
近期三维高斯泼溅（3DGS）的进展使得高质量、可渲染的场景表示用于新视角合成成为可能。然而，大多数现有的3DGS方法依赖于多视角观测（或对未来帧的非因果访问）来实现足够的覆盖，这在设备端机器人和AR场景中往往不可行，因为这些场景中的感知仅限于单个立体相机。因此，从单次立体观测中恢复高质量的3DGS场景由于遮挡、有限的视野和缺失的几何结构仍然具有挑战性。我们提出StereoSplat+，一个扩散增强的前馈框架，能够从单个立体对实现因果重建。我们的方法基于两个关键组件。首先，我们提出StereoSplat，一个输入不变的、前馈的三维高斯估计器，它接受可变数量的有姿态立体对作为输入，并预测高质量的三维高斯。StereoSplat通过代价体分支和三平面基三维体分支融合互补的几何线索，并利用连续姿态编码来泛化到不同的视角数量和相机配置。其次，由于推理时通常无法获得多个有姿态的立体对，我们引入了一种扩散增强的单次渐进推理方案，称为StereoSplat+：从单个立体对开始，我们从预测的3DGS渲染新的立体视图，用一步扩散增强器进行细化，并将其作为额外输入反馈回去更新3DGS。在KITTI-360数据集上的实验表明，StereoSplat+提高了新视角渲染质量和几何精度，特别是在遮挡区域和强视角外推情况下，优于最近的前馈3DGS基线。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled high-quality, render-ready scene representations for novel-view synthesis. However, most existing 3DGS pipelines rely on multi-view observations (or non-causal access to future frames) to achieve sufficient coverage, which is often unavailable in on-device robotics and AR settings where sensing is restricted to a single stereo rig. Recovering a high-quality 3DGS scene from one stereo observation, therefore, remains challenging due to occlusions, limited field of view, and missing geometry. We present StereoSplat+, a diffusion-enhanced feed-forward framework that enables causal reconstruction from a single stereo pair. Our method builds on two key components. First, we propose StereoSplat, an input-invariant feed-forward 3D Gaussian estimator that takes a variable number of posed stereo pairs as input and predicts high-quality 3D Gaussians. StereoSplat fuses complementary geometry cues via a cost-volume branch and a triplane-based 3D volume branch and leverages continuous pose encoding to generalize across view counts and camera configurations. Second, since multiple posed stereo pairs are typically unavailable at inference time, we introduce a diffusion-enhanced one-shot progressive inference scheme called StereoSplat+: starting from one stereo pair, we render novel stereo views from the predicted 3DGS, refine them with a one-step diffusion enhancer, and feed them back as additional inputs to update the 3DGS. Experiments on the KITTI-360 dataset show that StereoSplat+ improves novel-view rendering quality and geometry accuracy, especially in occluded regions and under strong view extrapolation, outperforming recent feed-forward 3DGS baselines.

---

## 论文详细总结（自动生成）

# StereoSplat+ 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：从单个立体相机对（单次观测）重建高质量、可渲染的三维高斯泼溅（3DGS）场景，面临遮挡、视野有限、几何缺失等挑战。现有前馈3DGS方法大多依赖多视角图像（或对未来帧的非因果访问）来获得足够的场景覆盖，在设备端机器人、增强现实（AR）等只能使用单立体对的场景中不适用。
- **研究动机**：实现一种因果的、实时的、从单立体对输入即可恢复高质量3DGS表示的框架，同时能够处理弱约束区域（如遮挡、大外推视角）的重建。
- **整体含义**：提出StereoSplat+，一种结合前馈3DGS估计器与扩散模型渐进推理的框架，在保持前馈高效性的同时，通过自增强方式弥补单视图信息不足的问题，显著提升新视角合成质量和几何精度。

## 2. 论文提出的方法论

### 核心思想
- 使用一个**输入不变的前馈3DGS估计器**（StereoSplat），能够接受可变数量的立体对（1到K对）并输出高质量3DGS。
- 结合**扩散增强的单次渐进推理**（StereoSplat+）：从单个立体对出发，先用StereoSplat估计初始3DGS，渲染出伪新视角，再经一步扩散模型增强，将这些增强视图作为额外输入重新馈入StereoSplat，得到细化后的3DGS。整个流程仅进行一次渲染-增强-再注入，保持前馈性质。

### 关键技术细节
- **StereoSplat 架构**：
  - 使用共享CNN + 预训练Depth-Anything V2提取图像和几何特征。
  - **双分支设计**：
    - **代价体分支**：构建多视图代价体，预测每视角深度和3DGS参数（中心、不透明度、颜色、置信度等），提供准确的立体匹配线索。
    - **3D体积分支**：基于三平面Transformer（Triplane Transformer），在3D空间聚合几何证据，处理代价体难以处理的遮挡和弱纹理区域。采用**连续正弦姿态编码**（将相机位姿映射到高维连续表示）替代固定可学习嵌入，支持可变视角数量。
    - 两个分支通过**投影引导融合**：将代价体分支的3D高斯及其特征投影到三平面上，与平面查询相加，生成融合后的3DGS。
  - **训练策略**：采用**随机视角子采样**（每次训练随机采样1~K对立体输入），使模型对输入数量与位置不变；并加入**伪视图混合训练**，随机用渲染伪立体对替换真实立体对，弥合渐进推理时的分布差异。
- **扩散增强渐进推理**：
  - 使用预训练的**一步扩散增强器**（基于SD-Turbo改进，参考DIFIX3D+）对渲染的低质量伪新视角进行增强，去除伪影并提高视图一致性。
  - 增强后的视图与原始视图一起输入StereoSplat，进行第二次估计。最后通过置信度引导融合两阶段结果。

### 算法流程（文字说明）
1. 输入：单个真实立体对（左、右图像及位姿）。
2. 使用StereoSplat（代价体+3D体积分支）估计初始3DGS G⁽⁰⁾。
3. 从G⁽⁰⁾渲染伪新视角立体对（如中心帧、末尾帧）。
4. 用一步扩散增强器细化渲染图像，得到增强伪视图。
5. 将原始立体对 + 增强伪视图作为输入，再次运行StereoSplat，得到细化3DGS G⁽¹⁾。
6. 基于置信度融合G⁽⁰⁾和G⁽¹⁾的渲染结果，输出最终新视角图与深度图。

## 3. 实验设计

- **数据集**：KITTI-360（城市驾驶基准），提供矫正立体图像、稀疏激光雷达标注、IMU/GPS姿态。排除序列0003/0007/0010（强动态场景），使用剩余6个序列。共54,862个bins（每个bin长度8米，含首、中、末三个立体对），按9:1分割为49,377训练/5,485评估。
- **Benchmark**：每个bin中，使用**首帧立体对作为输入**，渲染中心帧和末帧作为新视角目标。评估指标：
  - RGB质量：PSNR（越高越好）、SSIM（越高越好）。
  - 几何精度：渲染深度的AbsRel（越低越好）、SqRel（越低越好）。
- **对比方法**：PixelSplat、MVSplat、OmniScene、DepthSplat（均为近期前馈3DGS基线）。
- **对比设置**：所有方法均使用**单立体对输入**（首帧），渲染bin内所有帧（首帧、中心帧、末帧）并报告bin平均指标。

## 4. 资源与算力

- **论文明确说明**：
  - GPU：4块 NVIDIA A6000。
  - 优化器：AdamW (betas=0.9,0.99)，初始学习率8e-5，带温启动（1k步）。
  - 训练配置：30个epoch，batch size 4，总迭代次数1,450,000次。
  - 输入分辨率：112×544（实验设置中提到对分辨率112×544进行训练，但在最终实验描述中为112×544，实际代码可能与预处理有关）。
  - 允许梯度检查点以减少显存。
- **未明确说明**：训练总时间（小时），但基于4*A6000和30 epoch规模，可推断为几天级别。

## 5. 实验数量与充分性

- **消融实验**（Table I）：在KITTI-360序列0000上（9,207训练/1,023评估bins）进行六组消融：
  - 仅代价体分支（Branch CV）
  - 仅3D体积分支（Branch 3D）
  - 融合双分支（Branch CV+3D）
  - 增加随机子采样训练（StereoSplat）
  - 增加扩散渐进推理（StereoSplat+）
  - 并且报告了每个bin内首帧、中心帧、末帧及平均PSNR/SSIM。
- **与SOTA对比**（Table II）：在完整验证集上对比4种基线，报告bin平均RGB和深度指标。
- **定性实验**：图4展示消融效果（遮挡区域）；图5对比方法渲染质量；图6展示BEV外推视角；图7展示大规模逐bin融合重建。
- **充分性评价**：
  - 消融充分：验证了每个组件（双分支、随机子采样、扩散增强）的贡献，且定量定性结合。
  - 对比全面：包括所有主流前馈3DGS方法（PixelSplat、MVSplat、OmniScene、DepthSplat）。
  - 实验设计公平：所有方法采用相同的单立体输入和评估协议；深度对比使用稀疏激光雷达真值，避免深度估计偏差。
  - 可能存在局限：仅使用KITTI-360单一数据集，缺少其他场景（如室内、移动物体等）的验证。

## 6. 论文的主要结论与发现

- 提出的StereoSplat（双分支+随机子采样训练）本身在单立体输入下已达优于所有基线的PSNR/SSIM（20.42/0.67），并显著降低深度误差（AbsRel 0.071 vs. 基线最好0.094）。
- 进一步引入扩散增强渐进推理（StereoSplat+）后，bin平均PSNR提升至20.76/0.69，在遮挡区域和大外推视角下尤其明显。
- 定量上，StereoSplat+相比最强基线PixelSplat（19.04/0.60）提升+1.72 dB PSNR/+0.09 SSIM。
- 定性上，渲染更清晰、深度更连续，在BEV外推场景中有效抑制波纹和几何扭曲。
- 证明了**一步扩散增强无需大量测试时优化即可显著提升前馈3DGS在弱约束输入下的表现**。

## 7. 优点

- **方法亮点**：
  - 输入不变性设计：连续位姿编码 + 随机子采样训练，使模型自然适应不同数量和位置的输入，且推理时可扩展。
  - 双分支互补：代价体提供精确立体匹配，3D体积处理遮挡和长基线关系，融合效果好。
  - 扩散增强仅用一步，不增加推理延迟，保持前馈性质。
  - 训练中引入伪视图混合训练，弥合推理时分布偏移，确保渐进推理稳定。
  - 置信度引导融合避免低质量阶段贡献。
- **实验亮点**：
  - 消融实验设计细化，每帧单独报告，体现方法在不同距离下的表现。
  - 深度评价与RGB评价并行，展示几何质量提升。
  - 包含了BEV等强外推视角的定性验证，直观显示方法优势。

## 8. 不足与局限

- **实验覆盖有限**：仅在KITTI-360（室外驾驶场景）上验证，缺少室内、动态物体、多类相机等场景的评估。
- **扩散模型依赖**：一步扩散增强器可能偶尔引入伪影，且若进行多次渐进更新（论文仅做一次），误差可能累积。
- **动态对象未显式建模**：大规模逐bin融合时（图7），若场景包含移动车辆/行人，可能产生时序不一致。
- **硬件限制**：训练需4块A6000，对计算资源要求较高。
- **深度真值**：使用稀疏LiDAR+预训练NMRFStereo预测的伪稠密深度，存在一定噪声，可能影响深度监督的精确性。
- **未与基于优化的方法比较**：论文聚焦前馈方法，未与需测试时优化的3DGS（如原始3DGS、Instant-NGP等）对比，但论题是前馈范畴，此局限可接受。

（完）
