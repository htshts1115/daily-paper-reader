---
title: "DROID-ANCHOR: Odometry-Anchored Recurrent Metric Depth Estimation"
title_zh: "DROID-ANCHOR: 基于里程计锚定的递归度量深度估计"
authors: "Yuxuan Chen, Brook Du"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17058v1"
tags: ["query:mono-depth"]
score: 9.0
evidence: 里程计锚定的递归度量深度估计用于单目系统
tldr: 单目视觉SLAM存在尺度模糊与漂移问题。本文提出Metric-DROID，通过集成里程计作为几何锚点，利用LSTM更新算子编码高频里程计，并设计不确定性感知度量后端（BA_odom）平衡视觉与度量残差。该方法实现了零样本度量对齐，有效缓解了轮滑和传感器噪声的影响，显著提升了深度估计的鲁棒性和精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17058v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 857, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17058v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1148, \"height\": 601, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17058v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17058v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 288, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17058v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 291, \"label\": \"Table\"}]"
motivation: 单目深度估计存在尺度模糊和漂移，现有流式SLAM系统虽鲁棒但仍尺度模糊，需要物理锚点进行度量校正。
method: 提出Metric-DROID，集成里程计为几何锚点，用LSTM编码高频里程计序列，BA_odom通过异方差协方差平衡残差，选择性微调保留预训练先验。
result: 实现零样本度量对齐，有效抑制轮滑和传感器噪声，在多种场景下深度估计鲁棒性和精度显著提升。
conclusion: 通过里程计锚定解决尺度问题，为单目视觉SLAM提供一种可靠的度量深度估计方案，具有实际应用价值。
---

## 摘要
精确的度量深度估计对于自主机器人导航至关重要，但单目系统固有地存在尺度模糊和尺度漂移问题。尽管最近基于递归光流的SLAM系统展示了最先进的鲁棒性，但它们仍然存在尺度模糊。在本文中，我们提出了Metric-DROID，一种端到端递归架构，通过整合本体感觉里程计将视觉SLAM锚定到物理现实。我们的框架引入了以下创新：(1) 一个LSTM更新算子，将高频里程计序列编码为空间特征图，为迭代优化提供持久的度量偏差。(2) 一个不确定性感知的度量后端（$BA_{odom}$），将里程计视为具有学习到的异方差协方差的几何锚点。通过回归时变度量不确定性$Σ_{o}$，我们的系统智能地平衡视觉重投影和度量平移残差，有效减轻轮滑和传感器噪声的影响。(3) 我们进一步提出一种选择性残差微调策略，以保留预训练的几何先验，同时实现零样本度量对齐。

## Abstract
Precise metric depth estimation is fundamental for autonomous robot navigation, yet monocular systems inherently suffer from scale ambiguity and scale drift. While recent recurrent flow-based SLAM systems have demonstrated state-of-the-art robustness, they remain scale-ambiguous. In this paper, we propose Metric-DROID, an end-to-end recurrent architecture that anchors visual SLAM to physical reality by integrating proprioceptive odometry. Our framework introduces the following innovations: (1) A LSTM Update Operator that encodes high-frequency odometry sequences into spatial feature maps, providing a persistent metric bias for iterative refinement. (2) An Uncertainty-Aware Metric Backend ($BA_{odom}$) that treats odometry as a geometric anchor with learned heteroscedastic covariance. By regressing a time-varying metric uncertainty $Σ_{o}$, our system intelligently balances visual re-projection and metric translation residuals, effectively mitigating the impact of wheel-slip and sensor noise. (3) We further propose a selective residual fine-tuning strategy to preserve pre-trained geometric priors while enabling zero-shot metric alignment.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：单目视觉SLAM系统（如DROID-SLAM）在无外部参考时存在**尺度模糊（scale ambiguity）和尺度漂移**，导致输出的深度和轨迹只有相对尺度，无法直接用于机器人导航中的避障、路径规划等需要**绝对物理尺度**的任务。
- **研究动机**：现有方法要么依赖昂贵/复杂的RGB-D或立体相机（如MAC-VO），要么计算延迟过高（如MOGS），难以在实时闭环控制中同时满足**度量精度**和**低延迟**。作者希望仅通过**单目相机+通用的低精度里程计（如轮式编码器或IMU）** 实现精确的尺度恢复，且不牺牲实时性。
- **整体含义**：本文提出**Metric-DROID（也称DROID-ANCHOR）**，通过将**里程计作为持久几何锚点**嵌入递归SLAM框架，在保持DROID-SLAM鲁棒性的同时，首次实现**零样本的度量深度对齐**，为低成本单目机器人导航提供可行方案。

### 方法论：核心思想、关键技术细节

- **核心思想**：在DROID-SLAM的迭代优化框架中，融合本体感觉里程计（proprioceptive odometry）提供的**高频相对位姿序列**，作为约束视觉几何的**度量锚点**，并通过不确定性建模自适应平衡视觉与设备的置信度。

- **技术细节（三大创新）**：
  1. **LSTM更新算子（Odom Feature Injection）**：
     - 输入：连续帧间的相对SE(3)变换序列 $\{\Delta T_{k\to k+1}\}$。
     - 处理：通过LSTM编码为隐向量 $z_{ij}$，再经MLP投射为2D特征图，与视觉特征（代价体积、光流）拼接后送入ConvGRU。
     - 作用：提供**持久的度量偏差**，指导隐藏状态迭代优化。
  2. **不确定性感知度量后端（$BA_{odom}$）**：
     - 能量函数：$E(G,d) = \sum_{(i,j)\in E} \|r_{vis}\|^2_{\Sigma_{ij}} + \lambda_{odom} \sum_{(i,j)\in E} \|rodom\|^2_{\Sigma_{odom}}$，其中$rodom = \operatorname{Trans}(G_j G_i^{-1}) - \Delta O_{ij}$。
     - 创新点：通过**可学习的Metric Covariance Head**回归时变的异方差协方差$\Sigma_{odom,ij}$，动态调整里程计残差的权重，自动抑制轮滑、传感器噪声带来的异常影响。
     - 优化求解：采用右扰动李代数（se(3)）推导解析雅可比（$J_{oj}, J_{oi}$），构建稀疏Hessian矩阵，通过Schur补求解全局位姿和深度。
  3. **选择性残差微调**：
     - 冻结预训练的特征提取网络（fnet, cnet）和原始ConvGRU中对应448维视觉特征的权重，仅允许**新增的64维里程计特征**的梯度反向传播，从而在不破坏已有几何推理能力的前提下学会融合度量先验。

- **噪声建模**：在训练时对真实里程计施加合成扰动（尺度因子$\epsilon_s$和加性高斯噪声$\epsilon_t$），模拟不同平台上的非理想传感器。

- **损失函数**：多任务加权损失，包括深度损失（仅在有效深度区域计算）和异方差高斯似然形式的里程计损失（含可学习不确定性$\sigma$），并采用指数加权$\alpha_k$强调后期迭代结果。

### 实验设计

- **数据集**：TUM RGB-D benchmark（提供了RGB图像、度量深度真值和相机轨迹）。
- **基准（benchmark）**：标准深度估计指标：AbsRel（绝对相对误差）、RMSE（均方根误差）、δ1（阈值准确度，满足max(d*/d, d/d*)<1.25的像素比例）。
- **对比方法**：
  - DROID-SLAM（原始相对尺度SLAM）。
  - Depth Anything V3（单目度量深度估计模型，利用大量数据先验，非完整SLAM系统）。
  - 尝试对比MOGS但因开源包不完整未纳入。

### 资源与算力

- 明确说明：所有实验在一台**单张NVIDIA GeForce RTX 4080笔记本电脑GPU**和**Intel Core i9-13950HX CPU（5.50 GHz）** 上运行。
- 未提及训练时长、模型参数量或具体推理速度（仅定性说实时性）。算力资源相对有限，实验规模较小。

### 实验数量与充分性

- **实验数量**：仅进行了两组消融实验和一组主性能对比。
  - 主表：AbsRel/RMSE/δ1对比（DROID-ANCHOR vs DROID-SLAM vs Depth Anything V3）。
  - 消融1：LSTM vs Mean Pooling（分析时序建模作用）。
  - 消融2：合成噪声 vs 干净里程计（分析噪声建模作用）。
- **充分性评价**：实验覆盖较为有限。
  - **积极方面**：消融设计合理，验证了两个关键组件（LSTM编码器和噪声建模）的有效性；指标标准、对比对象合理。
  - **不足**：
    - 仅使用一个数据集（TUM RGB-D），缺乏对其他场景（如室外、动态场景、长距离）的评估。
    - 缺失与主流度量SLAM方法（如MOGS、ORB-SLAM with scale recovery、MAC-VO等）的直接定量对比（MOGS因开源问题未比，可理解；但应增加如DPVO+尺度校正等基线）。
    - 未提供轨迹精度（ATE/RPE）等导航相关指标。
    - 计算效率（帧率、延迟）无具体数字，仅定性提实时性。
    - 未进行跨数据集泛化测试（作者在结论中承认受限）。
- **公平性**：与DROID-SLAM比较是在同一架构基础上，控制变量较好；但与Depth Anything V3比较时需注意后者是利用更多训练数据的大模型，并非SLAM系统，直接比较深度精度可能存在不公平（作者也给出了说明）。

### 主要结论与发现

- **核心结论**：通过将里程计作为几何锚点，DROID-ANCHOR成功将DROID-SLAM从相对尺度系统转化为**度量尺度系统**，在TUM数据集上，AbsRel从0.617降至0.528，RMSE从6.800降至1.663，δ1从0.373提升至0.392。
- **消融结论**：
  - LSTM时序编码优于简单平均池化（AbsRel 1.644→0.528），表明时序运动动态建模对尺度恢复至关重要。
  - 训练中加入合成噪声比使用干净里程计更好（AbsRel 1.122→0.528），说明噪声建模有效提高了对真实传感器噪声的鲁棒性。
- **发现**：学习不确定性$\Sigma_{odom}$能够自动平衡视觉和里程计置信度，在轮滑等场景下避免尺度崩溃。

### 优点

1. **方法创新性**：首次将**LSTM编码的高频里程计序列**作为空间特征图注入递归SLAM的原生特征空间，实现端到端的度量学习。
2. **不确定性建模**：在Bundle Adjustment中引入可学习的异方差协方差，使系统自适应地权衡多传感器残差，提升对异常传感器读数的鲁棒性。
3. **高效微调策略**：选择性冻结+部分梯度更新，使得可在小数据集上快速微调而不遗忘预训练的几何先验，降低训练成本。
4. **零样本度量对齐**：无需真实尺度标签即可对齐到物理单位，具有实用部署潜力。
5. **实时性潜力**：基于DROID-SLAM的迭代框架，未引入显著额外计算（仅增加LSTM和协方差头部），理论上保持实时性能。

### 不足与局限

1. **实验覆盖不足**：仅在一个室内小规模数据集（TUM）上验证，缺乏室外、长序列、动态物体、不同里程计噪声类型（如IMU vs 轮式编码器）等场景的评估。
2. **缺乏轨迹精度指标**：未报告SLAM常用的ATE/RPE，无法全面评价整体定位精度。
3. **缺少与其他度量SLAM/深度估计系统的公平对比**：未与MAC-VO、MOGS、ORB-SLAM+尺度恢复等方法在相同基准下比较，削弱说服力。
4. **泛化性受限**：作者坦承受限于同时提供高保真RGB-D真值和同步里程计的大规模数据集稀缺，不同平台里程计噪声分布差异大，模型跨数据集泛化能力未验证。
5. **噪声建模简化**：使用高斯加性噪声和简单尺度因子模拟真实传感器噪声，实际复杂的非线性漂移、时变偏差等未建模。
6. **代码与数据未开源**：论文为预印本，未提供代码或数据集，实验结果难以复现。
7. **理论深度稍弱**：BA推导部分为补充材料，但整体方法叙述偏重工程实现，对算法收敛性、损失函数理论分析较浅。

（完）
