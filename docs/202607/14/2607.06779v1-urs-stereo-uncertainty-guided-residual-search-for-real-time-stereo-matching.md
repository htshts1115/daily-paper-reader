---
title: "URS-Stereo: Uncertainty-Guided Residual Search for Real-Time Stereo Matching"
title_zh: "URS-Stereo: 不确定性引导的残差搜索实现实时立体匹配"
authors: "Pouya Sohrabipour, Chaitanya kumar reddy Pallerla, Dongyi Wang"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06779v1"
tags: ["query:stereo-depth"]
score: 8.0
evidence: 面向手机双摄的实时粗到细立体匹配
tldr: 粗到细立体匹配中，传播视差不准确会导致后续细化阶段匹配失败。本文提出URS-Stereo框架，通过不确定性引导残差搜索模块（UGRSM）预测传播视差可靠性并自适应调整局部代价体中心。在SceneFlow、KITTI、Middlebury等数据集上，该方法在保持实时推理速度的同时显著提升视差估计精度，验证了不确定性引导搜索策略的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06779v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2607.06779v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 660}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06779v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1251, \"height\": 765}]"
motivation: 现有粗到细立体匹配依赖传播视差，当传播视差不准确时，真实对应可能超出局部搜索范围，导致不可恢复的匹配失败。
method: 提出不确定性引导残差搜索模块（UGRSM），预测传播视差可靠性及残差搜索偏移，自适应调整局部代价体中心以重新定位搜索区域。
result: 在SceneFlow、KITTI 2012/2015、Middlebury和ETH3D上，视差估计精度一致提升，且保持实时推理速度。
conclusion: 不确定性引导的搜索策略有效提升了粗到细立体匹配的鲁棒性和效率。
---

## 摘要
实时立体匹配对于机器人、自主系统和嵌入式视觉应用至关重要，这些应用既要求计算效率又要求视差精度。最近的从粗到细的立体匹配方法通过在高分辨率下使用局部代价体逐步细化视差估计来提高效率。然而，这些方法严重依赖于先前阶段传播的视差估计的准确性。当传播的视差不准确时，真实对应点可能超出预定义的局部搜索范围，导致后续细化过程中出现不可恢复的匹配失败。本文提出URS-Stereo，一种实时的从粗到细立体匹配框架，通过不确定性引导的搜索自适应来解决这一局限性。具体来说，我们引入了一个不确定性引导的残差搜索模块（UGRSM），该模块预测传播视差的可靠性以及残差搜索偏移量，从而在视差细化之前自适应地重新定位局部代价体的中心。通过根据传播视差的置信度动态调整搜索区域，所提方法显著提高了局部对应估计的鲁棒性，同时保持了从粗到细立体匹配的计算效率。在SceneFlow、KITTI 2012、KITTI 2015、Middlebury和ETH3D上的大量实验表明，URS-Stereo在保持实时推理速度的同时，持续改进了视差估计，验证了所提不确定性引导搜索策略的有效性。

## Abstract
Real-time stereo matching is crucial for robotics, autonomous systems, and embedded vision applications, where both computational efficiency and disparity accuracy are required. Recent coarse-to-fine stereo matching methods improve efficiency by progressively refining disparity estimates using local cost volumes at higher resolutions. However, these methods rely heavily on the accuracy of propagated disparity estimates from previous stages. When the propagated disparity is inaccurate, the ground-truth correspondence may fall outside the predefined local search range, leading to unrecoverable matching failures during subsequent refinement. In this paper, we propose URS-Stereo, a real-time coarse-to-fine stereo matching framework that addresses this limitation through uncertainty-guided search adaptation. Specifically, we introduce an Uncertainty-Guided Residual Search Module (UGRSM), which predicts the reliability of propagated disparities together with residual search offsets to adaptively relocate the centers of local cost volumes before disparity refinement. By dynamically adjusting the search region according to the confidence of the propagated disparity, the proposed method significantly improves the robustness of local correspondence estimation while preserving the computational efficiency of coarse-to-fine stereo matching. Extensive experiments on SceneFlow, KITTI 2012, KITTI 2015, Middlebury, and ETH3D demonstrate that URS-Stereo consistently improves disparity estimation while maintaining real-time inference speed, validating the effectiveness of the proposed uncertainty-guided search strategy

---

## 论文详细总结（自动生成）

# URS-Stereo 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：实时立体匹配在机器人、自动驾驶、嵌入式视觉等场景中要求同时具备快速推理和高视差精度。当前粗到细（coarse-to-fine）立体匹配方法通过逐步细化（先粗后精）提高效率，但在高分辨率阶段仅围绕传播视差构建局部代价体，大幅降低计算和内存开销。
- **核心问题**：粗到细方法严重依赖传播视差的准确性。当粗预测或上采样引入较大误差时，真实视差可能落在局部搜索范围之外，导致后续细化阶段无法恢复正确匹配，尤其在物体边界、薄结构、遮挡区域和视差剧烈变化处尤为严重。这种不可恢复的匹配失败是粗到细范式的固有局限。
- **研究动机**：提出一种既能保持粗到细高效性、又能提升鲁棒性的方法，通过自适应调整局部代价体中心，降低传播误差导致的匹配失败概率。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
- 提出 **URS-Stereo** 框架，核心是 **不确定性引导残差搜索模块（UGRSM）**。
- UGRSM 预测传播视差的可靠性（不确定性图），并据此预测残差偏移量，动态调整局部代价体的中心，使搜索区域适应传播误差，而非固定于传播视差。

### 2.2 整体架构（如图1所示）
- 多尺度特征提取（1/16、1/8、1/4 分辨率）。
- 在 **1/16 尺度**构建全代价体，使用 GWC（Group-wise Correlation）+ 3D卷积 + ConvGRU 迭代更新。
- 在 **1/8 和 1/4 尺度**使用 UGRSM 调整局部代价体中心，然后构建局部代价体，同样经 ConvGRU 细化。
- 最后在 1/4 尺度进行 **Mono-Stereo 互细化**，结合单目视差引导。

### 2.3 UGRSM 细节（分四步）
1. **不确定性预测**：根据左图特征和传播视差，通过轻量网络预测不确定性图 $U^{(s)} \in [0,1]$（公式9）。由于无真实不确定性标签，使用伪标签 $U_{gt}=1-\exp(-\beta|D_{pred}-D_{gt}|)$（公式10）。
2. **初始残差偏移预测**：同样基于特征和传播视差，预测初始偏移 $\Delta O^{(s)}_{init}$（公式11）。
3. **偏移调制**：最终偏移 $\Delta O^{(s)}_{final}=U^{(s)}\cdot \Delta O^{(s)}_{init}$（公式12），即不确定区域允许较大偏移，确信区域保持原中心。
4. **搜索中心调整**：调整后的中心 $D^{(s)}_{center}=D^{(s)}_{pred}+\Delta O^{(s)}_{final}$（公式13）。然后以该中心生成局部候选集（公式14），搜索半径为 $r$，间隔为 $\delta^{(s)}$。

### 2.4 训练损失
- 立体视差损失 $L_{stereo}$：每个迭代步的 L1 损失，指数加权（$\gamma=0.9$）。
- 不确定性损失 $L_{unc}$：预测不确定性图与伪标签的 L1 损失，同样指数加权。
- 总损失：$L = L_{stereo} + \lambda_{unc} L_{unc}$，其中 $\lambda_{unc}$ 控制不确定性监督权重。

## 3. 实验设计

### 3.1 训练与测试设置
- **训练数据集**：仅使用 **SceneFlow** 数据集进行训练（无任何真实世界数据微调）。
- **测试数据集**（零样本泛化）：
  - **KITTI 2012**：真实驾驶场景，指标为 D1（错误率）和 EPE（端点误差）。
  - **KITTI 2015**：类似场景，同样使用 D1 和 EPE。
  - **Middlebury**：高分辨率室内立体对，指标为 Bad 2.0（误差>2像素比例）和 EPE。
  - 论文摘要同时提到 ETH3D，但在实验表格中**未给出结果**。

### 3.2 对比方法
- 对比了 11 种高效方法，包括：
  - CoEX、MobileStereoNet-2D/3D、FastACV、FastACV+、Lite-CREStereo++、LightStereo-M/L、BANet-2D/3D、Lite Any Stereo。
- 所有方法均使用 SceneFlow 训练、在 KITTI 和 Middlebury 上零样本测试。

### 3.3 实验结果（表1）
- **KITTI 2012**：URS-Stereo 的 D1=3.98%，EPE=0.89，明显优于第二名 Lite Any Stereo（5.45%，1.18）。
- **KITTI 2015**：D1=5.85%，EPE=1.24，优于 Lite Any Stereo（6.45%，1.32）。
- **Middlebury**：Bad 2.0=13.03%，EPE=1.71，接近最佳（Lite Any Stereo：13.13%，1.60），略优或相当。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练时长、参数量或推理速度等算力信息。仅在引言和结论中强调“real-time inference speed”，但未提供具体帧率或计算资源。

## 5. 实验数量与充分性

### 5.1 实验数量
- 仅有一个主要实验表格（表1），覆盖 KITTI 2012、2015 和 Middlebury 三个数据集。
- 未见消融实验的定量表格。文中提到“offset-only variant”与完整 UGRSM 对比，但未给出具体数值，仅有一段文字描述。
- 未展示在 ETH3D 上的实验结果（尽管摘要和结论提及）。
- 缺乏运行速度对比（FPS/时间）。

### 5.2 充分性与客观性
- 训练集仅用 SceneFlow，在多个真实数据集上零样本测试，具有一定泛化性，对比方法也都是同样设置，**公平**。
- 但消融实验不足，无法明确孤立验证 UGRSM 各组件（不确定性预测、偏移调制）的单独贡献。
- 缺少与其他实时方法（如 HITNet、RT-MonSter 等）的对比，后者也使用类似粗到细结构但未出现在对比列表中。
- 实验设计可视为 **初步验证**，尚不够全面系统。

## 6. 主要结论与发现

1. 粗到细立体匹配的传播误差会导致不可恢复的匹配失败，这是其固有局限。
2. 不确定性引导的残差搜索（UGRSM）能显著提升鲁棒性：通过自适应调整局部代价体中心，降低真实视差落在搜索范围外的概率。
3. 在多个基准上，URS-Stereo 在保持实时推理速度的同时，视差精度一致提升（尤其在 KITTI 2012 上 D1 从 5.45% 降至 3.98%）。
4. 不确定性调制（$U\cdot \Delta O$）优于无调制的直接偏移，表明考虑可靠性信息是有效的。

## 7. 优点

- **方法创新性**：直接针对粗到细方法的根本缺陷，提出简单但有效的自适应搜索策略，而非简单增加搜索范围。
- **轻量高效**：UGRSM 为轻量网络，仅增加少量计算开销，保持实时性。
- **无需额外监督**：不确定性学习采用伪标签，避免人工标注。
- **泛化能力强**：仅在合成数据集 SceneFlow 训练，在真实场景 KITTI 和 Middlebury 上实现最佳或接近最佳的零样本性能。
- **清晰的问题分析**：论文对粗到细范式的局限性进行了明确分析和可视化（图略），提供了理论动机。

## 8. 不足与局限

- **实验覆盖不完整**：
  - 未给出 ETH3D 的定量结果（虽然摘要和结论提到）。
  - 缺少运行速度的实际测量（FPS/延迟）和计算资源消耗（参数量、FLOPs）。
  - 消融实验仅有文字描述，缺乏定量表格，无法评估各组件独立贡献。
- **对比不够全面**：未与主流实时方法（如 HITNet、RT-MonSter、RAFT-Stereo 等）进行直接对比。
- **偏差风险**：不确定性伪标签的公式（指数衰减）中 $\beta$ 超参数的选择未讨论敏感性；训练仅在 SceneFlow 上，对真实场景中未见过的分布（如 Middlebury 纹理缺失）的适应性有待进一步验证。
- **应用限制**：方法假设输入为已矫正立体对，对未矫正或图像失配情形未考虑；实时性依赖于粗到细框架，在最细尺度上仍需要局部代价体构建，移动端部署的内存和带宽限制未讨论。
- **消融实验缺失**：未实验不同搜索半径 $r$ 的影响、不同不确定性损失权重 $\lambda_{unc}$ 的影响等。

（完）
