---
title: "From RGB Generation to Dense Field Readout: Pixel-Space Dense Prediction with Text-to-Image Models"
title_zh: 从RGB生成到密集场读出：使用文本到图像模型的像素空间密集预测
authors: "Zanyi Wang, Xin Lin, Haodong Li, Dengyang Jiang, Yijiang Li, Pengtao Xie"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06553v1"
tags: ["query:vfm"]
score: 9.0
evidence: 利用文生图模型进行密集预测（深度、法线、alpha抠图、掩码）
tldr: 现有方法将密集预测视为目标生成，依赖解码器重建图像，继承了生成输出界面的冗余。本文发现DiT通过图像平面的补丁-标记-补丁格点组织输入，每个标记索引固定输出补丁，可直接携带任务原生量。提出ReChannel，保留VAE编码器并丢弃解码器，用LoRA适配冻结DiT，通过共享线性头将每个标记映射到像素空间补丁，仅33K参数。在六项任务中超越先前方法，抠图、KITTI深度、指代分割达到新SOTA，速度提升2.48倍。证明密集预测可受益于生成预训练而无需继承其输出接口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06553v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 711}, {\"url\": \"assets/figures/arxiv/2607.06553v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2607.06553v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1794, \"height\": 748}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06553v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1619, \"height\": 654}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1424, \"height\": 584}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1306, \"height\": 602}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 621, \"height\": 367}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1273, \"height\": 584}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 370}]"
motivation: 现有方法继承生成输出界面，需解码器重建图像，带来冗余；密集预测实际需要像素准确的任务原生场。
method: 保留VAE编码器，丢弃目标侧解码器，用LoRA适配冻结DiT，每个标记通过共享线性头直接映射到像素空间补丁。
result: 在trimap-free抠图、KITTI深度、指代分割上取得新SOTA，法线、显著性、姿态上具有竞争力，速度比匹配基线快2.48倍。
conclusion: 密集预测可从生成预训练中获益，无需继承生成输出接口，ReChannel提供了更高效准确的方案。
---

## 摘要
大规模文本到图像模型是密集预测的有吸引力的骨干网络，因为RGB生成预训练学习了丰富的语义、结构和几何先验。现有的生成和编辑方法通过将密集预测视为目标生成来重用这些先验：诸如深度、法线、alpha遮罩、掩码和热力图等标注被编码到RGB训练的VAE潜空间中，然后解码回类似图像的目标。我们认为这继承了比密集预测所需更多的生成输出接口：与RGB合成不同，密集预测要求在同一图像平面上具有像素正确、任务原生的场，而不是要渲染的新RGB内容。我们的关键观察是，预训练的DiT已经通过图像平面上的块到令牌到块的网格组织RGB输入，因此每个令牌索引一个固定的输出块，其通道可以携带任务原生的量而不是RGB外观。我们将其实例化为ReChannel：我们保留用于DiT输入分布的VAE编码器，但丢弃目标侧的解码器，使用任务LoRA适配冻结的DiT，并通过共享的令牌局部线性头（约33K参数，无空间混合）将每个令牌映射到其p x p x K_t像素空间块。使用FLUX-Klein，我们在六项密集预测任务和十几个基准上进行了评估。这种最小接口在无trimap抠图、KITTI深度和指代分割上设立了新的最先进水平，并在法线、显著性和姿态上保持竞争力。在匹配的4B设置中，它比编辑加潜解码的对应方法更准确且快2.48倍——密集感知可以从生成预训练中受益，而无需继承其输出接口。

## Abstract
Large-scale text-to-image models are attractive backbones for dense prediction because RGB generation pretraining learns rich semantic, structural, and geometric priors. Existing generative and editing approaches reuse these priors by casting dense prediction as target generation: annotations such as depth, normals, alpha mattes, masks, and heatmaps are encoded into an RGB-trained VAE latent space and decoded back as image-like targets. We argue this inherits more of the generative output interface than dense prediction requires: unlike RGB synthesis, dense prediction asks for pixel-correct, task-native fields on the same image plane, not new RGB content to be rendered. Our key observation is that a pretrained DiT already organizes RGB inputs through a patch-to-token-to-patch lattice on the image plane, so each token indexes a fixed output patch whose channels can carry task-native quantities instead of RGB appearance. We instantiate this as ReChannel: we keep the VAE encoder for the DiT's input distribution but drop the target-side decoder, adapt the frozen DiT with task LoRA, and map each token to its p x p x K_t pixel-space patch through a shared token-local linear head--about 33K parameters, no spatial mixing. Using FLUX-Klein, we evaluate on six dense prediction tasks and over a dozen benchmarks. This minimal interface sets new state-of-the-art on trimap-free matting, KITTI depth, and referring segmentation, and stays competitive on normals, saliency, and pose. In a matched 4B setting it is more accurate and 2.48x faster than an edit-plus-latent-decode counterpart--dense perception can benefit from generative pretraining without inheriting its output interface.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有基于文本到图像（T2I）生成模型的密集预测方法（如深度、法线、抠图、分割、姿态、显著性）通常将密集预测视为“目标生成”——将标注（深度图、法线图等）编码到RGB训练的VAE潜空间，再通过解码器重建回像素空间。论文认为这种做法继承了生成模型的输出接口，而密集预测实际只需要在同一图像平面上输出像素准确的任务原生场（task-native fields），不需要渲染新RGB内容。
- **动机**：预训练的DiT（Diffusion Transformer）已经通过“块→令牌→块”的网格结构组织RGB输入，每个令牌索引一个固定的输出块。那么，是否可以“读出”（readout）这些令牌携带的任务原生量，而不是通过VAE解码生成图像？论文提出**ReChannel**，核心思想是“读出而非生成”。
- **关键观察**：RGB输入令牌场是高维的，而任务适配后的场坍缩到紧凑子空间（参与率PR从32.8降至1.1~4.2），说明令牌场已包含足够结构信息，只需线性重解释即可得到任务输出。

## 2. 论文提出的方法论

### 核心思想
- 保留预训练T2I模型的VAE编码器（保持输入分布），但**丢弃目标侧的VAE解码器**。
- 使用**任务LoRA**（Low-Rank Adaptation）适配冻结的DiT骨干网络（权重冻结，仅更新LoRA参数），使RGB令牌场向目标语义对齐。
- 每个适配后的令牌通过一个**共享的令牌局部线性投影头**（token-local linear head）直接映射到对应的像素空间块（p×p×K_t），无需空间混合或高容量解码器。

### 关键技术细节
- **公式**：给定输入图像x，先用VAE编码器得到潜特征，再通过冻结的DiT（零噪声模式）和LoRA得到任务令牌场Z_t = F_{θ+Δt}(VAE_enc(x); σ=0, c_t)，其中c_t为文本条件。
- **读出**：对每个令牌z_ij，应用线性投影Ŷ_ij = reshape(W_t z_ij + b_t)，得到p×p×K_t的像素块。K_t为任务输出通道数（深度1、法线3、姿态多通道热图等）。
- **训练**：像素空间损失（连续回归用L1/L2，掩码用二值交叉熵等）。
- **参数**：线性头仅约33K参数（远小于传统解码器），LoRA适配参数也极轻量。

## 3. 实验设计

### 使用数据集和基准
- **深度估计**：NYU（Eigen split）、KITTI（Garg crop）、ScanNet val，使用log-space尺度对齐后的absRel和δ₁指标。
- **表面法线**：NYU、ScanNet、iBims（D-SINE划分），报告平均角度误差和11.25°精度。
- **无trimap抠图**：P3M-500-P、P3M-500-NP、零样本AIM-500，报告SAD、MSE、Conn。
- **指代分割**：RefCOCO/RefCOCO+/RefCOCOg的8个拆分，报告cumulative cIoU。
- **人体姿态估计**：COCO val，使用YOLOv11x检测框协议，报告AP/AP₅₀。
- **显著性检测**：DUTS-TE、ECSSD，报告F_max和MAE。
- **消融实验**：在FLUX-Klein 4B、512²分辨率下比较头设计、适配必要性、初始化、额外容量（卷积头、全微调）、不同输出接口（潜目标、VAE解码、编辑范式）。
- **效率**：L40S GPU单流时间。

### 对比方法
- 针对每个任务对比了现有最优方法：
  - **深度/法线**：Omnidata v2、DAv2、DSINE、GeoWizard、Marigold、GenPercept、Lotus-D、Edit2Perc等。
  - **抠图**：SHM、HATT、MODNet、P3M-Net、ViTAE-S、GenPercept。
  - **指代分割**：CRIS、PolyFormer、LISA、GLaMM、PSALM、OMG-LLaVA、Text4Seg、READ、CoPRS、FCLM等（多基于7B~8B LLM）。
  - **姿态**：HRNet-W48、TokenPose-L、ViTPose-L、RTMO-l、DynPose。
  - **显著性**：ICON、MENet、VST++、VSCode-B、EVPv2。
- **特别对比**：在消融中与“编辑范式”（image-to-image editing）对比速度和精度。

## 4. 资源与算力

- 论文未明确说明训练使用的GPU数量、具体型号（除推理用L40S外）、训练时长。
- 推理效率：在单张L40S GPU、512²分辨率下，ReChannel（4B）运行时间为**47.7 ms**（与冻结DiT前向传播一致），线性头无额外成本。相比之下，潜目标/VAE解码变体需74.4 ms（1.56×慢），编辑范式需118.1 ms（2.48×慢）。
- 未提及训练资源细节（可能认为不核心）。

## 5. 实验数量与充分性

- **实验数量**：覆盖6类任务、超过12个基准数据集，每个任务至少对比5~10种现有方法。消融实验包含6种配置（头仅线性、随机初始化、全微调、卷积头、潜目标、VAE解码、编辑范式），并报告延迟。
- **充分性**：实验设计较为全面，涵盖连续回归（深度/法线/抠图/姿态）和离散分割（指代/显著性），并测试了零样本泛化（AIM-500）。消融逐一验证了LoRA的必要性、预训练的重要性、额外容量无益、目标侧VAE有害等核心论点。
- **公平性**：在匹配骨干网络（FLUX-Klein 4B）和分辨率下进行消融，对比方法与论文报告结果一致（引用来源）。但需注意：某些对比方法可能使用不同骨干（如Marigold用SD2.1），论文未进行随机种子或超参数严格对齐的再训练对比，而是直接引用文献最优值，可能存在微小数码差异。整体上实验客观，结论支撑充分。

## 6. 论文的主要结论与发现

- **ReChannel**在无trimap抠图（P3M-500-P/NP）、KITTI深度、指代分割上取得**新SOTA**，在法线、显著性、姿态上保持最强竞争力。
- 目标侧VAE解码环节是冗余的，移除后不仅精度提升，速度也快2.48倍（对比编辑范式），且更简单。
- **令牌局部线性头**足够：在匹配4B骨干下，13倍参数的卷积头或全微调4B骨干均未能超越线性头；说明输出空间结构已在DiT令牌场中，头只需“读出”而非“解码”。
- 生成预训练提供了强大的RGB原生组织能力，但密集预测不需要继承其输出接口；应“重用先验，而非界面”。

## 7. 优点

1. **设计简洁优雅**：仅需冻结骨干+LoRA+线性头，无复杂解码器、无目标VAE、无编辑循环，参数极少（~33K）。
2. **任务统一**：同一配方适用于几何、抠图、分割、姿态、显著性等多种任务，无需任务特定架构。
3. **高效**：推理快（47.7ms），且精度与速度同时提升，而非trade-off。
4. **思路新颖**：将密集预测重新定义为“场读出”（field readout）而非“目标生成”，揭示了预训练DiT令牌场的空间载体属性。
5. **实验全面**：在6类任务、12+基准上验证，消融充分，结论可信。

## 8. 不足与局限

1. **未提供训练资源细节**：没有说明训练时的GPU型号/数量/时长，复现成本不透明。
2. **骨干网络受限**：仅使用FLUX-Klein（4B/9B），未验证其他T2I模型（如Stable Diffusion、Seedream等），泛化性待验证。
3. **像素对齐任务只限于同一图像平面**：对于需要全局推理或非规则输出的任务（如全景分割、实例分割的排序ID、深度不确定度等）可能不直接适用。
4. **指代分割需要文本条件**：虽然用了无LLM的简单方式，但依赖T2I的文本编码能力，对于复杂推理可能不如LLM变体强（但已在8个拆分上SOTA）。
5. **消融中未探索不同LoRA rank或线性头维度的影响**：仅验证了必要性和足够性，未进行超参数敏感度分析。
6. **对比方法可能存在差异**：论文中对比的许多方法来自不同骨干和分辨率，小差异可能受这些因素影响，但总体趋势清晰。
7. **零样本泛化**：仅抠图任务测试了跨数据集（AIM-500），其他任务未验证零样本性能；ReChannel训练需要每个任务的标注数据。

（完）
