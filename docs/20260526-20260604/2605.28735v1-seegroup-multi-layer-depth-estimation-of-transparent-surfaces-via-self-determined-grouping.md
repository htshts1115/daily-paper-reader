---
title: "SeeGroup: Multi-Layer Depth Estimation of Transparent Surfaces via Self-Determined Grouping"
title_zh: "SeeGroup: 通过自确定分组的透明表面多层深度估计"
authors: "Hongyu Wen, Jia Deng"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28735v1"
tags: ["query:mono-depth"]
score: 9.0
evidence: 透明表面的多层深度估计
tldr: 透明物体多层深度估计中，现有方法采用预定义的前后顺序分组，限制了层分配灵活性。为此提出SeeGroup，将每像素多层深度建模为泊松点过程，将深度层视为沿射线发生的无序事件，并设计排列不变的似然损失以自适应分组。在LayeredDepth基准上，四元组相对深度准确率提升8.75个百分点。该方法无需预定义层数，能更灵活地处理多种深度分布。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28735v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1806, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28735v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 342, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28735v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1809, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28735v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1803, \"height\": 1580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28735v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1804, \"height\": 706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28735v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1796, \"height\": 1831, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28735v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1800, \"height\": 255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28735v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1799, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28735v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1802, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28735v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 861, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28735v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 862, \"height\": 104, \"label\": \"Table\"}]"
motivation: 现有预定义深度层分组策略限制了模型对透明物体多层几何的适应能力。
method: 将逐像素多层深度建模为点过程，通过排列不变的似然损失实现自适应层分组。
result: "在LayeredDepth基准上四元组相对深度准确率从61.34%提升至70.09%。"
conclusion: SeeGroup通过自适应分组显著提升了透明表面多层深度估计的准确性。
---

## 摘要
透明物体在日常生活中很常见，理解其多层深度（包括透明表面及其背后的物体）非常重要。现有的多层深度方法通常扩展单层预测。它们根据3D点的前后顺序定义层并顺序预测。然而，由于分层几何可以允许3D点有多个有效的分组，预定义的分组策略本质上具有局限性。在这项工作中，我们提出了SeeGroup，一种多层深度估计方法，避免施加预定义的分组，允许模型自适应地将表面分配给深度图。我们将逐像素多层深度公式化为一个点过程，将深度层视为沿每条相机射线的无序事件。这在观测到的深度层上产生了一个置换不变的似然，从而产生一个自然支持任意层分组的损失。实验表明，我们的方法显著推进了多层深度估计的最新水平，在LayeredDepth基准上将四元组相对深度准确率从61.34%提高到70.09%。代码可在https://github.com/princeton-vl/SeeGroup获取。

## Abstract
Transparent objects are common in daily life, and it is important to understand their multilayer depth, including the transparent surface and the objects behind it. Existing methods for multilayer depth typically extend single-layer prediction. They define layers by the front-to-back ordering of 3D points and predict the layers sequentially. However, as layered geometry can admit multiple valid groupings of 3D points into layers, a predefined grouping strategy is inherently restrictive. In this work, we propose SeeGroup, a multi-layer depth estimation method that avoids imposing a predefined grouping and allows the model itself to adaptively assign surfaces to depth maps. We formulate per-pixel multi-layer depth as a point process, treating depth layers as unordered events along each camera ray. This induces a permutation-invariant likelihood over the observed depth layers, yielding a loss that naturally supports arbitrary layer groupings. Experiments demonstrate that our method significantly advances the state of the art of multi-layer depth estimation, improving quadruplet relative depth accuracy on LayeredDepth benchmark from 61.34% to 70.09%. Code is available at https://github.com/princeton-vl/SeeGroup.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究问题**：透明物体（如玻璃、塑料容器）的多层深度估计。由于透明物体允许光线通过，同一像素可能对应多个深度层（如玻璃表面、内部物体、背景），传统单层深度估计方法无法处理这种多深度对应。
- **现有方法局限**：现有方法（如LayeredDepth）采用预定义的深度顺序分组（将第i近的深度组成第i张深度图），但该策略不能灵活适应不同场景。例如当两个透明平面部分重叠时，深度顺序分组会将不同物体的深度混入同一张图，导致几何和语义上的不连续。
- **本文目标**：提出一种无需预定义分组、能够自适应将不同表面分配到不同深度图的方法，提升多层深度估计的准确性和鲁棒性。

## 2. 方法论

### 核心思想
- **自确定分组**：模型不强制深度值按前后顺序输出，而是允许每个像素预测任意顺序的深度序列，在评估时再按大小排序。
- **将多层深度建模为点过程**：将沿相机射线的多个深度层视为随机事件，用强度函数（intensity function）表示深度位置出现表面的期望速率。
- **排列不变损失**：似然函数对深度值的排列不变，因此模型可以自由学习分组。

### 关键技术细节
1. **特征提取**：使用DINOv2-ViT-L作为骨干编码器，遵循Depth Anything V2的管线。
2. **循环分解模块**：
   - 由分解器D和重映射器R组成。在每一步i，分解器从当前残差特征图F_{i-1}中提取一个组件特征C_i，代表当前最显著的一组深度层。
   - 重映射器将C_i映射回特征空间，从F_{i-1}中减去，得到更新的残差特征F_i。
   - 迭代次数n=4，产生4个组件特征。
3. **强度函数参数化**：
   - 每个组件特征通过预测器P映射为一个Laplace分布（中心d_i和尺度b_i）。
   - 总体强度函数取各Laplace分量的最大值：Λ = max_i L_i。这种max-mixture鼓励不同分量专精于不同深度范围，防止模式坍塌。
4. **损失函数**：
   - **排列不变似然损失**：L_int = -∑_i log Λ(d_i)，鼓励强度在真实深度处高。
   - **分量覆盖损失**：L_cov = -∑_j log (max_i L_j(d_i))，惩罚没有匹配任何真实深度的分量，减少过预测。
   - **梯度匹配损失**L_gm：改善精细细节。
   - 总损失：L = λ_int L_int + λ_cov L_cov + λ_gm L_gm，其中λ_int=1.0, λ_cov=0.1, λ_gm=1.0。

### 推理阶段
- 从强度函数中提取峰值作为预测深度，相邻峰值距离小于0.02的合并。

## 3. 实验设计

### 数据集与基准
- **训练集**：LayeredDepth-Syn（程序化生成的合成数据集，基于Infinigen-Indoors），14800张训练图像，500张验证图像。
- **评估基准**：
  - **真实世界基准**：LayeredDepth测试集（1500张真实图像，14.2M相对深度元组），用于零样本评估。
  - **合成验证集**：LayeredDepth-Syn验证集，用于消融和对比（但属于域内评估，信息量有限）。
- **评估指标**：元组准确率（相对深度排序正确率），包括对(P)、三(T)、四(Q)元组。四元组最可靠。

### 对比方法
- 来自[47]的四种基线：Multi-head (NeWCRFs), Index Concat (NeWCRFs), Recurrent (NeWCRFs), Multi-head (DA v2)。其中Multi-head (DA v2)与本文共享相同的预训练单层深度骨干（Depth Anything V2/DINOv2-ViT-L）。

### 消融实验
- 模型架构：对比Multi-head、GRU、本文的循环分解模块（RD）。
- 强度参数化：加权混合、排序分布（有顺序）、max-mixture（本文）。
- 损失函数：SiLog、L1、L1+GM、Int+GM、Int+Cov+GM（本文完整版）。

## 4. 资源与算力

- **GPU**：4× L40 GPU。
- **训练步数**：250k步，batch size=4（每卡batch size？文中未明确单卡batch，但总batch size为4）。
- **优化器**：AdamW，初始学习率1e-5（主网络）和1e-6（骨干），权重衰减0.01，梯度裁剪0.1。
- **分辨率**：图像调整至518×518。
- **参数数量**：本文模型（RD）总参数量356M，去掉编码器后51M，少于Multi-head（428M/124M）和GRU（731M/427M）。前向时间：CPU 16.29s，GPU 9.41s，FLOPs 1.23T。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要表格（Table 1）：在LayeredDepth测试集上对比4个基线，报告15个指标（5个子集×3种元组）。
  - 消融实验（Table 2）：在验证集上对3个维度（架构、参数化、损失）进行消融，每个维度多个变体，共约10种设置。
  - 合成验证集（Table 3）：对比4种方法，报告4个指标（AbsRel、RMS、δ1、δ2）。
  - 参数/速度对比（Table 4）。
- **充分性与客观性**：
  - 消融实验覆盖了关键设计选择，且通过控制变量（使用较小骨干DINOv2-ViT-S进行消融以节省计算）保证了比较公平。
  - 主要结果在真实世界零样本设置下获得，具有较强说服力。
  - 表格中明确标注最佳、次佳，对比方法均来自同一篇论文[47]，基线设置一致。
  - 提供了定性结果（图4-6）辅助评估。
- **潜在不足**：合成数据集上的评估仅报告了4个指标，且本文略逊于Multi-head (DA v2)，但文中解释了原因（参数少、噪声边界等）。消融实验使用的骨干（ViT-S）与最终模型（ViT-L）不同，可能对消融结论的绝对数值有影响。

## 6. 主要结论与发现

- SeeGroup在LayeredDepth真实测试集上全面领先所有基线，四元组准确率从61.34%提升至70.09%（相对提升14.26%）。
- 在15个评估指标中，14个取得最佳，1个次佳。
- 模型参数量更少（51M vs 124M），推理效率更高。
- 自确定分组能处理复杂场景（如部分重叠透明平面），输出更平滑、更少伪影的背景深度图。
- 点过程公式化和max-mixture参数化有效建模了多层深度中的不确定性。

## 7. 优点

1. **新颖问题定义**：首次明确多层深度估计中的分组问题，并提出自适应解决方案。
2. **技术优雅**：利用点过程的排列不变性自然实现自确定分组，无需显式排序或分支。
3. **实验充分**：在真实世界基准上零样本评估，消融实验覆盖架构、参数化、损失三个维度。
4. **高效轻量**：比基线方法参数量更少、速度更快。
5. **泛化性强**：在复杂透明场景中表现稳定，背景层更平滑。

## 8. 不足与局限

1. **过预测问题**：如文中所述，在部分场景下模型会将玻璃瓶背面错误分裂为两层。
2. **分布外失败**：对高度偏离训练分布的图像（如极罕见透明物体配置）效果较差。
3. **合成训练数据噪声**：LayeredDepth-Syn的射线追踪深度边界噪声大，可能导致模型欠拟合某些细节。
4. **消融实验骨干不一致**：消融使用ViT-S，最终模型使用ViT-L，绝对性能对比存在尺度差异。
5. **域内评估信息有限**：合成验证集的对比因所有方法均在该数据上训练，可提供的信息较少。
6. **应用限制**：当前在真实场景中仍存在错误，高可靠性任务（如自动驾驶安全）需谨慎。

（完）
