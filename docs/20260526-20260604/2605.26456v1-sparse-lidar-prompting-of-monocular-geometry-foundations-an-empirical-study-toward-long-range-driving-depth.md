---
title: "Sparse-LiDAR Prompting of Monocular Geometry Foundations: An Empirical Study Toward Long-Range Driving Depth"
title_zh: 稀疏激光雷达对单目几何基础模型的提示：面向远距离驾驶深度的实证研究
authors: "Kai Zheng, Qiang Feng, Xingjian Liu, Wenquan Tan, Yuan Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26456v1"
tags: ["query:mono-depth"]
score: 9.0
evidence: 稀疏LiDAR提示的单目深度基础模型
tldr: "针对现有稀疏激光雷达深度模型缺乏远距离评估、且未结合点图基础模型的问题，提出SLIM方法，首次将MoGe-2适配稀疏激光雷达输入，采用部分卷积稀疏编码器和多尺度融合，实现密度无关训练。在虚拟KITTI和CARLA上，100-150米距离内将绝对相对误差降低39-51%，六个注入比例消融实验显示部分卷积方法在大多数设置下有效。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26456v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26456v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1179, \"height\": 625, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26456v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1460, \"height\": 489, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26456v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1572, \"height\": 518, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26456v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1279, \"height\": 727, \"label\": \"Table\"}]"
motivation: 现有稀疏激光雷达深度模型缺乏远距离评估，且未在点图基础模型上探索真正稀疏注入。
method: 将MoGe-2适配稀疏激光雷达，采用部分卷积稀疏编码器和多尺度融合颈，在五个尺度融合特征，使用密度无关训练。
result: "在虚拟KITTI和CARLA上，100-150米绝对相对误差降低39-51%，消融实验部分卷积在大多数设置下改进AbsRel和RMSE。"
conclusion: SLIM有效提升远距离深度估计精度，为稀疏激光雷达注入点图基础模型提供新思路。
---

## 摘要
基于稀疏激光雷达提示的深度基础模型（PromptDA、Prior Depth Anything、DMD3C）在室内场景或KITTI标准80米评估范围内表现出色。然而，仍存在两个局限：（i）在远距离驾驶场景（50-150米）中缺乏系统性的距离分层评估；（ii）以往基于视差基础模型的方法依赖于预插值的密集先验，尚未探索在点图基础模型（例如MoGe-2，NeurIPS 2025）上真正注入稀疏激光雷达。我们提出SLIM（稀疏激光雷达注入的单目几何），这是首次将MoGe-2适配为接受真正稀疏激光雷达输入的方法。SLIM集成了一个部分卷积稀疏编码器和一个多尺度融合颈部，该颈部在五个尺度上将激光雷达特征融合到点图解码器中。我们采用密度无关训练（随机注入比例在[0.005, 0.30]之间），使得单一模型能够适应不同的输入密度。在Virtual KITTI和CARLA上，SLIM在100-150米范围内将MoGe-2基线的绝对相对误差降低了约39-51%。在六种注入比例下的消融实验表明，部分卷积注入在Virtual KITTI的所有六种设置中均改善了AbsRel和RMSE；在CARLA上，六种设置中有五种改善了AbsRel（一种在0.015比例下接近平局，差异为0.0013），RMSE在不同编码器间相当，其中部分卷积在三种设置中有所改善（最高改善0.31单位），在另外三种设置中最多损失0.11单位。

## Abstract
Sparse-LiDAR-prompted depth foundation models (PromptDA, Prior Depth Anything, DMD3C) have shown strong results on indoor scenes or within KITTI's standard 80-meter evaluation cap. However, two limitations remain: (i) systematic distance-stratified evaluation in long-range driving regimes (50-150 m) is largely absent; (ii) prior approaches built on disparity-based foundations rely on pre-interpolated dense priors, leaving truly sparse LiDAR injection on point-map foundations (e.g., MoGe-2, NeurIPS 2025) unexplored. We present SLIM (Sparse-LiDAR Injected Monocular geometry), the first adaptation of MoGe-2 to accept truly sparse LiDAR input. SLIM integrates a partial-convolution sparse encoder with a multi-scale fusion neck that fuses LiDAR features into the point-map decoder at five scales. We adopt density-agnostic training (random injection ratio in [0.005, 0.30]) so a single model serves diverse input densities. On Virtual KITTI and CARLA, SLIM reduces the absolute relative error of the MoGe-2 baseline by approximately 39-51% at 100-150 m. Ablation across six injection ratios shows partial-convolution injection improves both AbsRel and RMSE on Virtual KITTI in all six settings; on CARLA, AbsRel improves in five of six settings (one near-tie at 0.015 differs by 0.0013), and RMSE is comparable across encoders, with partial-convolution improving in three settings (by up to 0.31 unit) and losing by at most 0.11 unit in the other three.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：自动驾驶系统在50–150米远距离下的可靠度量深度估计至关重要。然而，现有的稀疏激光雷达提示深度基础模型（如PromptDA、Prior Depth Anything、DMD3C）主要验证场景局限在室内（<10米）或KITTI标准80米评估上限内，**缺乏远距离驾驶场景（50–150米）的系统性距离分层评估**。
- **现有方法的局限**：
  - 大多数方法基于视差基础模型（如Depth Anything），且需要**预先将稀疏激光雷达插值成密集图**（如PromptDA），丢弃了稀疏投影的二元特性。
  - **点图基础模型**（如MoGe-2）能够直接回归度量尺度的3D点图，但此前未被用作稀疏激光雷达注入的基础，且其设计是纯单目的。
- **论文目标**：填补上述两个空白，提出SLIM（Sparse-LiDAR Injected Monocular geometry），**首次将MoGe-2适配为接受真正稀疏激光雷达输入**，并在远距离驾驶场景中进行距离分层评估。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 在MoGe-2单目基线基础上，增加一个**部分卷积稀疏编码器**和一个**多尺度融合颈**，将稀疏激光雷达特征以真正（非插值）形式注入视觉特征金字塔的五个尺度，实现**密度无关训练**（单一模型覆盖0.5%–30%注入比例范围）。

### 关键技术细节

#### 1. 稀疏深度编码器（Sparse Depth Encoder）
- 输入：稀疏激光雷达投影（有效像素比例在训练时从[0.005, 0.30]均匀采样）。
- 处理：使用**部分卷积（Partial Convolutions）**[8]，每一层执行掩码卷积，并根据感受野内有效像素计数归一化，同时传播有效性掩码到后续层。
- 输出：五个特征图，通道宽度分别为[32, 64, 128, 256, 512]。

#### 2. 多尺度融合颈（Multi-Scale Fusion Neck）
- 替换MoGe-2的单目ConvStack颈部。
- 每个FeatureFusionBlock将RGB特征和深度特征融合为：
  \[
  F_{\text{fused}} = F_{\text{rgb}} + \text{SE}(\text{DWConv}(\text{Conv}([F_{\text{rgb}}; F_{\text{depth}}])))
  \]
  其中SE为Squeeze-and-Excitation通道注意力（缩减率r=4），后接批量归一化（γ=0.01）。残差连接确保训练初期接近恒等行为，可稳定地从MoGe-2检查点微调。

#### 3. 密度无关训练（Density-Agnostic Training）
- 每一步训练时，注入比例从[0.005, 0.30]均匀采样。
- 辅助损失：边缘感知重加权（基于GT 3D邻近距离）、对数距离加权（替代传统1/z）、在有效激光雷达像素处施加稀疏深度一致性项。

#### 整体架构（图1）
- 视觉分支：MoGe-2骨干（DINOv2 ViT-S/14），从第5和第11层取中间特征，投射维度384，产生五级视觉特征金字塔（通道384,256,128,64,32）。
- 稀疏几何分支：局部稀疏深度填充（最近邻传播）→五阶段PartialConv编码器（步长[1,2,2,2,2]，通道[32,64,128,256,512]）→每级输出。
- 融合颈：在L0–L4五个级别将稀疏几何特征注入视觉金字塔。
- 解码器：输出度量3D点图；辅助MoGe-2头部预测表面法线、有效性掩码、全局度量尺度（通过CLS token和全局稀疏特征拼接的MLP）。

## 3. 实验设计：数据集、基准、对比方法

### 数据集
- **Virtual KITTI 2**（约21K帧）——合成驾驶场景。
- **CARLA**——合成驾驶场景，额外增强远距离小目标以探测论文关注的范围。
- 对两个数据集采用相同的增强方法。

### 基准（Baseline）
- **MoGe-2 ViT-S/14基线**（单目，无LiDAR注入，35.1M参数）。
- **预插值基线（Interpolation baseline）**：控制变量实验，对稀疏深度进行双线性预插值后再注入，遵循PromptDA的设计原则，但替换为与SLIM相同的融合颈，以单独比较稀疏编码方式的影响。

### 评估指标与设置
- **距离分层**：1–50米、50–100米、100–150米三个区间。
- **指标**：绝对相对误差（AbsRel）、均方根误差（RMSE）、δ<1.25阈值精度。
- **注入比例**：六种固定比例（0.005, 0.008, 0.010, 0.015, 0.020, 0.030）用于消融实验。
- **主要对比**：SLIM vs MoGe-2基线（表1），以及PartialConv vs Interpolation编码器（表2）。

### 定性比较
- 在Virtual KITTI上展示RGB、GT、MoGe-2、SLIM的深度图对比，标注远距离行人、交通标志、路灯杆等细节。

## 4. 资源与算力

论文中**未明确提及**使用的GPU型号、数量、训练时长等算力信息。仅提到模型参数量为35.1M（MoGe-2 ViT-S/14），但未说明实际训练或推理的硬件配置。

## 5. 实验数量与充分性

### 实验规模
- **距离分层评估**：在Virtual KITTI和CARLA两个数据集上，分三个距离区间（共9个组合）报告AbsRel、RMSE、δ1指标（表1）。
- **稀疏编码器消融**：在六种注入比例下，比较PartialConv与Interpolation编码器在AbsRel和RMSE上的表现（表2），共12个配置（每个数据集6个比例），每个配置均报告结果。
- **定性比较**：2个场景的可视化对比。

### 充分性评价
- **优点**：消融实验涵盖了从极稀疏（0.5%）到较密集（3%）的宽范围注入比例，距离分层覆盖了远距离区间，且包含两个不同合成数据集。控制变量实验（预插值基线）有效隔离了稀疏编码方式的影响。
- **不足**：
  - **仅限合成数据集**，缺乏真实世界的评估（作者在局限性中明确点出）。
  - **未与最先进方法（PromptDA、Prior Depth Anything、DMD3C）直接比较**，因为PromptDA架构需要预插值输入，架构不同，作者采用自己实现的预插值基线进行对比，但未报告这些方法在相同设置下的性能。
  - **缺少对真实传感器噪声、运动模糊、天气变化等的鲁棒性测试**。
  - **未进行多传感器模式（线扫描、foveated FOV）的适配实验**。
- **公平性**：对比基线MoGe-2使用相同骨干，消融实验控制除编码器外所有因素相同，评估指标标准，结果具有可比性。

## 6. 论文的主要结论与发现

1. **远距离性能显著提升**：在100–150米区间，SLIM相比MoGe-2基线将AbsRel降低50.8%（Virtual KITTI）和39.5%（CARLA）。在40–100米区间也有显著改善。
2. **误差稳定性**：SLIM的AbsRel曲线随距离增长保持相对平坦，而MoGe-2基线误差随距离急剧增大（图2）。
3. **部分卷积优于预插值**：在Virtual KITTI所有六种注入比例下，PartialConv注入在AbsRel和RMSE上均优于Interpolation；CARLA上AbsRel在五种比例下更优（一种接近平局差异0.0013），RMSE相当（三种更好，三种略差，最大损失0.11单位）。最稀疏比例（0.005）下优势最大，因为预插值引入最多虚假密集值。
4. **密度无关训练有效**：单一模型在0.5%–30%广泛范围内均能适应，无需针对传感器特定密度重新训练。

## 7. 优点：方法或实验设计上的亮点

- **首次将点图基础模型（MoGe-2）适配真正稀疏LiDAR输入**，不同于以往依赖预插值密集先验的方法。
- **距离分层评估**：填补了现有工作在远距离（>80米）评估的空白，提出了50–150米的系统评估框架。
- **密度无关训练**：通过随机采样注入比例实现单一模型泛化，实用性强。
- **消融实验设计严谨**：控制变量比较PartialConv vs. Interpolation，在同一融合颈框架下进行，结果可信。
- **模块化设计**：部分卷积稀疏编码器、残差融合颈、辅助损失等可迁移至其他单目基础模型。
- **开源潜力**：虽然论文未声明代码开源，但方法描述详细，易于复现。
- **远距离性能提升显著**：在合成数据上接近40–51%的相对误差降低，具有重要的应用价值。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

### 实验覆盖
- **仅使用合成数据集（Virtual KITTI 2和CARLA）**，缺乏真实驾驶场景（如KITTI、nuScenes、DDAD）的验证，存在域差异风险。作者也承认这是关键未来工作。
- **未与现有稀疏LiDAR提示模型（PromptDA、Prior Depth Anything、DMD3C）直接比较**。虽然架构差异导致比较困难，但缺少在相同设置下的性能对标，难以评估SLIM的相对优势。
- **未在真实传感器数据上测试运动模糊、天气、传感器噪声等影响**。

### 偏差风险
- 合成数据中物体纹理、光照、深度分布可能与真实驾驶场景存在偏差，可能高估了SLIM在真实场景中的泛化能力。
- 预插值基线未使用PromptDA的完整设计（如视觉特征融合方式），可能低估了预插值方法的潜力。

### 应用限制
- **延迟研究未进行**：虽然参数规模35.1M适合嵌入式部署，但实际推理速度未报告，实时性未知。
- **仅支持固定FOV和投射模式**：未针对线扫描、foveated FOV等工业扫描模式进行模式感知训练。
- **动态物体处理不足**：未集成动态感知的GT生成管线（如DOC-Depth），对运动物体可能产生误差。
- **未探索测试时适应（TTA）**：可进一步结合TTA-Depth等方法提升零样本性能。

### 其他
- **算力信息缺失**：不利于复现和效能评估。
- **定性结果仅展示两例**，可能选择偏向性案例，代表性有待加强。

（完）
