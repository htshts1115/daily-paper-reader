---
title: "SIGMA: Bridging Structural and Distributional Gaps for Vision Foundation Model Adaptation"
title_zh: "SIGMA: 弥合视觉基础模型自适应中的结构与分布差异"
authors: "Lingyu Xiong, Jinjin Shi, Xuran Xu, Cong Luo, Runyu Shi, Ying Huang"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27893v1"
tags: ["query:vfm"]
score: 9.0
evidence: 针对深度、分割等稠密预测任务的参数高效微调方法
tldr: "视觉基础模型全微调计算开销大，参数高效微调在密集预测任务中面临结构性差距和分布性差距。SIGMA提出尺度自适应融合模块增强多粒度信息提取，结合语义调制模块进行全局特征对齐，桥接两种差距。仅用1.72%可训练参数，在多个密集任务和多种骨干网络上超越现有方法。统一了空间与分布适应，为高效视觉适应提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1181, \"height\": 1037, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 997, \"height\": 1467, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1110, \"height\": 576, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 584, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1295, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 647, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有PEFT方法在视觉基础模型的密集预测任务中，因结构性差距（多尺度特征）和分布性差距（特征对齐）导致性能下降。
method: "SIGMA包含尺度自适应融合模块（多尺度特征聚合）和语义调制模块（全局特征对齐），仅需1.72%可训练参数。"
result: 在语义分割、深度估计等任务上，SIGMA一致超越LoRA、Adapter等主流PEFT方法，接近全微调性能。
conclusion: 轻量级适配可有效消除结构性和分布性差距，为视觉基础模型高效适应密集预测任务提供可行方案。
---

## 摘要
视觉基础模型（VFM）展现了令人印象深刻的表现能力。然而，通过全微调将其适配到下游任务会带来高昂的计算和存储开销。参数高效微调（PEFT）成为了一种有吸引力的替代方案，旨在以最小的训练成本达到与全微调相当的性能。尽管如此，由于结构和分布差异的存在，将PEFT应用于VFM进行密集预测任务仍然具有挑战性。为弥合这些差异，我们提出了尺度集成全局调制适配器（SIGMA），这是一种新颖的轻量级PEFT方法，包含两个模块：尺度自适应融合和语义调制。具体而言，尺度自适应融合模块用于通过增强多粒度视觉信息的提取来弥合结构差异。此外，SIGMA对融合特征引入语义调制以执行全局特征对齐，从而进一步消除分布差异。该设计促进了统一的空间与分布适配，相对于VFM骨干网络仅需1.72%的可训练参数。在多种下游密集任务和多种VFM骨干网络上进行的全面实验表明，SIGMA在性能上持续优于最先进的PEFT方法。

## Abstract
Vision Foundation Models (VFMs) have demonstrated impressive representational capabilities. However, adapting them to downstream tasks via full fine-tuning incurs prohibitive computational and storage overhead. Parameter-Efficient Fine-Tuning (PEFT) has emerged as a compelling alternative, aiming to achieve performance parity with full fine-tuning at minimal training costs. Nonetheless, applying PEFT to VFMs for dense prediction tasks remains challenging due to the structural and distributional gaps. To bridge these gaps, we propose \textbf{S}cale-\textbf{I}ntegrated \textbf{G}lobal \textbf{M}odulation \textbf{A}dapter (\textbf{SIGMA}), a novel lightweight PEFT method, which consists of two modules: scale-adaptive fusion and semantic modulation. Specifically, the scale-adaptive fusion module is utilized to bridge structural gaps by enhancing the extraction of multi-granularity visual information. Furthermore, SIGMA introduces semantic modulation on the fusion features to perform global feature alignment to further eliminate the distribution gap. This design facilitates unified spatial and distributional adaptation, requiring only 1.72\% trainable parameters relative to the VFM backbone. Comprehensive experiments across various downstream dense tasks and multiple VFM backbones demonstrate that SIGMA achieves consistent and superior performance over state-of-the-art PEFT methods.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：视觉基础模型（VFM）如 DINOv2、SigLIP2、SAM 在密集预测任务（目标检测、语义分割、深度估计）中表现出强大表示能力。
- **问题**：全微调（Full Fine-tuning）所有骨干参数需要极高的计算和存储开销；参数高效微调（PEFT）虽能大幅减少可训练参数，但在密集预测任务中性能远低于全微调。
- **根本原因**：作者识别出两个关键障碍：
  - **结构性差距**：现有 PEFT 方法（如 Adapter、LoRA）源自 NLP，仅通过线性变换处理 1D 序列，缺乏对 2D 空间局部性和多尺度上下文的捕获能力。
  - **分布性差距**：预训练数据集与下游任务数据之间存在统计分布差异（如特征均值和方差），现有适配器没有显式的分布对齐机制。
- **论文目标**：提出 SIGMA（Scale-Integrated Global Modulation Adapter），同时弥合结构性和分布性差距，实现高效且高性能的 VFM 适配，仅需 1.48M 可训练参数（约占骨干的 1.72%）。

## 2. 论文提出的方法论

### 核心思想
在冻结的 Vision Transformer 骨干的每个 Transformer 块中，在多头注意力和前馈网络之后插入轻量级 SIGMA 层。该层由两个协同工作的模块组成：**尺度自适应融合**解决结构性差距，**语义调制**解决分布性差距。

### 关键技术细节
1. **尺度自适应融合（Scale-Adaptive Fusion）**：
   - 输入特征经 LayerNorm 和下投影降维至瓶颈维度 \(r\)（实验中 \(r=32\)）。
   - 使用三个并行的深度可分离卷积（核大小 3×3、5×5、7×7）提取多尺度空间特征，然后通过平均聚合，并通过 1×1 逐点卷积进行特征融合。
   - 包含两个残差连接：一个在深度卷积与平均聚合之间，一个在逐点卷积之后，保留原始信息并促进梯度流动。
   - 最后经 GeLU 激活和上投影恢复原始维度。

2. **语义调制（Semantic Modulation）**：
   - 同样对下投影后的特征进行，通过线性投影生成四组尺度（\(\gamma\)）和偏移（\(\beta\)）参数，分别作用于三个深度卷积输出和一个逐点卷积输出。
   - 调制操作为：\(\text{Mod}(F, \gamma, \beta) = \gamma \odot F + \beta\)。
   - 显式调整各阶段特征统计量，使预训练表示与下游分布对齐。

3. **整体流程**（以 MHA 后的 SIGMA 层为例）：
   - 输入 \(x_0\) → LayerNorm → 下投影 → 深度卷积 + 调制 → 平均聚合 → 逐点卷积 + 调制 → 激活 → 上投影 → 残差连接输出。
   - 参数复杂度：\(9r^2 + (2d + 84)r + 3d\)（\(d\) 为隐藏维度，\(r\ll d\)），例如 \(d=768\) 时仅 1.48M 参数。

## 3. 实验设计

### 数据集与任务
| 任务 | 数据集 | 测试指标 | 采用的框架 |
|------|--------|----------|------------|
| 目标检测 | MS-COCO 2017（train2017/val2017） | mAP, mAP₅₀, APₛ, APₘ, APₗ | DETR |
| 语义分割 | ADE20K（150类别） | mIoU, mAcc, aAcc | Mask2Former（MMSegmentation） |
| 深度估计 | NYUv2 | δ₁↑, AbsRel↓, RMSE↓ | DPT |

### 骨干网络
- DINOv2 ViT-Base
- SigLIP2 ViT-Base
- SAM ViT-Base

### 对比方法
- Full tuning（全微调）
- Fixed（仅训练头部）
- BitFit（仅微调偏置）
- Partial-1（仅微调最后一个 Transformer 块）
- LoRA（低秩适配）
- Adapter（顺序瓶颈适配器）
- AdaptFormer（并行适配器）
- LoRand（低秩适配用于密集预测）
- VFM-Adapter（超网络动态生成卷积参数）

### 实现细节
- COCO：输入 640×640，AdamW，lr=1e-4，weight decay=0.05，30 epochs，batch size=32，8×A800。
- ADE20K：遵循 Mask2Former 默认配置，160k iterations，batch size=16，8×A800。
- NYUv2：输入 640×480，AdamW，lr=1.5e-4，weight decay=0.01，20 epochs，batch size=16，1×A800。

## 4. 资源与算力
- **硬件**：目标检测和语义分割使用 8×NVIDIA Tesla A800 GPU；深度估计使用 1×NVIDIA Tesla A800 GPU。
- **训练时长**：文中未明确报告每个实验的具体训练时间（如小时数），仅说明了迭代次数/epoch 数和 batch size。
- **参数效率**：SIGMA 仅 1.48M 可训练参数，而全微调骨干参数约 85.7M，LoRand 为 3.59M，VFM-Adapter 为 2.78M。

## 5. 实验数量与充分性
- **主要定量实验**：三张主表（表1 目标检测、表2 语义分割、表3 深度估计）覆盖三项任务 × 三个骨干，共 9 组对比实验，每项实验报告多个指标。
- **消融研究**：
  - 表4：不同中间维度（16/32/64/128）的影响，确定最优维度为 32。
  - 表5：模块组件消融（去尺度自适应融合、去语义调制、完整），验证各模块贡献。
- **总结**：实验设计较为全面，覆盖了主流密集预测任务、多样化的骨干网络（三种不同预训练范式）以及多种 SOTA PEFT 方法。消融实验合理，分析了核心设计选择。但缺少更多数据集的验证（如 Cityscapes 分割、KITTI 深度估计）以及更大骨干（ViT-L/H）的实验，也未与最新方法如 ARC 等对比（论文中未列出）。总体充分且公平。

## 6. 论文的主要结论与发现
- SIGMA 在所有三个任务和三种骨干上均显著优于所有对比的 PEFT 方法，且在某些情况下接近全微调性能。
- 例如：在 DINOv2 目标检测中，SIGMA 达到 54.4 mAP，比最强基线 VFM-Adapter（53.2）高 1.2 mAP，同时参数减少 47%。
- 在语义分割中，SIGMA 在 DINOv2 上达到 56.42 mIoU，与全微调 57.10 差距仅 0.68 mIoU。
- 消融证实，尺度自适应融合和语义调制均不可或缺，其中尺度自适应融合贡献更大（mAP 下降 2.2）。
- 核心结论：通过联合解决结构性和分布性差距，轻量级适配器可以在密集预测任务中实现高效且准确的迁移。

## 7. 优点
- **创新性**：首次将多尺度深度可分离卷积与特征调制结合用于 PEFT，针对性解决 VFM 密集预测中的两个关键差距。
- **参数高度高效**：仅 1.48M 参数（< 1.7% 骨干参数）即可超越使用更多参数的方法，如 LoRand（3.59M）、VFM-Adapter（2.78M）。
- **通用性**：在三种不同预训练策略（自监督、视觉-语言对比学习、提示分割）的 VFM 上均取得一致优势，表明方法具有跨模型泛化能力。
- **实验全面**：覆盖三个主流密集任务、多个骨干、多种 SOTA 基线，并提供了消融分析和维度影响分析。
- **代码与可复现性**：论文提供了清晰的框架和参数配置，便于复现。

## 8. 不足与局限
- **未明确讨论局限性**：论文没有“Limitations”部分，也未提及潜在弱点或失败场景。
- **骨干模型单一尺寸**：仅在 ViT-Base 上实验，未验证在更大模型（ViT-L、ViT-H）或不同架构（如 Swin Transformer 等）上的表现。
- **数据集覆盖有限**：仅三个数据集（COCO、ADE20K、NYUv2），缺少其他挑战性数据集（如 Cityscapes、KITTI）的验证，且未在图像修复、全景分割等任务上评价。
- **对比方法时效性**：与 LoRand、VFM-Adapter 等相比，未与更近期的 PEFT 方法（如 ARC、DyT）对比。
- **计算开销**：虽然参数少，但深度可分离卷积（特别是较大核 7×7）可能带来额外的推理计算量，论文未分析 FLOPs 或推理速度。
- **分布差距分析**：对分布性差距的定量评估（如特征统计量比较）未提供，仅通过消融间接验证。
- **潜在风险**：方法对超参（如瓶颈维度）敏感，需要调优；在多任务微调场景下的泛化能力未知。

（完）
