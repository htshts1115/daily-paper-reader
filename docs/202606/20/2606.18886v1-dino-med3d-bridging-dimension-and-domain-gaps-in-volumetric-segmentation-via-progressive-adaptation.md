---
title: "DINO-Med3D: Bridging Dimension and Domain Gaps in Volumetric Segmentation via Progressive Adaptation"
title_zh: "DINO-Med3D: 通过渐进式适应弥合体分割中的维度与域差距"
authors: "Haoyu Hu, Xiyao Ma, Shiqi Liu, Linsen Zhang, Xiaoliang Xie, Xiaohu Zhou, Zeng-Guang Hou"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18886v1"
tags: ["query:vfm"]
score: 9.0
evidence: 通过渐进适应和即插即用适配器将DINOv3视觉基础模型迁移到3D医学分割
tldr: 针对DINOv3在自然图像上表现优异但无法直接应用于3D医学分割的维度与领域差异问题，提出DINO-Med3D两阶段渐进框架。首先通过多切片嵌入和分割代理任务弥合维度与领域鸿沟，再插入轻量3D适配器增强体素连续性，最后用并行细节恢复流保留边界信息。在五个公开数据集上显著超越现有方法，实现了从2D预训练到3D医学任务的有效迁移。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18886v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1234, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18886v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18886v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1262, \"height\": 189, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18886v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18886v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 688, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18886v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1080, \"height\": 359, \"label\": \"Table\"}]"
motivation: 直接使用DINOv3进行3D医学分割存在维度与领域双重鸿沟，需设计渐进适应策略以充分利用其语义判别能力。
method: 两阶段框架：第一阶段多切片嵌入与分割代理任务对齐维度与领域；第二阶段在冻结骨干中加入3D适配器，辅以并行细节恢复流补偿空间信息损失。
result: 在五个公开医学分割数据集上，DINO-Med3D全面超越当前最先进方法，验证了其有效性和泛化性。
conclusion: 通过渐进式维度与领域适应，DINO-Med3D成功将DINOv3迁移至3D医学分割，为利用2D预训练模型处理体数据开辟了新路径。
---

## 摘要
尽管DINOv3在自然图像中展现了显著的语义辨别能力，但其直接应用于体积医学分割受到固有的维度和域差异的阻碍。为解决这些问题，我们提出DINO-Med3D，一个两阶段渐进框架，将预训练的DINOv3编码器重新用于3D医学任务。在第一阶段，通过引入融合伪3D上下文的多切片嵌入模块来缓解维度差距，同时采用分割代理任务将从自然场景学习到的表示适应到医学领域。随后，通过在冻结的主干网络中添加轻量级3D适配器来进一步增强体积理解，以强制执行全局层间连续性。最后，为补偿嵌入过程固有的空间信息损失，我们设计了一个并行细节恢复流，以显式保留高频边界线索。在五个公共数据集上的大量实验表明，我们的方法成功地将DINOv3适应到医学领域，并显著优于最先进的基线方法。

## Abstract
Although DINOv3 has demonstrated remarkable semantic discrimination in natural imagery, its direct application to volumetric medical segmentation is hindered by inherent dimension and domain disparities. To resolve these issues, we propose DINO-Med3D, a two-stage progressive framework that repurpose the pre-trained DINOv3 encoder for 3D medical tasks. In the first stage, we mitigate the dimension gap by introducing a multi-slice embedding module that incorporates pseudo-3D context, while simultaneously employing a segmentation proxy task to adapt representations learned from natural scenes to the medical domain. Subsequently, we further enhance volumetric understanding by adding lightweight 3D adapters into the frozen backbone to enforce global inter-slice continuity. Finally, to compensate for the spatial information loss inherent in the embedding process, we design a parallel detail recovery stream to explicitly preserve high-frequency boundary cues. Extensive experiments on five public datasets demonstrate that our approach successfully adapts DINOv3 to the medical domain and significantly outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **背景**：自监督视觉基础模型 DINOv3 在自然图像上展现出强大的语义识别能力，但直接应用于三维（3D）医学体分割面临双重障碍——**维度差距**（2D 预训练 vs 3D 体数据）和**域差距**（自然场景 vs 医学影像的低对比度、细微纹理）。
- **研究动机**：现有工作（如 Dino U‑Net）仅做简单微调或投影，难以有效弥合这两类差距，导致 DINOv3 在医学分割任务上性能显著低于专用框架（如 nnU‑Net）。
- **整体含义**：提出 DINO‑Med3D，一个**两阶段渐进式适配框架**，系统性地将 2D 的 DINOv3 编码器转化为 3D 医学分割网络，同时解决维度和域迁移问题。

## 2. 方法论
- **核心思想**：采用“先局部对齐，后全局增强”的渐进策略。第一阶段通过伪3D嵌入和代理任务初步弥合差距；第二阶段冻结对齐后的骨干，引入参数高效的3D适配器和并行细节恢复流，进一步强化体感知和边界信息。
- **关键技术细节**：
  - **第一阶段**：
    - **ICE（Inter-slice Context Embedding）**：用深度感知的3D卷积处理连续K个切片构成的子体积（slab），输出尺寸与DINOv3 2D patch embedding兼容的伪3D嵌入，保持层间上下文。
    - **代理分割任务**：用一个轻量MLP头预测中心切片的二值掩膜，全量微调ICE和骨干，强迫模型从自然特征向医学语义偏移。
  - **第二阶段**：
    - **Volumetric Context Encoding Stream**：保留第一阶段权重，冻结骨干，仅训练LoRA（r=16, α=16）和轻量3D Adapter（3D反卷积），将堆叠的伪3D特征转化为真实体积表示，再经UperNet解码得到粗语义特征`Fsem`。
    - **Detail‑Recovery Stream（HRSE）**：轻量3D残差CNN，保留全分辨率及1/2、1/4下采样特征`Fdet`，弥补patch嵌入丢失的高频边界信息。
    - **Adaptive Gated Fusion (AGF)**：生成空间注意力门`G = σ(K[Fsem; Fdet])`，对语义特征和细节特征进行内容感知的残差校正：`F̃det = Fsem + G⊙Fdet`，再逐级聚合得到最终预测。
- **公式/算法**：主要体现为ICE的3D卷积操作、AGF的门控融合公式（见式(2)(3)），以及两阶段训练流程的文字描述。

## 3. 实验设计
- **数据集**：5个公开数据集，涵盖CT和MRI模态：
  - CT：AISD（脑梗死）、MSD‑Pancreas（胰腺）、MSD‑Colon（结肠）。
  - MRI：BraTS 2020（脑肿瘤）、ACDC（心脏）。
- **基准方法**：nnU‑Net、nnFormer、SwinUNETR、U‑Mamba、VoCo（Swin‑L）、Dino U‑Net（直接使用DINOv3的2D适应方法）。
- **评价指标**：Dice相似系数（DSC↑）和Hausdorff距离（HD95↓）。
- **数据预处理**：统一重采样至512×512，CT使用特定窗宽窗位，MRI截断至[0.5, 99.5]百分位并归一化。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量及具体训练时长。
- 仅给出训练配置：两阶段各50 epochs，Stage I batch size=24，学习率2×10⁻⁵；Stage II batch size=1，学习率1×10⁻⁴，使用LoRA（rank=16）。
- 总体100 epochs，但具体计算资源（如GPU卡数、显存）信息缺失。

## 5. 实验数量与充分性
- 共进行**三组主要实验**：
  1. **全量对比**（表1）：在5个数据集上对比7种前沿方法（包括CNN、Transformer、Mamba、预训练方法），DSC和HD95双指标。
  2. **ICE输入切片数消融**（表2）：测试K=1~5，确定最优K=3。
  3. **组件消融**（表3）：分析Stage I、Stage II的Stream 1、Stream 2及解码器（MLP vs UperNet）的贡献。
- 另提供**定性可视化**（图2、图3），展示分割效果及细节恢复流的作用。
- **充分性评估**：实验覆盖多模态、多解剖结构、多对比方法，消融全面，结论可靠。但缺少在更大规模（如另外2个以上数据集）或长尾病变上的验证，泛化性有待扩展。

## 6. 主要结论与发现
- DINO‑Med3D在**所有5个数据集**上均取得最优DSC和HD95，显著优于现有SOTA（如nnU‑Net、Dino U‑Net）。
- 在**高变异数据**（如MSD‑Colon）上提升最大：DSC 62.28%，较nnFormer提升11.60%。
- 观察到**正缩放定律**：模型参数量从Small（47M）到Large（334M）性能单调提升。
- **Small变体（47M）已超越**更大的Dino U‑Net（318M），证明所提策略效率高。
- 消融表明：第一阶段全微调是后续LoRA成功的前提；ICE最优切片数为3；细节恢复流能有效提升边界精度。

## 7. 优点
- **渐进式设计**：先对齐域和低维特征，再增强体连续性和细节，逻辑清晰，避免一次性引入过多模块造成训练不稳定。
- **即插即用模块**：ICE、3D Adapter、LoRA、HRSE、AGF均轻量且可独立迁移，便于后续扩展。
- **参数高效**：第二阶段冻结主干仅训练少量适配参数，计算成本可控；Small变体参数量仅47M。
- **多维评估**：5个数据集、多种模态、与当前最强方法对比，实验充分、客观。
- **开源代码**：提供GitHub仓库，促进复现与应用。

## 8. 不足与局限
- **计算负担**：第一阶段仍需全量微调DINOv3骨干，对显存和时长要求较高，未来可探索更高效的参数对齐策略。
- **细节恢复流依赖纹理**：消融显示其对纹理复杂度敏感，在部分数据集（如BraTS）提升较小，可能在目标极微小或纹理一致时效果有限。
- **实验覆盖有限**：未在更多病理类型（如肝脏、肺部）、更大量级数据集上验证；未评估零样本/少样本场景。
- **资源信息缺失**：未报告GPU型号、数量及实际训练时间，不利于复现和公平比较。
- **超参数敏感**：ICE输入切片数K对性能影响较大（表2），需根据数据集手动调整，缺乏自适应机制。

（完）
