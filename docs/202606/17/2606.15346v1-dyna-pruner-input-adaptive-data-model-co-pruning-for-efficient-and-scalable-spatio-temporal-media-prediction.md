---
title: "DYNA-PRUNER: Input-Adaptive Data-Model Co-Pruning for Efficient and Scalable Spatio-Temporal Media Prediction"
title_zh: "DYNA-PRUNER: 输入自适应的数据-模型协同剪枝，用于高效可扩展的时空媒体预测"
authors: "Fuyan Zhang, Yuqi Li, Yingli Tian, Edmond S. L. Ho"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15346v1"
tags: ["query:lite-vision"]
score: 9.0
evidence: 输入自适应的数据-模型联合剪枝以实现高效时空预测
tldr: "时空预测模型计算密集，但输入数据存在冗余（如平静海面），导致实时部署困难。Dyna-Pruner框架通过共享重要性同步机制，实现输入自适应的数据-模型共剪枝，为每个样本生成稀疏子网络。在WeatherBench、SEVIR和TaxiBJ上，FLOPs减少70%，实际加速2.5倍，精度损失低于1%。该方法可无缝集成CNN、RNN和Transformer，实现自动化资源感知优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15346v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1676, \"height\": 844, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15346v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 919, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15346v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 286, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15346v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1692, \"height\": 629, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15346v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15346v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1795, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15346v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 432, \"label\": \"Table\"}]"
motivation: 现有时空预测模型计算与输入冗余不匹配，静态模型浪费资源，实时部署成本高。
method: 提出Dyna-Pruner，采用共享重要性同步机制生成耦合掩码，同时剪枝冗余数据区域和对应卷积滤波器，得到输入依赖的稀疏子网络。
result: "在WeatherBench、SEVIR和TaxiBJ上，FLOPs减少70%，NVIDIA Jetson AGX Orin加速2.5倍，精度损失小于1%。"
conclusion: 实现了输入自适应的共剪枝，支持多种骨干网络，显著提升推理效率，利于实时部署。
---

## 摘要
时空预测支持雷达/卫星临近预报和城市级交通监控，但现代模型往往因计算成本过高而难以实时部署。这源于密集计算与强输入依赖性冗余（例如平静的海面或晴朗的天空）之间的不匹配。为了实现可扩展媒体分析中的自动化资源感知架构优化，我们提出了Dyna-Pruner，一个用于数据与模型结构输入依赖协同剪枝的端到端框架。一种共享重要性同步机制生成耦合掩码，剪枝冗余区域及其对应的计算单元（例如卷积滤波器），从而在推理时产生每个样本的稀疏子网络。在WeatherBench、SEVIR和TaxiBJ上的实验表明，该方法与CNN、RNN和Transformer骨干网络无缝集成，将FLOPs降低高达70%，在NVIDIA Jetson AGX Orin上实现2.5倍加速，且精度损失可忽略不计（<1%）。

## Abstract
Spatio-temporal prediction supports radar/satellite nowcasting and city-scale traffic monitoring, but modern models are often too expensive for real-time deployment. This stems from a mismatch between dense computation and strong input-dependent redundancy (e.g., calm seas or clear skies). To enable automated, resource-aware architecture optimization in scalable media analysis, we propose Dyna-Pruner, an end-to-end framework for input-dependent co-pruning of data and model structure. A shared-importance synchronization mechanism generates coupled masks that prune redundant regions and their corresponding computational units (e.g., convolutional filters), yielding per-sample sparse sub-networks at inference time. Experiments on WeatherBench, SEVIR, and TaxiBJ show seamless integration with CNN, RNN, and Transformer backbones, reducing FLOPs by up to $70\%$ and achieving a $2.5\times$ speedup on NVIDIA Jetson AGX Orin with negligible accuracy loss ($<1\%$).

---

## 论文详细总结（自动生成）

### 论文总结：Dyna-Pruner——输入自适应的数据-模型协同剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有时空预测模型（如天气、交通预测）采用密集计算范式，但输入数据通常存在大量冗余（例如平静海面、晴朗天空），导致计算资源浪费，难以在边缘设备上实时部署。
- **背景**：传统模型压缩（如权重剪枝）是输入无关的静态方法，无法动态适应不同样本的信息密度；而单纯的数据简化（如稀疏采样）忽略了模型端的优化。两者缺乏协同，导致精度与效率的次优权衡。

#### 2. 方法论
- **核心思想**：提出 **Dyna-Pruner**，一种端到端的输入自适应协同剪枝框架，通过**共享重要性同步机制**同时生成数据掩码和模型掩码，实现每个样本的稀疏子网络。
- **关键技术细节**：
  - **重要性分数图** $S \in [0,1]^{H \times W}$：由轻量级掩码生成器 $g_{mask}(X; \phi)$ 从输入 $X$ 动态生成，表示每个空间位置对预测的重要性。
  - **数据掩码** $M_{data}$：训练时用 $S$ 作为软掩码（$X' = X \odot S$），推理时通过 Straight-Through Estimator (STE) 二值化为硬掩码。
  - **模型掩码** $M_{weight}$：对每个卷积滤波器，通过感受野聚合重要性分数得到标量 $I_k$，与可学习阈值 $\tau$ 比较决定是否剪枝。
  - **损失函数**：$L_{total} = L_{task} + \lambda_d \|S\|_1 + \lambda_w \|I\|_1$，其中 $L_{task}$ 为任务损失（如 MSE），$\lambda_d, \lambda_w$ 控制稀疏正则化强度。
  - **训练与推理**：统一优化模型参数 $\theta$ 和掩码生成器参数 $\phi$；推理时仅需一次前向计算掩码，即可执行稀疏计算。

#### 3. 实验设计
- **数据集**：
  - WeatherBench（中期天气预报，大面积冗余）
  - SEVIR（风暴事件临近预报，稀疏突发）
  - TaxiBJ（城市交通流量预测）
- **骨干网络**：SimVP (CNN)、ConvLSTM (RNN)、TAU (Transformer)
- **对比方法**：
  - Dense Model（原始密集模型）
  - Model Pruning (MP)（基于 L1 范数的滤波器剪枝，与 Dyna-Pruner 保持相同稀疏度）
- **评价指标**：预测精度（MSE、MAE）、计算效率（GFLOPs、NVIDIA Jetson AGX Orin 上的推理延迟）

#### 4. 资源与算力
- **训练硬件**：4 块 NVIDIA A100 GPU
- **优化器**：AdamW
- **训练时长**：论文未明确说明，但提到所有实验均在此配置下完成。

#### 5. 实验数量与充分性
- **主实验**：在 3 个数据集 × 3 个骨干网络上对比 Dyna-Pruner、Dense、MP，共 9 组配置（见表 1）。
- **鲁棒性分析**：在 Gaussian 噪声（σ=0.01, 0.05）和掩码失效（5%, 10%）条件下对比（见表 2）。
- **参数敏感性**：对数据稀疏度 $s_d$ 和模型稀疏度 $s_w$ 进行 5 种组合测试（见表 3）。
- **消融实验**：分别移除数据剪枝、模型剪枝、协同机制，验证每个组件必要性（见表 4）。
- **可视化分析**：展示重要性分数图与未来变化幅度的空间对齐（图 3）。
- **充分性评价**：实验覆盖了不同架构、数据集、噪声干扰、稀疏度场景及消融，量足且公平（与同等 FLOPs 的 MP 对比），结论客观可靠。

#### 6. 主要结论与发现
- **有效性**：Dyna-Pruner 在相似 FLOPs 下精度损失（<2%）远小于 MP（4-7%），实际加速比（2.5 倍）也优于 MP（约 1.8 倍）。
- **通用性**：可无缝集成 CNN、RNN、Transformer，且性能提升模式一致。
- **鲁棒性**：在噪声和掩码失效下，精度下降幅度显著小于 Dense 和随机剪枝。
- **协同关键性**：消融表明，数据与模型独立剪枝或非协同组合均不如协同机制，验证了共享重要性同步的核心作用。

#### 7. 优点
- **创新性**：首次提出输入自适应的数据-模型协同剪枝框架，突破静态、输入无关压缩的局限。
- **端到端可训练**：通过连续重要性分数和 STE 支持梯度反传，无需两阶段训练。
- **高效推理**：推理时仅需一次掩码生成，即可实现稀疏计算，实际延迟降低明显（因同时减少数据访问和计算）。
- **可解释性**：重要性分数图能聚焦物理活跃区域（如气旋），与气象先验一致。
- **鲁棒性**：对噪声和掩码失效具有天然容错能力。

#### 8. 不足与局限
- **稀疏度范围有限**：论文仅测试 70% 稀疏度下的默认设置，更高稀疏度（如 90%）精度下降明显，未探讨自适应稀疏度策略。
- **额外开销**：掩码生成器引入少量额外计算和参数，论文未对比该部分在极低算力设备上的影响。
- **超参数敏感性**：$\lambda_d, \lambda_w$ 需要人工调节，对不同数据集可能需重新调参。
- **泛化性验证不足**：仅评估三种视觉骨干，未在图神经网络、物理信息网络等架构上测试。
- **对比基线有限**：未与动态稀疏训练方法（如 DynST）或基于重要性的数据剪枝方法直接比较，削弱了 SOTA 定位。
- **未公开代码**：论文称代码将公开，但在发布时尚未提供，影响可复现性。

（完）
