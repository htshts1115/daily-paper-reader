---
title: Leveraging Metric Depth for Relative Depth Prediction
title_zh: 利用度量深度进行相对深度预测
authors: "Xiaoyang Bi, Shuaikun Liu, Zhaohong Liu, Yuxin Yang, Zhe Zhao, Mengshi Qi, Liang Liu, Huadong Ma"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10628v1"
tags: ["query:mono-depth"]
score: 9.0
evidence: 利用度量深度辅助相对深度预测，零样本预训练模型
tldr: 足球场景相对深度预测面临训练样本不足的挑战。为此，提出利用大规模数据集预训练模型的零样本能力来学习度量深度，并将其迁移到相对深度预测任务中。该方法在SoccerNet单目深度估计竞赛中取得了2.68×10⁻³的分数，验证了度量深度预训练的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 687, \"height\": 569, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10628v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 855, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10628v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 794, \"height\": 457, \"label\": \"Table\"}]"
motivation: 足球场景相对深度预测的训练样本仅数千，导致模型泛化困难。
method: 利用预训练模型的零样本能力学习度量深度，再迁移至相对深度预测。
result: 在SoccerNet挑战集上得到2.68×10⁻³的分数。
conclusion: 度量深度预训练能有效提升相对深度预测性能。
---

## 摘要
我们提出了针对2025年SoccerNet单目深度估计竞赛挑战的解决方案。在足球场景中预测相对深度具有挑战性，特别是仅有数千个训练样本可用的情况下。为了解决这个问题，我们的方法利用了在大规模数据集上预训练的模型强大的零样本能力来学习度量深度，从而实现有效的相对深度预测，在挑战集上取得了$2.68 \times 10^{-3}$的分数。

## Abstract
We present our solution to the 2025 SoccerNet Monocular Depth Estimation Competition Challenge. Predicting the relative depth in football scenarios is challenging, especially with only thousands of training samples available. To address this issue, our method leverages the powerful zero-shot capabilities of models pretrained on large-scale datasets to learn metric depth for effective relative depth prediction, achieving a score of $2.68 \times 10^{-3}$ on the challenge set.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：足球场景下的单目相对深度预测面临训练样本严重不足（仅数千张标注图像）的挑战，直接训练难以获得良好泛化。
- **研究动机**：传统相对深度预测需要大量标注，而SoccerNet-Depth数据集只提供相对深度（非米制绝对值）。然而，对于多数视觉任务，点之间的序关系比绝对尺度更重要，因此可以利用大规模预训练模型学到的**度量深度**（metric depth）作为代理，通过微调使其适应相对深度预测任务。
- **整体含义**：提出一种将预训练零样本深度模型（Depth Anything）迁移到小样本足球场景的方法，通过将相对深度视为0–256米范围内的度量值进行训练，实现了有效的相对深度预测，并在竞赛中取得优异成绩。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用预训练的大规模零样本深度估计模型（Depth Anything）的强大特征表示能力，将其编码器冻结，仅训练一个基于LocalBins的度量深度解码器（ZoeDepth改进版），将相对深度视为度量深度进行端到端微调。
- **关键技术细节**：
  - **网络架构**：Depth Anything编码器（基于DinoV2，预训练于6200万无标签图像）；ZoeDepth解码器（随机初始化，仅此部分可训练）。
  - **训练目标**：采用尺度不变对数损失（scale-invariant logarithmic loss），舍弃Chamfer损失（因高内存成本且收益有限）。
  - **数据增广**：仅使用水平翻转（训练和测试时）；其他增广（颜色抖动、随机旋转、多尺度测试增广、模型集成）均导致性能下降。
  - **训练细节**：输入图像缩放至384×704，输出上采样至原始分辨率（1080×1920）；优化器AdamW，学习率6.44×10⁻⁴，100 epoch，前70% epoch保持恒定，后30%用余弦退火降至1/10000。
- **算法流程**（文字说明）：
  1. 加载预训练的Depth Anything编码器（冻结）。
  2. 随机初始化ZoeDepth解码器。
  3. 将训练图像（含相对深度标注）作为输入，解码器输出度量深度预测。
  4. 计算预测与真实深度之间的尺度不变对数损失。
  5. 反向传播更新解码器参数。
  6. 测试时将原始图像和水平翻转图像分别输入，取预测均值。

## 3. 实验设计
- **数据集**：SoccerNet-Depth（体育视频单目深度数据集，包含篮球和足球场景）。竞赛仅使用足球场景，提供数千张相对深度标注样本。
- **Benchmark**：2025 SoccerNet Monocular Depth Estimation Competition Challenge，评价指标为RMSE（均方根误差）。
- **对比方法**：
  - 在不同学习率（1.61×10⁻⁴ vs 6.44×10⁻⁴）和训练轮数（50, 80, 100）下进行自对比。
  - 对比了Depth Anything V2（使用作者推荐学习率）作为主干。
  - 对比了不同数据增广（无增广、仅水平翻转、加颜色抖动、加随机旋转）在2 epoch后的效果。
  - 对比了测试时增广（多尺度0.5/1.0/1.5）和两模型集成的效果。
- **未与其他公开方法对比**：论文仅报告了自己方法的消融实验和最终竞赛得分，未列出其他参赛队伍结果（竞赛结果未公开）。

## 4. 资源与算力
- **GPU型号与数量**：8块RTX 4090 GPU。
- **批量大小**：每GPU batch size = 1（由于图像分辨率大，内存受限）。
- **训练时长**：训练100个epoch（具体总时间未说明）。
- **说明**：文中明确给出了硬件资源和超参数设置。

## 5. 实验数量与充分性
- **实验组数**：
  - 主干与超参数消融：4组（不同学习率和epoch，见表1）。
  - 训练时增广消融：4组（无、水平翻转、颜色抖动、随机旋转，见表2前4行）。
  - 测试时增广与集成消融：3组（翻转、多尺度、集成，见表2后4行）。
  - 另对Depth Anything V2进行了对比（1组）。
- **充分性评价**：
  - 消融实验覆盖了关键超参数（学习率、epoch数）、数据增广策略、测试时技术。
  - 但仅使用了RMSE单一指标，未提供其他深度估计指标（如δ1、δ2等）。
  - 未与其他独立方法或基线（如直接使用Depth Anything零样本预测）对比，仅在方法内部进行消融。
  - 实验规模较小（竞赛限定，且仅数千样本），但消融设计合理，结论明确。

## 6. 论文的主要结论与发现
- 通过将相对深度视为度量深度（0–256米范围），微调预训练的Depth Anything模型，可以实现有效的足球场景相对深度预测，最终RMSE为2.68×10⁻³。
- **关键发现**：
  - 较高的学习率（6.44×10⁻⁴）和更长的训练轮数（100 epoch）有利于收敛。
  - 只有水平翻转增广有效；其他增广（颜色抖动、随机旋转、多尺度、集成）均导致性能下降。
  - Depth Anything V2在该设置下表现更差（RMSE 2.63×10⁻³ vs DA的1.37×10⁻³），可能需要进一步调参。

## 7. 优点
- **方法简洁高效**：仅需微调解码器（编码器冻结），计算开销小，充分利用预训练零样本能力。
- **数据增广谨慎**：通过实验排除了有害增广，避免了过拟合或不匹配问题。
- **超参数调优细致**：分析了学习率和训练轮数的交互效应，并可视化训练曲线。
- **针对小样本场景**：有效解决了标注不足的困难，为体育视频深度估计提供了实用方案。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅使用RMSE单一指标，缺乏绝对相对误差（AbsRel）、δ阈值等常见深度估计指标。
  - 未与现有的相对深度预测方法（如MiDaS、DPT等零样本模型）进行直接对比，无法评估方法的相对优势。
  - 仅针对足球场景实验，未验证其他体育场景（如篮球）或一般场景的泛化性。
- **方法局限性**：
  - 依赖将相对深度视为0–256米均匀尺度，但真实相对深度分布可能并非均匀，可能引入偏差。
  - 模型只在SoccerNet数据集上训练，该数据由游戏引擎渲染，与真实拍摄视频可能存在域差异。
  - 冻结编码器可能限制了自适应能力；未能探索全微调或LoRA等高效调优。
- **资源与可复现性**：
  - 使用8×RTX 4090，普通实验室可能难以复现（但batch size=1，可通过梯度累积以单卡训练）。
  - 未提供代码或模型权重，可复现性存疑。
- **消融深度不足**：
  - 未对损失函数中的不同项（如是否使用SiLog vs 其他损失）进行消融。
  - 未探索不同解码器架构（如直接线性层 vs LocalBins）的影响。

（完）
