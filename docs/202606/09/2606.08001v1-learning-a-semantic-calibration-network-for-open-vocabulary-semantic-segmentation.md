---
title: Learning a Semantic Calibration Network for Open-Vocabulary Semantic Segmentation
title_zh: 学习用于开放词汇语义分割的语义校准网络
authors: "Yang Sun, Tao Wang, Anastasia Ioannou, Ge Xu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08001v1"
tags: ["query:seg"]
score: 9.0
evidence: 开放词汇语义分割
tldr: 开放词汇语义分割旨在分割任意文本描述的类别，但存在语义歧义问题。本文提出语义校准网络SCN，通过类歧义消除模块捕获类间依赖，生成视觉感知伪文本嵌入，并利用logits融合模块整合多源证据。在多个基准上，SCN显著优于现有方法，同时保持CLIP的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 743, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1408, \"height\": 731, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1415, \"height\": 994, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 682, \"height\": 685, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 194, \"label\": \"Table\"}]"
motivation: 现有方法未显式建模类间语义相关性，导致开放词汇场景下分割性能受限。
method: SCN包含类歧义消除（交叉注意力生成伪文本嵌入，残差结构捕获类间依赖）和logits融合（动态整合多源语义证据）两个核心模块。
result: 在多个主流基准上，SCN超越了一系列现有开放词汇语义分割方法，验证了语义校准的有效性。
conclusion: 通过显式建模类间语义关联并融合多源语义证据，SCN有效提升了开放词汇分割的准确性和鲁棒性。
---

## 摘要
语义图像分割为每个像素分配预定义的类别标签，最近取得了显著进展。开放词汇分割（OVS）将分割任务从固定集合扩展到开放集合，使得能够基于任意文本输入（如类别名称或描述）识别和分割新概念。在本文中，我们提出了一种新颖的语义校准网络（SCN），用于开放词汇语义分割。与先前侧重于特征聚合或简单微调预训练模型的方法不同，SCN通过显式建模类间语义相关性来细化掩码分类过程，旨在增强模型的判别能力，同时有效保持预训练CLIP模型的泛化能力。具体而言，SCN包含两个核心组件：类别消歧（CD）和对数融合（LF）。首先，利用交叉注意力机制将文本嵌入转换为视觉感知的伪文本嵌入，以导出增强的相似度分数，补充原始掩码-文本相似度分数。随后，类别消歧模块通过残差架构捕获隐式类间依赖关系，有效解决语义歧义。最后，对数融合模块动态整合多方面的语义证据，确保模型在保持CLIP固有泛化能力的同时，达成稳健的语义共识。在主流基准上的综合实验结果表明，与最先进算法相比，所提方法实现了显著的性能提升。

## Abstract
Semantic image segmentation assigns a predefined category label to each pixel, has achieved significant progress lately. Open-Vocabulary Segmentation (OVS) extends the segmentation task from a fixed set to an open set, enabling the identification and segmentation of novel concepts based on arbitrary text inputs, such as category names or descriptions. In this paper, we propose a novel Semantic Calibration Network (SCN) for open-vocabulary semantic segmentation. Different from prior approaches that focus on feature aggregation or simple fine-tuning of pre-trained models, SCN refines the mask classification process by explicitly modeling the semantic correlations between classes, aiming to enhance the model's discriminative power while effectively preserving the generalization abilities of the pre-trained CLIP model. Specifically, SCN comprises two core components: Class Disambiguation (CD) and Logits Fusion (LF). First, a cross-attention mechanism is utilized to transform the text embeddings into visually aware pseudo-text embeddings, in order to derive an enhanced similarity score that complements the original mask-text similarity score. Subsequently, the Class Disambiguation module captures implicit inter-class dependencies through a residual architecture to effectively resolve semantic ambiguities. Finally, the Logits Fusion module dynamically integrates multifaceted semantic evidence to ensure that the model achieves a robust semantic consensus while maintaining CLIP's inherent generalization capability. Comprehensive experimental results on mainstream benchmarks demonstrate that the proposed method achieves significant performance improvements compared to state-of-the-art algorithms.

---

## 论文详细总结（自动生成）

# 论文总结：《Learning a Semantic Calibration Network for Open-Vocabulary Semantic Segmentation》

## 1. 核心问题与整体含义

- **研究背景**：开放词汇语义分割（OVS）旨在利用预训练的视觉语言模型（如CLIP）对任意文本描述的类别进行像素级分割。现有“先分割后识别”范式的主流方法中，性能瓶颈已从掩码生成转移到掩码分类阶段，容易出现语义混淆。
- **核心问题**：CLIP的图像-文本对齐能力基于全局语义，与语义分割所需的细粒度像素级标注存在粒度差异；直接微调易导致在训练类别上过拟合，破坏CLIP的零样本泛化能力。
- **论文目标**：提出**语义校准网络（Semantic Calibration Network, SCN）**，通过显式建模类间语义相关性来精炼特征表示，增强模型对语义歧义类别的判别能力，同时保持CLIP的泛化能力。

## 2. 方法论

### 核心思想
在掩码分类的相似度（logit）空间直接建模类间依赖关系，利用交叉注意力生成视觉感知的伪文本嵌入，并通过动态融合多源语义证据获得校准后的分类分数。

### 关键技术细节

1. **总体框架**（见图1）：输入图像和文本描述 → CLIP视觉/文本编码器 → 掩码生成器 → 掩码池化得到掩码特征Fm → SCN输出最终相似度分数Q。

2. **双相似度计算**：
   - **标准相似度 S**：S = Fm^T Ft，即掩码嵌入与文本嵌入的点积。
   - **增强相似度 T**：通过交叉注意力将文本特征作为查询，视觉特征作为键/值，得到视觉感知伪文本嵌入 F'_t，再计算 T = Fm^T F'_t，采用残差结构保留原始文本信息。

3. **类消歧模块（Class Disambiguation, CD）**：
   - 输入：原始相似度 S 和掩码特征 Fm。
   - 通过三个轻量适配器（A1、A2、A3）学习隐式类间依赖，A1处理S，A2处理Fm，A3融合两者，残差连接输出精炼相似度 R = S + A3(A1(S) + A2(Fm))。
   - 直接对logits操作以保持CLIP原始嵌入几何和相似度排序。

4. **对数融合模块（Logits Fusion, LF）**：
   - 利用轻量生成器从掩码特征Fm生成融合权重α = Generator(Fm)。
   - 加权融合：Q = α·T + (1-α)·R，最终Q用于掩码分类。
   - 动态整合标准相似度（经CD精炼后）和增强相似度，平衡泛化与判别能力。

5. **参数细节**（表I）：适配器A1~A3及生成器均由1x1卷积、BN、ReLU等组成，轻量高效，整体可端到端训练，使用标准交叉熵损失。

## 3. 实验设计

### 数据集
- **训练集**：COCO-Stuff（约118k图像，171类）
- **测试基准**：
  - ADE20K：A-150（150类）和 A-847（847类）
  - PASCAL-Context：PC-59（59类）和 PC-459（459类）
  - PASCAL VOC：PAS-20（20类）
- **评价指标**：mIoU（平均交并比）

### 对比方法
ZegFormer、DeOP、OvSeg、SAN、OpenSeg、EBSeg、SED、CAT-Seg、MAFTP、MAFTP w/ MaskAdapter（基线）等，涵盖不同VLM规模（ViT-B/16、ConvNeXt-B等）。

### 主要结果（表II）
- SCN在A-150（36.2）、A-847（14.7）、PC-59（58.6）上取得最优结果，在PC-459（18.8）和PAS-20（95.1）上与基线持平或更好。
- 相较于基线（MaskAdapter），分别提升0.6、0.5、0.2、0.9、0.0 mIoU。

## 4. 资源与算力

- **框架**：Detectron2
- **VLM**：CLIP ConvNeXt-B backbone，特征维度640
- **优化器**：AdamW，初始学习率1e-4，权重衰减0.05，多步学习率衰减
- **训练配置**：batch size 12，5个epoch，图像分辨率1024×1024（训练）/ 896×896（评估）
- **GPU**：2块NVIDIA RTX A6000
- 未明确报告总训练时长，但5 epoch、batch 12下，2块A6000预计数小时内完成。

## 5. 实验数量与充分性

- **定量对比实验**：在5个benchmark上与9+种方法比较，覆盖不同难度和粒度。
- **消融实验**（表III）：对基线+CD、基线+CD+LF逐步消融，验证每个模块的有效性（A-150上从35.6→35.9→36.2）。
- **定性结果**：图3展示3个案例，突出对语义混淆（如椅/扶手椅、树、门）的改善。
- **充分性评价**：实验设计较为全面，但缺少以下内容：
  - 不同融合权重生成策略的对比。
  - 在更多训练集（如同时使用COCO+其他数据）上的泛化测试。
  - 对模型计算开销/推理速度的量化。
  - 仅使用单一backbone（ConvNeXt-B），未测试更大/其他VLM。
- **客观性**：与基线在同一设置下比较，公平；但未开源代码，复现性待验证。

## 6. 主要结论与发现

- SCN通过在相似度空间显式建模类间依赖并整合多源语义证据，显著提升了开放词汇语义分割的准确性，尤其在语义歧义类别上。
- 类消歧模块利用残差结构有效捕获隐式类间依赖，不会破坏CLIP的原始相似度排序，从而保持零样本泛化能力。
- 对数融合模块动态平衡标准相似度和增强相似度，实现鲁棒的语义共识。
- 实验结果证明SCN在主流基准上达到SOTA，验证了语义校准策略的有效性。

## 7. 优点

- **创新性**：首次在相似度（logit）空间显式建模类间依赖进行语义校准，区别于以往仅聚焦特征聚合的方法。
- **简洁高效**：模块设计轻量（1x1卷积+残差），可无缝嵌入现有mask-based OVS框架，端到端训练。
- **泛化保持**：通过残差结构保留原始相似度排序，有效避免过拟合。
- **实验充分**：在多个数据集和多种设置下进行对比和消融，定性可视化直观。
- **实用价值**：方法不依赖复杂先验或额外数据，易于复现和部署。

## 8. 不足与局限

- **实验覆盖**：仅使用COCO-Stuff单一训练集，未验证在多个训练源下的泛化能力；未在更大规模VLM（如ViT-L/14）上测试。
- **偏差风险**：训练类别分布偏向COCO-Stuff的171类，可能对长尾或域外类别推广有限。
- **计算开销未量化**：仅说明使用2块A6000，未给出推理速度或参数量对比，实用性评估不足。
- **设计限制**：CD模块的适配器结构较简单，对复杂类间关系建模能力可能有限；融合权重仅依赖掩码特征，未利用视觉或文本上下文。
- **应用限制**：对于严重遮挡、小目标或极细粒度类别（如A-847中的罕见类），改进幅度相对较小（+0.5 mIoU）。
- **可复现性**：未提供开源代码，关键超参数细节（如适配器维度、学习率调度策略等）不够完整。

（完）
