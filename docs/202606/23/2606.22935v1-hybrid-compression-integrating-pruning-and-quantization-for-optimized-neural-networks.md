---
title: "Hybrid Compression: Integrating Pruning and Quantization for Optimized Neural Networks"
title_zh: 混合压缩：集成剪枝与量化的优化神经网络方法
authors: "Minh-Loi Nguyen, Long-Bao Nguyen, Van-Hieu Huynh, Minh-Triet Tran, Trung-Nghia Le"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22935v1"
tags: ["query:lite-vision"]
score: 9.0
evidence: 通过剪枝和量化进行模型压缩
tldr: 深度神经网络在边缘设备部署时面临内存和计算资源限制。本文提出一种混合压缩方法，先通过剪枝和量化大幅缩小模型，再引入混合专家架构路由多个压缩模型以恢复性能。在基准数据集上，CNN模型的FLOPs和参数显著下降，精度损失可忽略。该方法有效平衡了模型压缩与推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1003, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1240, \"height\": 793, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1262, \"height\": 316, \"label\": \"Table\"}]"
motivation: 边缘设备资源有限，需要模型压缩技术在减小模型尺寸的同时保持高精度。
method: 两阶段压缩：首先使用剪枝和量化减少模型大小，然后通过混合专家（MoE）路由多个压缩模型以提升性能。
result: 在多个基准数据集上，CNN模型的FLOPs和参数量大幅降低，准确率几乎无下降。
conclusion: 提出了一种混合压缩策略，结合剪枝、量化与MoE，实现了模型大小与性能的平衡。
---

## 摘要
深度神经网络近年来取得了显著进展，已成为各种应用中不可或缺的一部分。然而，伴随这些发展，在嵌入式设备和边缘设备上训练和部署神经网络模型面临内存和计算资源有限的重大挑战。这些问题可以通过深度神经网络压缩来解决，这涉及到模型大小与性能之间的权衡。本文提出了一种通过两个阶段进行模型压缩的新方法。首先，我们利用剪枝和量化等模型压缩技术大幅减小模型大小。然后，我们使用混合专家（MoE）来路由先前压缩的模型，以在保持推理效率平衡的同时提升性能。MoE由多个中等大小且性能稳定的专家模型（即压缩模型）组成。在多个基准数据集上的实验结果表明，我们的方法成功压缩了CNN模型，实现了FLOPs和参数数量的显著减少，而准确率下降可以忽略不计。

## Abstract
Deep neural networks have witnessed remarkable advancements in recent years and have become integral to various applications. However, alongside these developments, training and deployment of neural network models on embedding and edge devices face significant challenges due to limited memory and computational resources. These problems can be addressed with deep neural network compression, which involves a trade-off between model size and performance. In this paper, we propose a novel method for model compression through two phases. First, we utilize model compression techniques, such as pruning and quantization, to significantly reduce the model size. Then, we use Mixture of Experts to route the previously compressed models to enhance performance while maintaining a balance in inference efficiency. MoEs consist of multiple expert models (i.e., compressed models) that are moderately sized and deliver stable performance. Experimental results on several benchmark datasets show that our method successfully compresses CNN models which achieves substantial reductions in FLOPs and parameters with a negligible accuracy drop.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：深度神经网络在嵌入式设备和边缘设备上部署时，面临内存和计算资源严重受限的挑战。随着模型参数规模激增（从低于1百万到1750亿），直接部署变得不切实际。
- **研究动机**：需要一种高效的模型压缩方法，在显著减小模型尺寸和计算开销的同时，尽可能保持或恢复模型精度，使得复杂模型能在资源受限的Io T、移动端等场景落地。
- **整体含义**：本文提出了一种结合剪枝、量化与混合专家（Mo E）的混合压缩框架，通过“硬压缩”减负，再通过“软路由”恢复性能，为平衡模型压缩比与下游任务表现提供了一种新思路。

## 2. 论文提出的方法论

- **核心思想**：采用三阶段流水线：先剪枝，后量化，最后用Mo E路由多个压缩后的专家模型，从而在保持高效推理的前提下提升精度。
- **关键技术细节**：
  - **剪枝**：使用**自动渐进剪枝**，基于幅度剪枝（Magnitude Pruning）逐轮增加稀疏度，公式为：
    \[
    s_t = s_f + (s_i - s_f) \left(1 - \frac{t - t_0}{n \Delta t}\right)^3
    \]
    其中稀疏度随训练步数从初始值 \(s_i\) 平滑增长至目标值 \(s_f\)，避免层坍塌。剪枝后使用**Model Speedup**技术移除被剪的特征图以加速推理。
  - **量化**：采用**量化感知训练**，采用8位整数量化，定义量化步长 \(\Delta = \frac{\max(x) - \min(x)}{2^n - 1}\)，并在训练中模拟伪量化操作（公式4），利用直通估计器使梯度反向传播。损失函数加入量化损失项。
  - **Mixture of Experts**：将多个压缩后的专家模型（如VGG16、ResNet18等压缩版）与一个路由网络（门控网络）组合。门控采用**Top-k Noisy Gating**，通过添加可调高斯噪声实现概率化选择，公式：
    \[
    H(x)_i = (x \cdot W_g)_i + \epsilon \cdot \text{Softplus}((x \cdot W_{\text{noise}})_i)
    \]
    只选取 top-k 专家参与计算，其余置为 \(-\infty\)，防止专家坍塌。
- **算法流程**：预训练→剪枝（AGP）→量化（QAT）→重训练→Mo E集成与端到端训练。

## 3. 实验设计

- **数据集**：
  - **CIFAR-10**（60,000张32×32彩色图像，10类）
  - **BloodMNIST**（17,092张血细胞图像，8类）
- **基准模型**：VGG16、ResNet18、InceptionV3、DenseNet121（均为标准CNN架构）
- **对比方法**：
  - 未压缩的原始模型（表1中的“Original”行）
  - 本文提出的压缩模型（“Ours”行）
  - 压缩模型通过Mo E集成后的整体性能（“MOE (Ours)”行）
- **指标**：总参数量、FLOPs、推理时间（秒）、准确率。
- **缺乏外部对比**：未与现有的其他压缩方法（如Deep Compression、结构化剪枝等）进行直接基准对比，仅展示自身压缩前后的变化。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长或内存消耗等算力信息。仅提到使用PyTorch和NNI框架实现自动化剪枝与量化。
- **推断**：基于常见CNN模型规模（如VGG16 21M参数）和两个数据集，训练可能在单卡或双卡GPU（如RTX 3080/3090或更高）上完成，但无法确认。

## 5. 实验数量与充分性

- **实验数量**：共测试了4个模型×2个数据集=8组压缩前后对比，外加1个Mo E集成结果（表1）。实验中还给出了CIFAR-10上的准确率随FLOPs减少的图示（图1）。
- **充分性评估**：
  - **正面**：覆盖多样化的CNN变体（VGG、ResNet、Inception、DenseNet），验证了方法的通用性；数据集涵盖自然图像和医学图像，体现一定领域泛化性。
  - **不足**：缺少消融实验（如单独剪枝 vs 单独量化 vs 两者结合的效果对比）；未与SOTA压缩方法（如彩票假设、知识蒸馏等）定量比较；Mo E部分仅报告整体准确率，未分析不同专家数量或路由策略的敏感性；推理速度仅在单一硬件上测量，缺乏跨平台验证。

## 6. 论文的主要结论与发现

- 所提出的剪枝+量化压缩方法可将FLOPs减少10.5–11倍，参数量减少约10.5倍，同时精度下降可忽略（大部分模型降幅<3%），部分模型（InceptionV3、DenseNet121）压缩后准确率反而略有提升。
- 推理时间显著降低（如VGG16从0.162秒降至0.041秒）。
- 集成Mo E后，准确率进一步提升：CIFAR-10达到92.1%（优于所有单个压缩模型），BloodMNIST达到96.9%（接近或超过原始模型），表明Mo E能有效弥补压缩带来的精度损失。

## 7. 优点

- **技术融合创新**：首次将剪枝、量化与Mo E系统性地结合在一个流水线中，利用Mo E的择专能力补偿硬压缩的精度下降。
- **渐进剪枝策略**：采用AGP配合全局幅度剪枝，避免层坍塌，同时保持训练稳定性。
- **量化感知训练**：通过STE和伪量化使模型自适应低精度，比后训练量化精度更好。
- **Mo E路由防坍塌**：采用Top-k Noisy Gating，引入噪声和稀疏选择，提升专家多样性，降低不平衡风险。
- **自动化实现**：借助NNI框架降低实践门槛，便于复现。

## 8. 不足与局限

- **消融分析缺失**：无法量化剪枝和量化各自对精度和压缩比的贡献，也未分析不同稀疏度或量化位宽的影响。
- **对比基准有限**：未与现有主流压缩方法（如结构化剪枝、蒸馏、量化感知训练baseline等）进行公平比较，说服力不足。
- **Mo E细节不足**：未说明专家模型的数量、训练Mo E所需的额外计算开销、门控网络结构、以及是否需要对专家进行同步或异步更新。
- **实验覆盖窄**：仅限图像分类任务和CNN模型，未涉及Transformer、目标检测、语义分割等更广泛场景；数据集仅有2个，且BloodMNIST引用可能非真实（[5]为虚构名），影响可复现性。
- **资源开销不透明**：缺少训练Mo E的阶段所需的算力和时间，难以评估实际部署的成本效益。
- **推理速度测量不严谨**：仅给出绝对推理时间，未说明硬件平台、批次大小、精度（FP32 vs INT8）等关键细节。

（完）
