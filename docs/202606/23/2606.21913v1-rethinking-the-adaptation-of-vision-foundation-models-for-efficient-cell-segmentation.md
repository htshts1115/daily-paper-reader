---
title: Rethinking the Adaptation of Vision Foundation Models for Efficient Cell Segmentation
title_zh: 重新思考用于高效细胞分割的视觉基础模型的适应
authors: "Qing Xu, Xiangjian He, Wenting Duan, Jiebo Luo, Zhen Chen"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21913v1"
tags: ["query:vfm"]
score: 8.0
evidence: 视觉基础模型在分割任务上的适配
tldr: 针对细胞分割任务，现有视觉基础模型（VFM）的微调范式计算成本高且依赖大规模标注。本文提出EffiCell-Seg框架，通过冻结VFM编码器，设计CSP-Encoder提取全局显著性与局部形态先验，并利用SM-Decoder联合预测几何距离场与语义图。在多种细胞成像模态下，仅需约5M可训练参数，性能全面超越完全微调方法，参数减少130倍以上。该工作为高效医学图像分析提供了无需重训练编码器的轻量级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21913v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1142, \"height\": 329, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1190, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1142, \"height\": 320, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21913v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 319, \"label\": \"Table\"}]"
motivation: 解决VFM在细胞分割中微调计算开销大、依赖大量标注的问题，实现高效适配。
method: 提出CSP-Encoder从冻结VFM中合成语义显著性与形态先验，SM-Decoder通过互引导联合预测几何距离场与语义图。
result: 在多种细胞成像数据集上超越现有方法，仅需约5M可训练参数，较全微调减少130倍以上。
conclusion: EffiCell-Seg无需重训练编码器即可高效分割细胞，为医学图像分析提供轻量级新范式。
---

## 摘要
细胞分割对于计算病理学和生物医学发现至关重要。尽管最近的视觉基础模型（VFM）展示了卓越的通用特征表示，但释放其在细胞成像中的全部潜力目前受到资源密集型适应范式的瓶颈限制。现有方法通常依赖微调重型视觉编码器，导致大量计算开销和对大规模标注的依赖。为解决这一问题，我们提出了EffiCell-Seg框架，用于在无需重新训练视觉编码器的情况下实现高效细胞分割。我们的核心洞察是，预训练的VFM本质上编码了互补的结构先验：用于定位潜在细胞的全局显著性，以及用于描绘细胞结构的局部形态模式。为了利用这些先验，我们设计了一个细胞结构提示编码器（CSP-Encoder），该编码器将来自冻结VFM表示的语义感知显著性和主要形态特征合成为显式的结构先验图。此外，我们提出了一种协同掩码解码器（SM-Decoder），通过相互交叉引导联合预测几何距离场和语义图，从而强制上下文一致性。大量实验表明，EffiCell-Seg在多种细胞成像模态下优于最先进的方法，同时仅需约500万个可训练参数，比完全微调的VFM方法少130倍以上。代码可在https://github.com/xq141839/EffiCell-Seg获取。

## Abstract
Cell segmentation is critical for computational pathology and biomedical discovery. While recent Vision Foundation Models (VFMs) have demonstrated remarkable universal feature representations, unlocking their full potential for cellular imaging is currently bottlenecked by resource-intensive adaptation paradigms. Existing methods typically rely on fine-tuning heavy visual encoders, leading to extensive computational overhead and a dependency on large-scale annotations. To address this, we propose the EffiCell-Seg framework for highly efficient cell segmentation without re-training the visual encoder. Our core insight is that pretrained VFMs intrinsically encode complementary structural priors: global saliency for localizing potential cells, and local morphological patterns for delineating cellular structures. To harness these priors, we devise a Cell Structure Prompt Encoder (CSP-Encoder) that synthesizes semantic-aware saliency and principal morphological features from frozen VFM representations into explicit structural prior maps. Moreover, we propose a Synergistic Mask Decoder (SM-Decoder) that enforces contextual consistency by jointly predicting geometric distance fields and semantic maps via mutual cross-guidance. Extensive experiments demonstrate that EffiCell-Seg outperforms state-of-the-art methods across diverse cell imaging modalities while requiring only ~5M trainable parameters, over 130x fewer than fully fine-tuned VFM counterparts. The code is available at https://github.com/xq141839/EffiCell-Seg.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **动机**：细胞分割是计算病理学和生物医学发现的关键任务，但面临成像模态多样、细胞形态复杂等挑战。现有的视觉基础模型（VFM）虽然具有强大的通用表示能力，但在适配到细胞分割时存在资源密集型瓶颈：完全微调重型视觉编码器需要大量计算和标注数据，而参数高效微调（如Adapter、LoRA）仍依赖重新训练编码器特征表示，且需要大规模像素级标注。
- **问题**：如何在不重新训练视觉编码器（即冻结VFM）的情况下，高效利用VFM中已有的结构先验知识，实现高性能的细胞实例分割。
- **意义**：提出一种轻量级适配范式，大幅降低参数和计算开销，同时保持甚至超越现有的全微调方法，为医学图像分析提供更实用的解决方案。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：预训练VFM（如DINOv3）的CLS token和空间token分别编码了全局显著性和局部形态模式这两种互补的结构先验。通过提取并融合这些先验，生成结构提示，再协同预测几何距离场和语义图，即可在不重新训练编码器的情况下实现准确分割。
- **关键技术细节**：
  - **Cell Structure Prompt Encoder (CSP-Encoder)**：
    - 将CLS token与每个空间token计算余弦相似度，得到语义感知的显著性图（`a_i`），用于定位细胞区域。
    - 对空间token进行PCA降维，保留前K个主成分（`m_i`），捕获形态和纹理变化（如边界、纹理）。
    - 通过逐元素相乘融合两者，得到结构先验图（SPM），再经轻量卷积块生成提示嵌入`z`。
  - **Synergistic Mask Decoder (SM-Decoder)**：
    - 维护两组可学习查询：几何查询`q_g`（用于距离场）和语义查询`q_s`（用于语义图）。
    - 使用双向Transformer初始化两组表示，并通过交叉注意力机制（Geo-to-Sem和Sem-to-Geo）实现几何与语义的相互引导，确保两者一致。
    - 最终通过轻量转置卷积预测水平/垂直距离场和语义图。
  - **优化**：冻结VFM，仅训练CSP-Encoder和SM-Decoder。损失函数为交叉熵 + Dice损失（语义） + MSE损失（距离场），用超参数λ平衡。推理时对距离场应用分水岭算法，结合语义图分离实例。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - CellSeg数据集：1101张多模态显微镜图像，涵盖不同细胞类型（如浆细胞、白细胞）。
  - DSB数据集：670张图像（2018 Data Science Bowl挑战赛）。
  - 两者均按7:1:2划分为训练/验证/测试集。
- **基准与对比方法**：
  - 经典方法：CPPNet、Swin-UMamba。
  - VFM微调方法：CellSAM（全微调）、PromptNucSeg（训练辅助模型）、CellViT、SAC（部分微调）。
- **评估指标**：语义分割用Dice、mIoU；实例分割用Panoptic Quality (PQ)、F1 score。

### 4. 资源与算力
- 论文明确提到在**单个NVIDIA H20 GPU**上进行所有实验，使用PyTorch。批量大小16，训练100个epoch，优化器为AdamW，学习率1e-4。但未说明具体训练时间（小时）或使用的GPU数量。文中指出EffiCell-Seg仅需约4.99M训练参数，而全微调方法（如CellSAM）需682.92M参数，差距超130倍。

### 5. 实验数量与充分性
- **实验数量**：两个标准数据集（CellSeg、DSB）上的主实验（对比多种方法），以及消融实验（验证CSP-Encoder的显著性图和PCA图、SM-Decoder的贡献）。主实验包括语义分割和实例分割两套完整结果，消融实验展示了逐步添加组件的效果。此外提供了定性结果图（图3）。
- **充分性与公平性**：
  - 对比方法覆盖了经典方法和最新VFM适配方法，且均在相同训练集、相同输入分辨率（512×512）下微调或训练，SAM类方法使用自动提示模式。指标全面（Dice、mIoU、PQ、F1）。
  - 消融实验设计合理：基线 → 添加相似度图 → 添加PCA图 → 添加CSP-Encoder完整 → 添加SM-Decoder，逐步验证各模块必要性。
  - 存在局限性：未在更多模态（如电子显微镜、活细胞成像）上验证，也未与更多基于Transformer的医学分割框架（如nnUNet）比较。

### 6. 论文的主要结论与发现
- EffiCell-Seg在CellSeg和DSB两个数据集上，语义分割和实例分割指标均全面超越所有对比方法（包括全微调和部分微调方法），且仅需约5M可训练参数（比CellSAM少130倍以上）。
- 核心发现：冻结VFM编码器，仅通过轻量模块重新对齐其固有的结构先验，比重新训练编码器更高效、更鲁棒。全局显著性为定位提供区域级指导，局部形态为精细结构提供细节，两者互补性强。
- 协同预测几何和语义（SM-Decoder）可有效解决低对比度区域和粘连细胞的分割难题。

### 7. 优点
- **高效性**：参数极少（~5M），训练成本低，适合资源受限场景（如临床部署、大型实验）。
- **创新性**：首次提出完全冻结VFM编码器，仅通过重新对齐其内部先验实现细胞分割，摆脱了对大量标注数据和昂贵微调的依赖。
- **技术设计**：CSP-Encoder巧妙利用CLS token与空间token的天然异质性；SM-Decoder的交叉引导机制优雅地融合了几何和语义信息，避免了单一任务的不一致。
- **实验充分**：在多个指标上验证了有效性，消融实验清晰展示贡献。
- **代码开源**：提升可复现性。

### 8. 不足与局限
- **实验覆盖有限**：仅在两个数据集（CellSeg和DSB）上验证，它们虽是标准基准但细胞类型和染色方式有限，未涵盖更多临床病理切片或活细胞成像。结论的泛化性需要更多模态数据检验。
- **未与更多非VFM基线比较**：如经典的U-Net、Cellpose、nnUNet等，尽管论文中对比了CPPNet和Swin-UMamba，但Cellpose在细胞分割领域广泛使用，缺少对比可能削弱结论的说服力。
- **资源报告不完整**：未提供具体训练时间、显存占用等，使读者难以评估实际效率。
- **依赖分水岭后处理**：推理时依赖分水岭算法，该算法对超参数敏感，可能影响实例分割稳定性。
- **未讨论失败案例**：缺乏对模型在模糊、严重重叠、异质性极大细胞场景下表现的定性分析。

（完）
