---
title: Prompt-Calibrated SAM 3 for Open-Vocabulary Remote Sensing Semantic Segmentation
title_zh: 面向开放词汇遥感语义分割的提示校准SAM 3
authors: "Yanghui Song, Nanqing Liu, Haonan Yin, Yingjie Gao, Chengfu Yang, Qi Ming"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21863v1"
tags: ["query:seg"]
score: 9.0
evidence: 开放词汇语义分割，通过提示校准优化
tldr: "遥感开放词汇语义分割面临类别复杂、提示覆盖不足及噪声传播问题。ProC-SAM3从三方面校准SAM3提示：构建离线提示池优化类别提示，缓存文本嵌入避免重复编码，提出存在指导残差融合与峰值保留聚合抑制噪声。在8个基准上平均mIoU 56.1%，提升3.9个百分点，代码开源。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21863v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1854, \"height\": 975, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 426, \"height\": 250, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 426, \"height\": 246, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21863v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1681, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21863v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 602, \"height\": 194, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21863v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 836, \"height\": 260, \"label\": \"Table\"}]"
motivation: 现有SAM3方法单提示语义覆盖不足、多提示编码冗余且直接聚合传播噪声。
method: 构建离线提示池（类别匹配器分组MLLM候选+扩展约束精炼），缓存文本嵌入，采用存在指导残差融合与峰值保留聚合。
result: "在8个基准数据集上平均mIoU 56.1%，较前最佳无训练方法提升3.9%。"
conclusion: ProC-SAM3通过全面校准提示接口，实现了高效精准的遥感开放词汇语义分割。
---

## 摘要
遥感图像中的开放词汇语义分割（OVSS）旨在分割固定标签空间之外的类别。近期基于SAM 3的方法提供了有前景的无训练基础，但仍存在三个关键问题：（1）单个类名提示对于复杂遥感类别的语义覆盖不足；（2）将每个类别扩展为多个提示会引入冗余的在线文本编码；（3）直接聚合多个提示响应会将噪声激活传播到最终预测中。为解决这些问题，我们提出ProC-SAM3，该方法从三个互补方面校准SAM 3的提示接口以用于遥感OVSS。首先，我们构建一个离线提示池，其中类别匹配器将MLLM生成的候选提示分组到每个类别的集合中，扩展约束进一步利用类别先验知识细化每个集合。其次，得到的文本嵌入被缓存并在所有测试图像中重用，消除了重复的文本编码。第三，我们引入存在引导残差融合，根据提示存在和置信度对不可靠的解码器输出进行门控，然后通过保留峰值类别聚合来保留小目标和稀疏目标的细粒度激活。在八个基准上的实验表明，ProC-SAM3的平均mIoU达到56.1%，比之前最佳的无训练方法高出3.9个百分点。代码将在https://github.com/YanghuiSong/ProC-SAM3上提供。

## Abstract
Open-vocabulary semantic segmentation (OVSS) in remote sensing images aims to segment categories beyond a fixed label space. Recent SAM 3-based methods provide a promising training-free foundation, yet three key issues remain: (1) a single class-name prompt lacks sufficient semantic coverage for complex remote sensing categories; (2) expanding each category into multiple prompts introduces redundant online text encoding; and (3) directly aggregating multiple prompt responses propagates noisy activations into the final prediction. To address these issues, we propose ProC-SAM3, which calibrates SAM 3's prompt interface for remote sensing OVSS from three complementary aspects. First, we construct an offline prompt pool where a Category Matcher groups MLLM-generated candidates into per-category sets, and Expansion Constraints further refine each set using category-specific prior knowledge. Second, the resulting text embeddings are cached and reused across all test images, eliminating repeated text encoding. Third, we introduce Presence-Guided Residual Fusion to gate unreliable decoder outputs by prompt presence and confidence, followed by peak-preserving class aggregation that retains fine-grained activations for small and sparse objects. Experiments on eight benchmarks show that ProC-SAM3 achieves an average mIoU of 56.1%, outperforming the previous best training-free method by 3.9 percentage points. Code will be available at https://github.com/YanghuiSong/ProC-SAM3.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究任务**：开放词汇语义分割（OVSS）在遥感图像中的应用，目标是分割超出固定类别的场景。
- **现有方法瓶颈**：基于 SAM 3 的方法提供了强大的无训练分割基础，但存在三个关键问题：
  1. **语义覆盖不足**：单一类名提示无法充分描述遥感中视觉多样的类别（如“道路”包含不同材质、场景）。
  2. **编码冗余**：将每个类别扩展为多个提示后，在线推理中需重复编码每个提示的文本嵌入，显著增加计算开销。
  3. **噪声传播**：直接聚合多个提示的解码输出会将不相关或错误的激活（如误检背景区域）带入最终预测。
- **本文目标**：在不使用任何训练和真实标注（GT-free）的前提下，通过校准 SAM 3 的提示接口，同时提升语义覆盖率、推理效率和预测质量。

## 2. 方法论：核心思想与关键技术细节

- **总体框架**：ProC-SAM3 从三个互补阶段改进 SAM 3 的提示接口：
  - **（a）离线提示池构建（OPC）**：利用多模态大语言模型（MLLM，具体为 Qwen3-VL）生成丰富的候选描述，通过类别匹配器（Category Matcher）将候选分配到正确类别集合，再经扩展约束（Expansion Constraints，如属性、描述等先验知识）滤除语义漂移项，最终形成紧凑、语义清晰的全局提示池。该池在测试前一次性构建，无在线成本。
  - **（b）提示优化批处理推理（POBI）**：离线缓存提示池的文本嵌入（Text Encoder 一次编码），在推理时与图像特征（Vision Encoder 一次提取）分块送入 Mask Decoder，避免重复编码；支持混合精度（图像编码器 bf16，解码器 fp32）加速。
  - **（c）存在引导残差融合（PGRF）**：对每个提示 q 的输出，计算一致性 \(A_q = 1 - |S^{sem}_q - S^{base}_q|\)、正残差 \(\Delta_q = [S^{base}_q - S^{sem}_q]_+\)、门控因子 \(G_q = \rho_q \cdot k_q \cdot A_q\)（含存在得分 \(\rho_q\)、实例置信度 \(k_q\)、一致性 \(A_q\)）。融合后响应 \(\tilde{S}_q = \rho_q \cdot (S^{sem}_q + G_q \odot \Delta_q)\)，再按类别进行逐像素最大池化（peak-preserving aggregation）得到最终预测。

- **关键公式**（文字说明）：
  - 类别匹配与约束投影：\( \tilde{P}_i = CM(Qwen3(c_i, EC_i)) \) → \( P_i = Projection(\tilde{P}_i, Attribute_i, Description_i) \)
  - 全局提示池：\( P^* = \bigcup_{i=1}^N P_i \)，每个提示 q 有类别映射 \(\pi(q)\)
  - 文本缓存：\( T_{\{Q\}} = TextEncoder(P^*) \)
  - 批处理解码：\( Z_q = MaskDecoder(F, T_q) \) 产生 \(S^{sem}_q, S^{ins}_q, \rho_q, k_q\)
  - PGRF 融合规则：公式 (5)-(9)
  - 最终分类：\(\hat{Y}(u) = \arg\max_i \tilde{S}_i(u)\)

## 3. 实验设计

- **数据集**：8 个遥感基准数据集：
  - Vaihingen, Potsdam, LoveDA, OpenEarthMap, iSAID, UAVid, UDD5, VDD。
- **Benchmark 设置**：按 SegEarth-OV3 的类别定义评估，所有方法均使用相同标签映射。
- **对比方法**：
  - 基于 CLIP/DINO 的无训练方法：VIP, SCLIP, ClearCLIP, ProxyCLIP, SegEarth-OV, RSCLIP, CorrCLIP, ConInfer, ReAttnCLIP。
  - 基于 SAM 3 的方法：ActiveSAM, SegEarth-OV3。
- **评估指标**：平均交并比（mIoU %）。
- **主要结果**：ProC-SAM3 在所有 8 个数据集上均取得最高 mIoU，平均 56.1%，比此前最佳无训练方法 SegEarth-OV3（52.2%）提升 3.9 个百分点。提升最大的是 OpenEarthMap（+7.9%）、UAVid（+5.3%）、LoveDA（+4.5%）。

## 4. 资源与算力

- **硬件**：单张 RTX 4090 GPU（24GB 显存）。
- **离线构建**：使用 Qwen3-VL-8B 生成候选，但在 SAM 3 推理前将其从 GPU 卸载避免内存冲突。
- **推理配置**：图像编码器使用 bf16 半精度，解码器使用 fp32 全精度；批大小在 UDD5/VDD/UAVid 上为 1，其余为 4；未提及训练时长（因为方法完全无训练）。
- **效率提升**：POBI 使 FPS 在各数据集上提升约 1.5~2 倍（例如 Potsdam 从 1.35 → 2.42 FPS）。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验：8 个数据集 × 12 种方法（+ 本文）全面对比。
  - 消融实验 1：组件消融（OPC, PGRF）在 3 个数据集（UDD5, OEM, LoveDA）上进行，验证了 OPC 贡献主要增益（+3.7%）、PGRF 进一步改善。
  - 消融实验 2：融合策略对比（max, mean, PGRF）在相同 3 个数据集上，显示 PGRF 最优。
  - 消融实验 3：提示构建策略对比（类名、开放式 MLLM、纯文本扩展、图像条件扩展、约束引导池）在 UDD5/OEM/LoveDA 上，展示了逐级提升。
  - 消融实验 4：推理速度对比（有/无 POBI）在 4 个数据集上。
- **充分性与公平性**：
  - 对比方法覆盖了近年主流 CLIP 和 SAM 3 变体，且所有结果均在统一评估协议下获得（部分标 * 为复现）。
  - 消融实验全面覆盖了提出的三个模块，并分析了不同提示构造策略的影响。
  - 但缺乏与基于真实标签的概念银行方法（如 ConceptBank）的定量比较（仅在引言中讨论），且未进行多轮随机实验（通常遥感分割论文单次运行，但可接受）。
- **结论客观性**：实验设计严谨，结果一致支持方法优势。

## 6. 论文的主要结论与发现

- **结论**：ProC-SAM3 通过离线提示池构建（OPC）、缓存批处理推理（POBI）和存在引导残差融合（PGRF），在完全无训练、无 GT 的条件下，显著提升了 SAM 3 在遥感 OVSS 任务上的性能。在 8 个基准上平均 mIoU 达 56.1%，超越此前最佳无训练方法 3.9 个百分点。
- **关键发现**：
  - OPC 是最大增益来源（+3.7%），说明语义校准比模型容量更重要。
  - 约束引导的 MLLM 提示生成优于开放式或纯文本扩展，图像条件进一步改善。
  - PGRF 的残差门控和峰值保留聚合能有效抑制噪声并保留小目标激活。
  - POBI 可显著加速推理，且不损失精度。

## 7. 优点

- **完全无需训练和标注**：在测试时零训练开销，且不依赖任何地面真值掩码，便于实际部署。
- **三位一体优化**：从提示内容、编码效率、聚合策略三个完整维度系统改进，覆盖整个分割流程。
- **高效实用**：离线和缓存设计使得推理速度大幅提升，单卡即可运行。
- **泛化性强**：在 8 个涵盖不同场景（城市、乡村、航拍）的数据集上均获得一致提升。
- **消融实验充分**：逐步拆解各模块贡献，并分析不同提示构造、融合策略的影响，证明设计选择的合理性。
- **代码开源**：提供 GitHub 仓库，促进可复现性。

## 8. 不足与局限

- **依赖 MLLM 质量**：OPC 阶段使用 Qwen3-VL 生成候选提示，其输出可能受模型能力及提示工程影响，对某些稀有或专业类别可能产生不稳定或错误的候选。
- **类别先验定义需人工**：扩展约束（如属性、描述）需要领域专家预设，自动化程度不足，且在不同数据集间可能需要调整。
- **未与其他 GT-based 方法公平对比**：论文提及 ConceptBank 使用 GT 构建概念库可达更好校准，但未进行直接实验比较（可能因设定不同），削弱了绝对优势的论证。
- **仅评估遥感场景**：方法在通用自然图像上的表现未知，领域迁移性有待验证。
- **单次运行**：未报告多次运行的标准差，虽然这一做法在 CV 中常见，但可能掩盖偶然性波动。
- **推理时 batch size 较小**（部分数据集为 1），可能未充分利用 GPU 并行能力，POBI 在大批次下的效率潜力未探索。

（完）
