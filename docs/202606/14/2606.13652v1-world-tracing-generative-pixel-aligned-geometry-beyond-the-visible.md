---
title: "World Tracing: Generative Pixel-Aligned Geometry Beyond the Visible"
title_zh: 世界追踪：超越可见的生成式像素对齐几何
authors: "Hao Zhang, Mohamed El Banani, Jen-Hao Cheng, Paul Zhang, Yi Hua, Ben Mildenhall, Christoph Lassner, Narendra Ahuja, Gengshan Yang"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13652v1"
tags: ["query:mono-depth"]
score: 9.0
evidence: 像素对齐几何的单目深度估计
tldr: 图像到3D方法常在忠实性和完整性间取舍：深度估计锚定像素但止于可见表面，生成模型完成形状却常错位。本文提出World Tracing，一种生成式像素对齐几何表示，为每个像素预测有序堆叠的3D点，覆盖可见与遮挡表面。通过WT-DiT扩散transformer和混合噪声调度训练，在物体、场景、动态基准上同时实现高精度可见表面重建和完整几何生成，并保持2D-3D对应，支持文本驱动编辑等应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1571, \"height\": 1053, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13652v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1566, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13652v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1560, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13652v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1583, \"height\": 1129, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13652v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1568, \"height\": 1964, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13652v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1100, \"height\": 1001, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13652v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 784, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 784, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1578, \"height\": 681, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 753, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 667, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1278, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13652v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1582, \"height\": 494, \"label\": \"Table\"}]"
motivation: 现有方法难以兼顾可见表面的精确对齐与遮挡区域的几何生成。
method: 提出World Tracing，为每个像素预测多层有序3D点，用WT-DiT扩散transformer处理多层token并耦合注意力。
result: 在多个物体、场景和动态基准上优于深度预测器和图像到3D生成器，并保持2D-3D对应。
conclusion: 统一了可见表面重建与完整几何生成，拓展了图像到3D的实用性和编辑能力。
---

## 摘要
图像到三维的方法常常在忠实度和完整性之间权衡：深度估计器锚定于输入像素，但止于可见表面；而图像到三维模型生成完整形状，但往往与输入不对齐。我们提出世界追踪（World Tracing），一种生成式像素对齐几何表示，它预测与观测像素对齐的三维点，同时补全可见表面之外的几何结构。对于每个输入像素，世界追踪预测一个按序排列的相机空间三维点栈，其中第一层表示可见表面，后续层表示从前到后与遮挡表面的交点。我们通过一个世界追踪扩散变换器（WT-DiT）实例化这种表示，该变换器将多个几何层视为独立的去噪令牌，通过分解注意力和全局注意力耦合。WT-DiT使用像素空间流匹配和混合噪声调度进行训练，平衡可见表面重建与遮挡几何生成。世界追踪在可见表面重建和完整几何生成方面，跨物体、场景和动态基准测试均表现出色，优于深度估计器和图像到三维生成器。它还保留了二维到三维的对应关系，支持文本驱动的三维场景编辑、几何条件的新视角视频合成，以及与纹理网格生成器的无需训练集成。

## Abstract
Image-to-3D methods often trade off faithfulness and completeness: depth estimators are anchored to input pixels but stop at the visible surface, while image-to-3D models generate complete shapes that are often misaligned with the input. We introduce World Tracing, a generative pixel-aligned geometry representation that predicts 3D points aligned with observed pixels while completing geometry beyond the visible surface. For each input pixel, World Tracing predicts an ordered stack of camera-space 3D points, where the first layer represents the visible surface and subsequent layers represent front-to-back intersections with occluded surfaces. We instantiate this representation with a world-tracing diffusion transformer, WT-DiT, which treats multiple geometry layers as separate denoising tokens coupled through factorized and global attention. WT-DiT is trained with pixel-space flow matching and a mixed noise schedule that balances visible-surface reconstruction with occluded-geometry generation. World Tracing achieves strong performance on visible-surface reconstruction and complete geometry generation across object, scene, and dynamic benchmarks, outperforming both depth predictors and image-to-3D generators. It also preserves 2D-to-3D correspondence, enabling text-driven 3D scene editing, geometry-conditioned novel-view video synthesis, and training-free integration with textured-mesh generators.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入且客观的学术论文总结。

### 论文结构化总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：图像到三维重建领域的现有方法面临一个根本性的矛盾：
    - **丢顾失真**：专注于重建可见表面的方法（如单目深度估计）能精确对齐输入像素，但**止步于可见表面**，无法预测被遮挡区域的几何结构。
    - **生成完整**：生成式的图像到三维模型（如TRELLIS）能输出完整的物体形状，但通常是在**规范坐标系（canonical frame）中生成**，失去了与输入图像的像素级对齐关系，导致几何结构错位。
- **研究动机**：下游任务（如场景编辑、三维感知）同时需要**忠实（faithful）** 和**完整（complete）** 的几何信息，现有两种范式均无法满足。作者旨在填补这一空白，提出一种既能忠实重建可见表面，又能合理生成不可见区域的“忠实生成（faithful generation）”方法。
- **整体含义**：论文提出了一种全新的**像素对齐多层几何表示**和生成模型，将“重建可见面”和“生成遮挡面”统一为一个端到端的任务。这一思路有望成为三维感知、生成、编辑和视频应用的通用基础。

#### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：对于输入图像中的**每个像素**，预测一个**有序的、坐标为相机空间的三维点堆栈（stack）**，其中：
    - 第一层 (Layer 0)：代表**可见表面**。
    - 后续层 (Layer 1~L-1)：代表沿着该射线方向从前到后的**遮挡表面**。
- **关键技术细节**：
    - **World Tracing (WT) 表示**：
        - 输出为一个张量 \( X \in \mathbb{R}^{L \times H \times W \times 3} \)，L是层数，H、W是图像尺寸。
        - **深度填充 (Depth Filling)** 策略：对于射线交点少于L层的像素，将最近的有效层填充到后面的层中。这避免了训练稀疏掩码（mask）带来的类别不平衡问题，将任务转化为纯稠密的XYZ回归。
        - **尺度归一化**：对物体和场景使用不同的归一化策略（z-score 和 对数中位数变换），以适应不同尺度的输入。
        - **损失函数**：端点损失（Endpoint Loss）加上一个用于维持层间前后顺序的软惩罚项（Monotonicity Penalty）。
    - **WT-DiT 架构**：
        - **编码器**：使用冻结的 MoGe ViT-L 编码器提取像素级图像特征。
        - **去噪**：采用**流匹配（Flow Matching）** 直接在XYZ信号上进行扩散生成。
        - **三维注意力分解机制**：在 DiT 块中，设计了三种注意力模式来耦合多层令牌：
            1.  **逐层注意力 (Layer-wise Attention)**：同一层内的像素互相通信，视为二维图像，使用2D RoPE。
            2.  **逐射线注意力 (Ray-wise Attention)**：同一条射线上不同层的令牌互相通信，强制深度排序和层间一致性。
            3.  **全局注意力 (Global Attention)**：所有令牌全局交互，恢复场景级上下文。
        - **时间注意力**：对于动态模型（WT-D），额外插入时间注意力模块处理帧间一致性。
        - **层感知条件**：通过特征线性调制层（FiLM）将层索引信息注入模型。
    - **训练与调度**：
        - **混合噪声调度 (Mixed Noise Schedule)**：采用层感知的训练课程，对可见层（Layer-0）使用偏向小噪声的调度，对遮挡层使用标准调度，最后混合，平衡重建与生成。
        - **混合训练 (Mix-training)**：该框架可同时使用完整多层三维资产数据和仅含可见表面的单层RGBD数据，通过一个门控逻辑（bsingle）控制损失，极大扩展了数据来源。

#### 3. 实验设计：数据集、基准和对比方法
- **数据集与场景**：
    - **数据构建**：通过深度剥离（Depth Peeling）从三维资产渲染出多层监督数据。
    - **训练集**：
        - **物体**：约30万个物体，1700万渲染视图，来源包括 Objaverse-XL、Objaverse、3D-FUTURE 等。
        - **场景**：3D-FRONT 室内房间及内部数据集。
        - **动态**：约1.68万个动画资产片段。
        - **RGBD混合源**：12个数据集的单层RGBD数据（如 ScanNet, MegaDepth, Hypersim）。
- **评估基准**：
    - **物体**：100个保留物体资产的基准。
    - **场景**：保留的 3D-FRONT 分割（50个场景）和 200个内部场景测试集。
    - **动态**：Obj.-Val, Truebone, ActionBench 三个基准。
    - **真实场景深度**：NYU Depth V2 和 ETH3D。
- **对比方法**：
    - **深度预测器**: Depth Anything 3, LaRI, Pi3X, MoGe-2, VGGT。
    - **图像到三维生成器**: TRELLIS.2, SAM 3D, LaS-Comp, ReconViaGen。
    - **动态几何方法**: GVFDiffusion, SS4D, ActionMesh。
    - **混合方法**: 本文方法作为前置条件输入到TRELLIS的解码器中。

#### 4. 资源与算力
- 文中明确说明：
    - **模型规模**：WT-DiT 模型约 **17亿参数**（其中14亿可训练）。
    - **训练硬件**：64块 **H100 GPU**。
    - **批大小**：全局批大小为 **512**。
    - **优化器**：AdamW。
    - **训练时长**：物体模型分为低分辨率（196x196）和高分辨率（504x504）两阶段，各训练100K步；动态模型从物体模型微调50K步。总共约 **250K步**。

#### 5. 实验数量与充分性
- **实验数量**：
    - 论文进行了**非常充分**的实验验证，覆盖了可见表面重建、完整几何生成、动态序列构建三大类任务。
    - 包含多个基准上的定量评估，以及广泛的定性比较。
    - 开展了关键的**消融实验**，包括：
        1.  深度填充 vs. 掩码预测。
        2.  **时间步采样策略** (Table 4) 的比较。
        3.  **混合训练**的影响 (Table 6) 的对比。
        4.  对架构细节（如LayerScale）的小规模消融。
- **充分性与客观性**：
    - **设计合理**：实验设计覆盖了研究的核心矛盾（忠实性 vs. 完整性）。
    - **对比公平**：所有方法在相同指标下进行比较，且对生成式方法进行了多次随机采样（K=8次，报告最佳结果），保证了对比的严谨性。
    - **结论扎实**：定量结果和定性案例均支持论文的论点。
    - 消融实验的设计也直接验证了作者提出的关键方法（深度填充、噪声调度）的有效性，体现了实验的客观性。

#### 6. 论文的主要结论与发现
- **核心结论**：World Tracing 方法成功地将“忠实可见表面重建”和“完整遮挡几何生成”统一在一个框架下。生成的几何不仅准确度高，而且保留了与输入图像的像素级对应关系。
- **主要发现**：
    - **性能领先**：在所有基准测试中，WT的可见表面精度（Layer-0）超越所有单层深度预测器；完整几何精度（Chamfer距离）超越所有图像到三维生成器。
    - **生成不损害重建**：即使需要生成遮挡的几何，可见表面的重建精度反而更高，证明两者是相辅相成的。
    - **高效几何先验**：WT输出的像素对齐点云可作为强大的几何先验，无需额外训练即可集成到下游应用中（如TRELLIS混合、场景编辑、视点合成），并能显著提升这些应用的性能。

#### 7. 优点
- **方法创新**：
    1.  **表示优雅**：多层像素对齐点云堆栈是一个简洁、直接且信息丰富的表示，同时实现了像素对齐和完整几何输出。
    2.  **架构巧妙**：WT-DiT 的三维注意力分解机制（层-射线-全局）设计精妙，有效解决了不同维度（同一层内、射线方向、全局上下文）上的信息交互问题。
    3.  **训练策略高效**：深度填充策略巧妙地化解了掩码预测的训练难题；混合噪声调度针对可见层和遮挡层的不同不确定性进行了优化；混合训练策略能够整合不同来源的数据，极大扩展模型的可训练数据。
- **实验亮点**：
    - 严格的消融实验验证了关键设计（深度填充 vs. 掩码、噪声调度、混合训练）的有效性。
    - 多个下游任务的定性/定量展示（场景编辑、视点合成、TRELLIS混合）有力地证明了所学几何表示作为通用先验的强大潜力。
    - 对于真实场景深度估计的混合训练消融实验，明确了不同数据源对性能的贡献，结论清晰。

#### 8. 不足与局限
- **固定层数限制**：模型使用固定6层的设计，对于非常复杂的、具有高度穿孔的结构（如树叶、铁丝网）可能不够用，限制了超级复杂场景的表示能力。
- **渲染数据依赖**：虽然提出了混合训练，但高质量的多层监督数据仍然依赖于对三维资产的渲染，存在合成到真实（Synthetic-to-Real）的域迁移问题，在无纹理或高反射区域可能有伪影。
- **计算成本高**：流匹配模型在推理时需要20步ODE积分迭代，对于实时应用来说计算开销较大。
- **动态模型局限**：当前动态模型（WT-D）仅处理短视频片段，对于需要长时记忆和多帧一致性的应用（如长时间动态场景）仍有局限。
- **场景结构偏向**：场景模型主要基于室内场景（3D-FRONT）训练，对多样化室外场景的泛化能力有待进一步验证。尽管混合训练有所帮助，但主要训练数据仍是虚拟场景。
- **输入掩码依赖**：模型需要输入RGBA图像（含alpha通道），这意味着需要一个前置的物体分割或场景天空掩码模块。在实际应用中，掩码的质量会直接影响最终结果。

（完）
