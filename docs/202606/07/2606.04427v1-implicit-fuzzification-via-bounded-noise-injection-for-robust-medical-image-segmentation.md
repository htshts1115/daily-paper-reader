---
title: Implicit Fuzzification via Bounded Noise Injection for Robust Medical Image Segmentation
title_zh: 通过有界噪声注入实现隐式模糊化以进行鲁棒医学图像分割
authors: "Bisheng Tang, Zhangfeng Ma, Chuchu Zhai, Feng Dong, Yaoqun Wu, Ammar Oad, Yifei Peng"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04427v1"
tags: ["query:seg"]
score: 8.0
evidence: 提出有界噪声注入用于鲁棒的医学图像分割，直接解决边界模糊问题
tldr: 医学图像分割常因采样损失和标注不确定性导致边界模糊，现有模型如U-Net易产生过度自信的预测。本文提出NoiseUNet，在跳跃连接中注入有界噪声，隐式实现模糊化，生成软边界隶属度。在真实甲状腺超声数据集ThyR上，该方法显著提升了分割精度和边界保真度。这一简单框架无需显式模糊建模即可增强鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04427v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04427v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 920, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04427v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1863, \"height\": 799, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04427v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1798, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04427v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1245, \"height\": 468, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04427v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 644, \"height\": 438, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04427v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04427v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 746, \"height\": 289, \"label\": \"Table\"}]"
motivation: 医学图像分割面临边界模糊和不确定性，现有方法预测过于自信，缺乏对过渡区域的准确建模。
method: 提出NoiseUNet，在U-Net跳跃连接中注入有界噪声，以隐式模糊化方式正则化特征融合，无需额外模糊建模。
result: 在甲状腺超声数据集ThyR上，NoiseUNet一致提升了分割精度和边界保真度，优于基线。
conclusion: 有界噪声注入是一种简单有效的正则化手段，能提升医学图像分割的边界感知和鲁棒性。
---

## 摘要
图像分割仍然受到采样引起的信息损失和像素级标注固有不确定性导致的边界模糊性的根本限制。尽管U-Net等编码器-解码器架构取得了强劲性能，但它们常常产生过于自信的预测，无法捕捉过渡区域的模糊性。为解决这一问题，我们提出**NoiseUNet**，一个简单而有效的框架，它将有界扰动注入跳跃连接中，以正则化跨尺度特征融合。该机制增强了对局部特征变化的鲁棒性，并促进了边界感知表示。理论上，该扰动产生隐式模糊化效果，提供软性的、数据驱动的隶属度，无需显式模糊建模。我们进一步引入**ThyR**，一个具有固有模糊边界的真实世界甲状腺超声数据集。实验表明，NoiseUNet在分割精度和边界保真度上均持续提升。

## Abstract
Image segmentation remains fundamentally limited by boundary ambiguity arising from sampling-induced information loss and inherent uncertainty in pixel-wise labeling. Although encoder-decoder architectures such as U-Net achieve strong performance, they often produce overconfident predictions that fail to capture transition-region ambiguity. To address this issue, we propose \textbf{NoiseUNet}, a simple yet effective framework that injects bounded perturbations into skip connections to regularize cross-scale feature fusion. This mechanism enforces robustness to local feature variations and promotes boundary-aware representations. Theoretically, the perturbation induces an implicit fuzzification effect, yielding soft, data-driven memberships without requiring explicit fuzzy modeling. We further introduce \textbf{ThyR}, a real-world thyroid ultrasound dataset with inherently ambiguous boundaries. Experiments demonstrate that NoiseUNet consistently improves both segmentation accuracy and boundary fidelity.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：医学图像分割中，编码器-解码器架构（如U-Net）虽然性能强大，但由于下采样导致高频边界信息丢失，以及跳跃连接在跨尺度特征融合时存在不确定性，使得模型在边界区域产生过度自信的预测，无法准确刻画过渡区域的模糊性。
- **背景**：传统的边界增强方法（如辅助边缘检测分支、边界损失函数）增加了模型复杂度，且通常将边界退化视为确定性问题，忽略了采样和成像质量导致的固有不确定性。模糊系统虽能建模渐变，但与深度学习结合不足。
- **整体意义**：本文旨在通过一种简单、无额外参数的机制——在跳跃连接中注入有界噪声，隐式实现模糊化，提升模型对边界不确定性的鲁棒性，同时保持U-Net架构的简洁性。

## 2. 论文提出的方法论

### 核心思想
- 在U-Net的每个跳跃连接中，对编码器特征图施加有界加性扰动（满足无穷范数上界δ），再将扰动后的特征图与解码器特征拼接融合。这种做法迫使解码器在面对局部特征变化时保持稳定，从而学习边界感知表示。
- 从模糊系统角度看，该机制将每个确定性特征扩展为一个有界邻域，在边界区域自然产生软化的数据驱动隶属度，即隐式模糊化。

### 关键技术细节
- **噪声注入形式**：\( \tilde{F}_l = F_l + \epsilon_l \)，其中 \( \|\epsilon_l\|_\infty \le \delta \)，默认δ=0.5，噪声分布为均匀分布。
- **训练目标**：最小化期望分割损失 \( \min_\theta \mathbb{E}_\epsilon [\ell(\text{U-Net}_\theta(X;\epsilon), Y)] \)，通过蒙特卡洛采样近似。
- **理论支撑**：
    - 基于局部Lipschitz连续性，解码器输出变化有界（\( \le L\delta \)）。
    - 梯度平滑：期望梯度可降低方差，抑制不稳定更新。
    - 隐式模糊隶属度：均匀噪声使决策函数与均匀核卷积，产生介于0~1的软输出。

### 算法流程（文字说明）
1. 输入图像X，编码器逐层提取特征 \( F_l \)。
2. 在每个跳跃连接处，对 \( F_l \) 添加独立同分布的有界均匀噪声 \( \epsilon_l \)。
3. 将扰动后的 \( \tilde{F}_l \) 与解码器上采样特征拼接。
4. 解码器处理拼接特征，输出分割预测。
5. 使用交叉熵+Dice损失，通过多次前向传播（扰动不同采样）训练网络。

## 3. 实验设计

### 数据集
- **BUSI**（乳腺超声）：647张图像，256×256。
- **GlaS**（组织腺体分割）：165张图像，512×512。
- **ThyR**（自建甲状腺超声）：205张图像，256×256，含正常/良性/恶性，边界模糊真实。

### Benchmark与对比方法
- 对比方法：U-Net、U-Net++、U-NeXt、Rolling-UNet、U-Mamba、Seg. U-KAN。
- 评价指标：IoU（交并比）、F1分数、GFLOPs、参数数量。

### 实验场景
- **主要性能比较**：在三个数据集上对比SOTA，报告三次运行均值和标准差。
- **消融实验**：
    - 编码器噪声 vs 跳跃噪声（表III）。
    - 不同网络类型（CNN/MLP/KAN）加入噪声（表IV）。
    - 不同噪声类型（均匀、截断高斯、椒盐、泊松衍生）（表V）。

## 4. 资源与算力

- **GPU**：单张NVIDIA RTX A6000。
- **训练细节**：batch size=8，初始学习率1e-4，Adam优化器，余弦退火调度至1e-5，训练400个epoch。未明确报告总训练时间或GPU数量。
- **说明**：文中未提及多卡并行或具体训练时长，仅给出硬件型号和超参数。

## 5. 实验数量与充分性

- **实验组数**：共进行15组以上定量比较（3个数据集×7种方法=21组主要结果，加上3组消融实验各含多个变体），以及定性可视化对比（图3）。
- **充分性**：
    - 覆盖三种异质医学图像模态（超声、组织病理），且有自建真实模糊数据集ThyR。
    - 与6种主流SOTA方法全面对比。
    - 消融实验系统探讨了噪声注入位置、基干类型、噪声分布三个维度，论证充分。
    - 采用三次运行取均值标准差，保证统计稳定性。
- **公平性**：所有方法在同一框架下使用相同数据划分、超参数和训练策略，对比客观。

## 6. 论文的主要结论与发现

1. **NoiseUNet在所有数据集上均优于基线U-Net和现有SOTA**，在ThyR上IoU提升1.92%（相对+2.6%），且计算开销与U-Net相同（8.98 GFLOPs, 4.75M参数）。
2. **跳跃连接噪声比编码器噪声更有效**（表III），因为跳跃连接保留了高分辨率边界信息，噪声在此处直接作用于模糊区域。
3. **CNN基干优于MLP和KAN基干**（表IV），卷积的局部感受野与空间边界不确定性匹配。
4. **均匀噪声表现最佳**（表V）：截断高斯、椒盐、泊松衍生均不如均匀噪声，说明均匀分布能更平衡地建模边界邻域的不确定性。
5. **理论分析**支持方法有效：Lipschitz约束、梯度平滑和隐式模糊化共同解释了边界鲁棒性的提升。

## 7. 优点

- **简洁高效**：无需修改网络拓扑、无需额外参数或标注，在标准U-Net基础上仅添加扰动操作。
- **理论新颖**：从局部鲁棒正则化和模糊系统角度解释了噪声注入的作用，提供了扎实的理论基础。
- **实验扎实**：涵盖多模态数据、多种SOTA对比、消融实验全面（噪声位置、网络类型、噪声类型），且结果公开可复现。
- **自建数据集ThyR**：填补了甲状腺超声模糊边界分割的基准空白。
- **鲁棒性好**：在低对比度、噪声、模糊条件下仍能保持边界清晰。

## 8. 不足与局限

- **未显式讨论局限性**：论文未提及方法在强噪声或严重遮挡场景下的表现边界，也未分析扰动幅度δ的敏感性（仅用固定0.5）。
- **泛化性受限**：仅在医学图像上验证，未在自然图像或遥感等其他分割任务上测试。
- **理论假设简化**：Lipschitz常数L在实际网络中难以计算，梯度平滑的论证偏定性。
- **计算开销**：训练时需多次前向采样（期望损失），较标准U-Net增加训练时间，但推理时无需噪声（可固定为0）。
- **数据集规模较小**：ThyR仅205张图像，可能不足以支撑高度泛化；BUSI和GlaS也是中等规模。
- **噪声分布的物理意义**：均匀噪声虽由Lipschitz分析支持，但其与真实成像噪声的匹配程度未深入探讨。

（完）
