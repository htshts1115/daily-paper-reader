---
title: "URS-Stereo: Uncertainty-Guided Residual Search for Real-Time Stereo Matching"
title_zh: URS-Stereo：不确定性引导的残差搜索用于实时立体匹配
authors: "Pouya Sohrabipour, Chaitanya kumar reddy Pallerla, Dongyi Wang"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06779v1"
tags: ["query:stereo-depth"]
score: 9.0
evidence: 基于不确定性引导残差搜索的实时粗到细立体匹配
tldr: 粗到细立体匹配依赖传播视差进行局部代价体搜索，但视差不准确时易导致匹配失败。本文提出URS-Stereo框架，通过不确定性引导残差搜索模块（UGRSM）预测传播视差的可靠性并给出偏移量，动态调整搜索中心，从而提升鲁棒性。在SceneFlow、KITTI等数据集上，该方法在保持实时速度的同时显著提高了视差精度。该工作为实时立体匹配提供了一种有效的不确定性引导策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06779v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06779v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 660, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06779v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1251, \"height\": 765, \"label\": \"Table\"}]"
motivation: 粗到细立体匹配中传播视差不准确会导致局部搜索范围失效，引发不可恢复的匹配失败。
method: 提出UGRSM模块，预测传播视差的可靠性与残差搜索偏移，自适应调整代价体搜索中心后进行细化。
result: 在SceneFlow、KITTI、Middlebury等数据集上提升视差精度，同时保持实时推理速度。
conclusion: 不确定性引导搜索策略有效增强了粗到细立体匹配的鲁棒性与精度，且不牺牲效率。
---

## 摘要
实时立体匹配对于机器人、自主系统和嵌入式视觉应用至关重要，这些应用同时需要计算效率和视差精度。近年来，粗到细的立体匹配方法通过在高分辨率下使用局部代价体逐步细化视差估计来提高效率。然而，这些方法严重依赖于前一阶段传播的视差估计的准确性。当传播的视差不准确时，真实对应点可能会超出预定义的局部搜索范围，导致后续细化过程中出现不可恢复的匹配失败。在本文中，我们提出URS-Stereo，一种实时粗到细立体匹配框架，通过不确定性引导的搜索自适应来解决这一限制。具体来说，我们引入了一个不确定性引导的残差搜索模块（UGRSM），该模块预测传播视差的可靠性以及残差搜索偏移量，从而在视差细化前自适应地重新定位局部代价体的中心。通过根据传播视差的置信度动态调整搜索区域，所提方法显著提高了局部对应估计的鲁棒性，同时保持了粗到细立体匹配的计算效率。在SceneFlow、KITTI 2012、KITTI 2015、Middlebury和ETH3D上的大量实验表明，URS-Stereo在保持实时推理速度的同时，始终改善了视差估计，验证了所提不确定性引导搜索策略的有效性。

## Abstract
Real-time stereo matching is crucial for robotics, autonomous systems, and embedded vision applications, where both computational efficiency and disparity accuracy are required. Recent coarse-to-fine stereo matching methods improve efficiency by progressively refining disparity estimates using local cost volumes at higher resolutions. However, these methods rely heavily on the accuracy of propagated disparity estimates from previous stages. When the propagated disparity is inaccurate, the ground-truth correspondence may fall outside the predefined local search range, leading to unrecoverable matching failures during subsequent refinement. In this paper, we propose URS-Stereo, a real-time coarse-to-fine stereo matching framework that addresses this limitation through uncertainty-guided search adaptation. Specifically, we introduce an Uncertainty-Guided Residual Search Module (UGRSM), which predicts the reliability of propagated disparities together with residual search offsets to adaptively relocate the centers of local cost volumes before disparity refinement. By dynamically adjusting the search region according to the confidence of the propagated disparity, the proposed method significantly improves the robustness of local correspondence estimation while preserving the computational efficiency of coarse-to-fine stereo matching. Extensive experiments on SceneFlow, KITTI 2012, KITTI 2015, Middlebury, and ETH3D demonstrate that URS-Stereo consistently improves disparity estimation while maintaining real-time inference speed, validating the effectiveness of the proposed uncertainty-guided search strategy

---

## 论文详细总结（自动生成）

# URS-Stereo：不确定性引导的残差搜索用于实时立体匹配 —— 详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：粗到细（coarse-to-fine）立体匹配方法通过在高分辨率下构建以传播视差为中心的局部代价体来降低计算开销，但其性能严重依赖前一阶段传播视差的准确性。当传播视差存在较大误差（尤其在物体边界、薄结构、遮挡区域）时，真实视差可能落在局部搜索范围之外，导致后续细化阶段无法恢复正确视差，产生不可恢复的匹配失败。
- **研究动机**：现有粗到细方法缺乏对传播视差可靠性的感知，无法自适应调整搜索中心。本文旨在通过引入不确定性信息，动态修正局部搜索位置，提升鲁棒性而不牺牲效率。
- **整体含义**：提出 URS-Stereo 框架，通过不确定性引导残差搜索模块（UGRSM）预测传播视差的不确定性并给出偏移量，重新定位代价体搜索中心，从根本上缓解上述局限，实现实时且稳健的立体匹配。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在粗到细的每一细化阶段（1/8 和 1/4 分辨率），不直接围绕原始传播视差构建局部代价体，而是先由 UGRSM 估计每个像素的可靠性（不确定性），并预测一个残差偏移量；用不确定性调制该偏移量后，得到调整后的搜索中心，再生成局部候选视差集，最后用 ConvGRU 进行迭代细化。
- **整体架构**（图 1）：
  - 多尺度特征提取（共享 CNN，下采样因子 16、8、4）。
  - 在 1/16 尺度构建全分组相关（GWC）代价体，经 3D 卷积和 ConvGRU 迭代得到初始视差估计。
  - 在 1/8 和 1/4 尺度，先由 UGRSM 给出调整后的搜索中心，构建局部代价体，再经几何编码和 ConvGRU 细化。
  - 在 1/4 尺度额外引入单目视差分支，进行立体-单目互细化。
- **UGRSM 关键步骤**（图 2）：
  1. **不确定性预测**：以左图特征和上采样传播视差为输入，用轻量网络预测不确定性图 \(U^{(s)} \in [0,1]\)。伪标签通过指数函数由预测误差生成：\(U^{(s)}_{gt} = 1 - \exp(-\beta |D_{pred} - D_{gt}|)\)。
  2. **初始残差偏移预测**：另一个像素级网络预测初始偏移 \(\Delta O^{(s)}_{init}\)。
  3. **不确定性调制**：最终偏移 \(\Delta O^{(s)}_{final} = U^{(s)} \cdot \Delta O^{(s)}_{init}\)（高不确定性区域偏移增大，低不确定性区域偏移被抑制）。
  4. **调整搜索中心**：\(D_{center} = D_{pred} + \Delta O^{(s)}_{final}\)。
  5. **生成局部候选**：\(D^{(s)} = \{ D_{center} + k \delta^{(s)} \mid k \in [-r, r] \}\)。
- **训练损失**：
  - 视差损失：对所有迭代的 L1 损失加权求和（指数衰减权重）。
  - 不确定性损失：预测不确定性图与伪标签的 L1 损失。
  - 总损失：\(L = L_{stereo} + \lambda_{unc} L_{unc}\)。

## 3. 实验设计：数据集、Benchmark、对比方法

- **训练数据集**：SceneFlow（合成数据）唯一训练集，无任何真实数据集微调。
- **测试数据集与 Benchmark**：
  - KITTI 2012（真实道路场景）
  - KITTI 2015（真实道路场景）
  - Middlebury（高分辨率室内场景）
  - 摘要中还提及 ETH3D，但正文表 1 仅列出前三者（ETH3D 可能在其他补充或消融中，但文中未给出详细结果）。
  - 评价指标：D1 error（视差误差百分比）、EPE（端点误差）、Bad 2.0（误差大于2像素的比例）。
- **对比方法**：选取近期高效立体匹配方法，均在同一设置（SceneFlow 训练、零样本测试）下比较：
  - CoEX、MobileStereoNet-2D/3D、FastACV、FastACV+、Lite-CREStereo++、LightStereo-M/L、BANet-2D/3D、Lite Any Stereo。
- **实验环境**：文中未明确说明硬件资源（GPU 型号、数量、训练时长）。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练迭代次数或训练时长。仅在方法部分描述了网络结构轻量级，推理速度“实时”，但未给出具体参数（如推理帧率、参数量、FLOPs）。
- **结论**：本文在算力资源方面缺乏透明性，无法判断其训练和推理效率的具体硬件要求。

## 5. 实验数量与充分性

- **主要实验**：一组零样本泛化对比实验（表 1），涵盖三个常用真实世界数据集（KITTI 2012、KITTI 2015、Middlebury），与 12 种高效方法比较，结果在多数指标上达到最优或次优。
- **消融实验**：文中仅用一句话提到“offset-only variant”的效果不如 UGRSM，但**未给出定量消融表**（如移除不确定性调制、不同权重设置、不同偏移量范围的影响）。也未报告对 uncertainties 分支的分析（如预测不确定性图的可视化）。
- **充分性评估**：跨数据集零样本测试具有客观性和公平性（统一训练），但缺失消融实验限制了对各组件贡献的深入理解。实验数量偏少（仅一个主表），且未包含 ETH3D 的结果。整体而言，**实验覆盖不足，充分性一般**。

## 6. 主要结论与发现

- 在 KITTI 2012 上，URS-Stereo 的 D1 error 为 3.98%，EPE 为 0.89，显著优于所有对比方法（第二名为 Lite Any Stereo：5.45% / 1.18）。
- 在 KITTI 2015 上，D1 误差 5.85%，EPE 1.24，排名第一（第二：Lite Any Stereo 6.45% / 1.32）。
- 在 Middlebury 上，Bad 2.0 为 13.03%，EPE 1.71，仅次于 Lite Any Stereo（13.13% / 1.60）。
- 所有测试均保持实时推理速度（文中声明，但未给出具体帧率）。
- 验证了不确定性引导搜索策略能够有效减轻传播误差带来的匹配失败，提升粗到细立体匹配的鲁棒性和精度。

## 7. 优点

- **问题洞察尖锐**：准确指出粗到细立体匹配的根本局限——传播误差导致搜索中心偏移，并据此提出针对性解决方案。
- **方法简洁有效**：UGRSM 仅增加轻量网络，不引入复杂 3D 卷积或迭代，保持了粗到细的高效率，同时显著提升精度。
- **零样本泛化能力强**：仅在合成数据 SceneFlow 上训练，即在多个真实世界数据集上达到 state-of-the-art，说明方法具有良好的泛化能力。
- **训练损失合理**：利用指数函数由预测误差生成不确定性伪标签，避免了额外标注成本，支持端到端学习。

## 8. 不足与局限

- **缺乏详细的消融实验**：未定量分析不确定性调制、偏移量范围、不同不确定性伪标签形式等的影响；也未展示预测不确定性图的可视化或与真实误差的对比。
- **资源与效率信息不透明**：未报告训练/推理 GPU 型号、训练时长、推理帧率、参数量、FLOPs 等关键效率指标，无法与其他实时方法做公平的效率比较。
- **对比方法选择有限**：未与近年来同样追求实时的 HITNet、RAFT-Stereo（虽然其非实时但精度高）等方法对比；仅与高效方法对比，未涉及高精度但慢的迭代方法，削弱了论证的全面性。
- **实验覆盖不完全**：摘要提及 ETH3D 但正文未给出结果；仅做了零样本测试，未进行在 KITTI 上微调后的性能报告（许多方法会报告 fine-tune 结果，本文缺少）。
- **潜在偏差风险**：在 Middlebury 上的 Bad 2.0 略高于 Lite Any Stereo，说明在细节保持上仍有提升空间；对极端遮挡、反射区域、稀纹理区域的表现未单独分析。
- **应用限制**：方法假设传播视差已有一定准确度，在初始全代价体也有误差的场景下，UGRSM 的偏移量可能依然不足；且不确定性预测依赖误差伪标签，该伪标签在真实数据上是否可靠尚需验证。

（完）
