---
title: "From RGB Generation to Dense Field Readout: Pixel-Space Dense Prediction with Text-to-Image Models"
title_zh: 从RGB生成到稠密场读出：利用文本到图像模型进行像素空间稠密预测
authors: "Zanyi Wang, Xin Lin, Haodong Li, Dengyang Jiang, Yijiang Li, Pengtao Xie"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06553v1"
tags: ["query:vfm"]
score: 9.0
evidence: 利用预训练文生图模型进行像素级密集预测（深度、法线、蒙版、抠图）
tldr: 现有方法将密集预测视为目标生成，继承生成输出界面造成冗余。本文提出ReChannel，利用预训练DiT的patch-token-patch格点结构，通过共享线性头将token直接映射到像素空间任务原生场，抛弃目标解码器。在六项密集任务上，ReChannel在抠图、深度、指代分割上取得新SOTA，且比同类方法快2.48倍。揭示密集预测可从生成预训练中获益而无需继承其输出接口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06553v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06553v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06553v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1794, \"height\": 748, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06553v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1619, \"height\": 654, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1424, \"height\": 584, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1306, \"height\": 602, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 621, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1273, \"height\": 584, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06553v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 370, \"label\": \"Table\"}]"
motivation: 现有方法将密集预测建模为图像生成，引入了不必要的VAE解码和RGB生成开销。
method: 冻结预训练DiT，用任务LoRA适配，通过共享线性头将每个token映射到对应像素块的任务场。
result: 在无trimap抠图、KITTI深度、指代分割上达到新SOTA，其他任务竞争力强，速度快2.48倍。
conclusion: 密集预测能够利用生成预训练先验，但应避免继承其输出接口。
---

## 摘要
大规模文本到图像模型是稠密预测的有吸引力的骨干网络，因为RGB生成预训练学习了丰富的语义、结构和几何先验。现有的生成和编辑方法通过将稠密预测视为目标生成来重用这些先验：将深度、法线、alpha抠图、掩码和热图等注释编码到RGB训练的VAE潜空间中，然后解码回类似图像的目标。我们认为这继承了比稠密预测所需的更多的生成输出接口：与RGB合成不同，稠密预测要求在同一个图像平面上获得像素正确、任务原生的场，而不是要渲染的新RGB内容。我们的关键观察是，预训练的DiT已经通过图像平面上的块到令牌到块的格结构组织了RGB输入，因此每个令牌索引一个固定的输出块，其通道可以携带任务原生量而不是RGB外观。我们将其实例化为ReChannel：我们保留DiT输入分布的VAE编码器，但丢弃目标侧的解码器，用任务LoRA适配冻结的DiT，并通过共享的令牌局部线性头（约33K参数，无空间混合）将每个令牌映射到其p x p x K_t像素空间块。使用FLUX-Klein，我们在六个稠密预测任务和十几个基准上进行了评估。这种最小化接口在无trimap抠图、KITTI深度和指代分割上取得了新的最先进性能，并在法线、显著性和姿态上保持竞争力。在匹配的4B设置下，它比编辑加潜在解码的对应方法更准确，速度快2.48倍——稠密感知可以从生成预训练中受益，而无需继承其输出接口。

## Abstract
Large-scale text-to-image models are attractive backbones for dense prediction because RGB generation pretraining learns rich semantic, structural, and geometric priors. Existing generative and editing approaches reuse these priors by casting dense prediction as target generation: annotations such as depth, normals, alpha mattes, masks, and heatmaps are encoded into an RGB-trained VAE latent space and decoded back as image-like targets. We argue this inherits more of the generative output interface than dense prediction requires: unlike RGB synthesis, dense prediction asks for pixel-correct, task-native fields on the same image plane, not new RGB content to be rendered. Our key observation is that a pretrained DiT already organizes RGB inputs through a patch-to-token-to-patch lattice on the image plane, so each token indexes a fixed output patch whose channels can carry task-native quantities instead of RGB appearance. We instantiate this as ReChannel: we keep the VAE encoder for the DiT's input distribution but drop the target-side decoder, adapt the frozen DiT with task LoRA, and map each token to its p x p x K_t pixel-space patch through a shared token-local linear head--about 33K parameters, no spatial mixing. Using FLUX-Klein, we evaluate on six dense prediction tasks and over a dozen benchmarks. This minimal interface sets new state-of-the-art on trimap-free matting, KITTI depth, and referring segmentation, and stays competitive on normals, saliency, and pose. In a matched 4B setting it is more accurate and 2.48x faster than an edit-plus-latent-decode counterpart--dense perception can benefit from generative pretraining without inheriting its output interface.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大规模文本到图像（T2I）模型（如FLUX）通过RGB生成预训练学习了丰富的语义、结构和几何先验，因此被用作稠密预测（深度、法线、抠图、分割等）的骨干网络。现有方法通常将稠密预测建模为**目标生成**：将深度图、法线图等注释编码到RGB训练的VAE潜空间，再解码回像素空间，即将稠密预测视为图像到图像的生成或编辑问题。
- **核心问题**：论文指出，这种目标侧VAE编解码路径继承了RGB生成的输出接口，但稠密预测并不需要“生成新的RGB内容”，只需要在同一个图像平面上输出任务原生的像素空间场（如几何、透明度、热图等）。因此，现有方法引入了不必要的渲染模块和计算开销。
- **研究动机**：能否仅利用生成模型作为RGB图像形成的先验，而不继承其目标侧的输出接口，直接读出任务原生场？

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：**“读出而非生成”（readout, not generation）**。预训练DiT的patch-token-patch结构已经在图像平面上组织了一个空间令牌格点，每个令牌对应一个固定位置的输出块。在生成任务中这些块通道被解读为RGB外观，但在稠密预测中，这些通道可以直接承载任务原生量（深度、法线等）。因此，只需要**重新解释每个空间载体的通道内容**，无需复杂解码器。
- **关键技术细节**：
  - **保留RGB输入接口**：保持预训练VAE编码器不变，使DiT能够接收与训练时一致的潜空间输入。
  - **冻结DiT，轻量适配**：冻结全部DiT权重，仅对每个任务插入任务特定的**LoRA参数**（Δθ），将RGB令牌场朝向任务语义调整。
  - **令牌局部线性读出（token-local linear readout）**：每个适配后的令牌\(z_{ij}^t\)通过一个共享的线性投影\(W^t z_{ij}^t + b^t\)，直接映射到对应的\(p \times p \times K_t\)像素空间任务块\(\hat{Y}_{ij}^t\)。该头仅约33K参数，无空间混合，无多级解码器。
  - **像素空间监督**：所有任务的目标在像素空间进行损失计算，无需目标侧VAE编解码。
  - 公式表示：
    \[
    Z^t = F_{\theta+\Delta\theta}(\text{VAE}_{\text{enc}}(x); \sigma=0, c_t)
    \]
    \[
    \hat{Y}_{ij}^t = \text{reshape}(W^t z_{ij}^t + b^t)
    \]

- **核心洞察**：论文通过诊断实验（Fig. 2）显示，RGB输入令牌场维度高（参与比32.8），而任务适配后的场压缩到紧凑子空间（1.1–4.2），说明线性读出足够。目标侧VAE解码器是继承的渲染模块，非必要。

### 3. 实验设计

- **任务与数据集**：
  - **单目深度**：NYU（Eigen split）、KITTI（Garg crop）、ScanNet val，使用log空间尺度-移位对齐。
  - **表面法线**：NYU / ScanNet / iBims（按DSINE划分）。
  - **无trimap抠图**：P3M-500-P、P3M-500-NP、零样本AIM-500。
  - **指代分割**：RefCOCO / RefCOCO+ / RefCOCOg的8个标准子集，评价累积cIoU。
  - **人体姿态估计**：COCO val，AP/AP50（配合YOLOv11x检测框）。
  - **显著性检测**：DUTS-TE、ECSSD，评价\(F_{\text{max}}\)和MAE。
- **基准方法**：
  - 专门/判别方法：Omnidata v2、DAv2、DSINE、SHM、HATT、MODNet、P3M-Net、ViTAE-S、HRNet-W48、TokenPose-L、ViTPose-L等。
  - 生成/编辑方法：Marigold、GeoWizard、GenPercept、Lotus-D、Edit2Perc等。
- **骨干网络**：FLUX-Klein（4B和9B变体），冻结，使用LoRA适配。
- **核心对比**：在匹配4B设置下，与编辑+潜在解码对应方法（Edit paradigm）比较准确率和速度。

### 4. 资源与算力

- **文中未明确说明训练使用的GPU数量、总时长等具体算力资源**。
- 仅报告了推理延迟：在单张L40S GPU、512²分辨率下，ReChannel为47.7ms（与DiT前向一致），而编辑+潜在解码为118.1ms（2.48倍慢）。训练资源细节缺失。

### 5. 实验数量与充分性

- **实验数量充足**：覆盖6个不同性质的稠密预测任务，超过12个基准数据集，每个任务均有多个指标报告。
- **消融实验充分**（表5）：
  - 诊断1：是否需要LoRA？→ 仅训练头部导致性能崩溃（法线>44°、抠图>180 SAD），证明LoRA适配必要。
  - 诊断2：是否依赖预训练先验？→ 随机初始化大幅下降（NYU法线22.90° vs 15.82°；抠图SAD 11.56 vs 5.99），证明先验关键。
  - 诊断3：额外容量或生成接口是否有帮助？→ 13×更大的卷积头、全微调4B体、潜在目标、VAE解码、编辑范式均未超越ReChannel，且速度更慢。
- **公平性**：所有对比均在匹配的4B骨干和512²分辨率下进行，控制变量良好。主要结果（表1-4）与公开方法公平比较，但部分方法使用不同骨干（如Marigold基于Stable Diffusion），论文未控制骨干差异。

### 6. 论文的主要结论与发现

- 稠密预测能够从生成预训练中受益，但**不需要继承其目标侧输出接口**。ReChannel通过简单的令牌局部线性读出，在6个任务上达到SOTA或竞争力结果。
- **具体SOTA任务**：无trimap抠图（P3M-500-P SAD 5.69）、KITTI深度（absRel 0.063）、指代分割（平均cIoU 82.0）。
- 在匹配4B设置下，ReChannel比编辑+潜在解码方法准确率更高，速度快2.48倍。
- 核心发现：预训练DiT的令牌场已经组织了空间结构，只需重新诠释通道即可；目标侧VAE解码器是多余且有害的。

### 7. 优点：方法或实验设计上的亮点

- **方法极简**：单个共享线性头（33K参数）处理所有任务，无需空间解码器、无注意力、无多级上采样，设计优雅。
- **统一框架**：连续场（深度、法线）、二值掩码（分割、显著性）、多通道热图（姿态）均可由同一形式建模。
- **效率优异**：推理仅需一次DiT前向，线性头几乎零开销，是精度匹配中最快的方案。
- **实验诊断深入**：通过参与比诊断、消融实验系统地验证了核心假设，逻辑清晰。
- **出色跨域鲁棒性**：在零样本抠图（AIM-500）上大幅超越基线（SAD 34.90 vs 75.5），表明无需目标侧解码有助于泛化。

### 8. 不足与局限

- **骨干限制**：仅在FLUX-Klein（4B/9B）上验证，未扩展到其他T2I模型（如Stable Diffusion、Seedream等），缺乏通用性论证。
- **训练资源未报告**：未说明LoRA训练所需的GPU型号、数量、时长，难以评估训练开销。
- **深度任务非全面SOTA**：在NYU深度上absRel 0.051落后于Edit2Perc的0.044，法线任务上虽最优但优势较小（NYU 15.6° vs Edit2Perc 15.7°）。
- **可控性有限**：方法依赖冻结DiT的预训练分布，对于非对齐目标（如高分辨率输出或非标准分辨率）可能受限。
- **潜在泛化边界**：论文仅关注像素对齐的稠密预测，未扩展至视频、3D等更广泛任务；方法对模型结构（DiT的patch-token-patch）有依赖。
- **公平性细节不足**：主要结果表中，部分基线方法使用不同分辨率和骨干，论文未进行严格公平复现，比较可能受第三方报告影响。

（完）
