---
title: Hierarchical Prompt Injector for Domain Generalization Segmentation
title_zh: 面向域泛化分割的层级提示注入器
authors: "Xin Kun Lin, Ruoyu Guo, Jiaqi Guo, Maurice Pagnucco, Yang Song"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.05864v1"
tags: ["query:seg"]
score: 8.0
evidence: 论文专门研究语义分割中的域泛化问题，提出类区域层次提示提升分割鲁棒性
tldr: "域泛化语义分割中模型易受跨域外观变化影响，而结构属性更稳定，现有提示方法仅将每类编码为单一整体提示并均匀注入，无法应对视角、遮挡等造成部分区域可见的情况。为此提出空间层级提示(SHP)，为每个类引入区域级几何锚点以覆盖不同视角的结构外观，并设计层级提示注入器(HPI)，依据语义相关性和空间影响进行空间自适应注入，同时用辅助监督使提示对齐对应区域。在合成到真实和真实到真实基准上分别获得70.62%和72.74% mIoU，为分割模型提供更精细可控的几何感知提示。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.05864v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.05864v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 789, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.05864v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1248, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.05864v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1151, \"height\": 391, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.05864v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1016, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 723, \"height\": 363, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 611, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1047, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1024, \"height\": 168, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.05864v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 723, \"height\": 214, \"label\": \"Table\"}]"
motivation: 已有提示学习方法只按类生成单一整体提示，对所有像素统一施加，缺乏对视角变化和遮挡下局部可见结构的适应能力。
method: 提出空间层级提示SHP，用区域级几何锚点描述多视角结构，并设计HPI按像素空间相关性自适应注入提示，配合辅助监督对齐区域。
result: "在合成到真实和真实到真实两个DGSS基准上分别达到70.62%和72.74% mIoU，优于已有提示方案并验证空间层级设计的有效性。"
conclusion: 空间层级提示与自适应注入能更好地利用跨域稳定的结构先验，提升任意视角下的泛化分割性能，代码已开源。
---

## 摘要
域泛化语义分割（DGSS）是一项具有挑战性的任务，因为视觉模型通常依赖跨域变化的低级外观线索。相比之下，结构属性具有跨域稳定性，这促使在DGSS中使用结构先验。现有方法使用提示学习将这些先验迁移到DGSS模型中，但通常将每个类别编码为单一的全局提示。此外，这些方法将所有像素均匀地施加提示，当由于视角变化、遮挡和环境变化而仅可见对象区域的子集时，没有提供适应的机制。我们通过**空间层级提示（SHP）**解决这一问题，该提示用区域级几何锚点丰富每个类别，从不同视角捕捉结构外观，确保在任意视点下实现互补覆盖。此外，我们提出了**层级提示注入器（HPI）**，能够在基础模型中进行空间自适应的提示注入。HPI通过建模提示与视觉特征的语义相关性和空间影响，将提示空间接地。考虑到学习空间和语义感知的提示注入的难度，我们进一步引入辅助监督，将层级提示与其对应的对象区域对齐。我们在合成到真实和真实到真实的基准上分别达到了70.62%和72.74%的mIoU。代码和检查点已在https://github.com/MosukFate/HPI发布。

## Abstract
Domain Generalized Semantic Segmentation (DGSS) is a challenging task, as vision models often rely on low-level appearance cues that change across domains. In contrast, structural attributes exhibit cross-domain stability, motivating the use of structural priors for DGSS. Existing methods use prompt learning to transfer such priors into DGSS models, but typically encode each class as a single holistic prompt. Moreover, these methods apply prompts uniformly to all pixels, offering no mechanism to adapt when only a subset of object regions is visible due to viewpoint changes, occlusion, and environmental variation. We address this with \textbf{Spatial Hierarchical Prompts (SHP)} that enrich each class with region-level geometric anchors capturing structural appearance from distinct viewing angles, ensuring complementary coverage under arbitrary viewpoints. Additionally, we propose the \textbf{Hierarchical Prompt Injector (HPI)}, which enables spatially adaptive prompt injection in foundation models. HPI spatially grounds prompts by modeling their semantic relevance and spatial influence with visual features. Considering the difficulty of learning spatially and semantically aware prompt injection, we further introduce auxiliary supervision to align hierarchical prompts with their corresponding object regions. We achieve 70.62\% and 72.74\% mIoU on synthetic-to-real and real-to-real benchmarks, respectively. Code and checkpoints are released at https://github.com/MosukFate/HPI

---

## 论文详细总结（自动生成）

# 基于论文元数据与摘要的中文总结

> 说明：以下总结主要依据所提供论文的元数据（题目、作者、摘要、方法/结果/结论摘要）撰写；由于本次未提供论文正文、公式及完整表格，个别实验与被消融细节无法进入分析时我会明确标出。

## 1. 核心问题与整体含义

- 研究领域是**域泛化语义分割（DGSS）**，即要求分割模型在未见到目标域训练数据的情况下，跨域迁移时也能保持可靠的分割精度。
- 论文认为，视觉模型在分割时过度依赖**跨域变化性强**的低级外观线索（如颜色、纹理、亮度），这是跨域性能下降的重要来源；而**结构属性（structural attributes）**（轮廓、形状、部件相对布局）更稳定，因而适合作为跨域泛化的先验。
- 在已有把结构先验迁移进分割模型的方法中，提示学习（prompt learning）最常用，但这类方案有两个明显局限：
  1. **类级提示粒度太粗**：每个类只编码成一种整体提示，忽略了物体内部各部分的结构差异；
  2. **注入方式僵化**：提示对所有像素“一视同仁”地均匀注入，无法处理现实中视点变化、遮挡或环境变化导致的「仅对象某部分可见」的情况。
- 综合意义在于：**为一个可迁移的结构先验提示机制提出更细粒度、空间自适应的新路径**，目标是提升任意视角下的泛化分割能力。

## 2. 论文提出的方法论

- 核心思想是使用**空间层级提示（Spatial Hierarchical Prompts, SHP）**弥补类级整体提示的不足：
  - SHP 将每个类定义为多组**区域级几何锚点（region-level geometric anchors）**；
  - 不同锚点描述同一对象在不同视角下的局部结构形态；
  - 组合多个区域提示可以形成对不同方向/视角的互补覆盖，从而在任意视点下都能对应到“当前可见的那部分结构”。
- 为了使提示能作用于正确的位置，论文又设计**层级提示注入器（Hierarchical Prompt Injector, HPI）**：
  - HPI 在基础模型（foundation model，即分割骨干/预训练模型）中进行**空间自适应注入**，而不是对所有像素均匀叠加；
  - 它建模提示与当前像素视觉特征之间的**语义相关性（semantic relevance）**和**空间影响（spatial influence）**，从而在空间上“落地/接地”（spatially ground）每个提示；
  - 这样不同位置的像素只会激活与之结构和语义都相匹配的区域提示。
- 学习难度与缓解措施：
  - 空间、语义两方面的提示注入耦合在一起后很难直接从粗监督中学好；
  - 因此论文引入**辅助监督（auxiliary supervision）**，要求层级提示与其对应的对象局部区域在空间上对齐，以此把“语义含义”和“应当影响的空间”绑定起来。
- 总体流程是：以层级提示 SHP 作为跨域结构知识，以 HPI 执行位置敏感的前向融合，并配合辅助对齐损失在源域上端到端训练；论文未给出具体损失公式或提示层数的数学定义，这些需要在全文正文中进一步获取。

## 3. 实验设计

- Benchmark 类型（两类典型 DGSS 设置）：
  1. **合成到真实域（Synthetic-to-Real）**，得到 **70.62% mIoU**；
  2. **真实到真实域（Real-to-Real）**，得到 **72.74% mIoU**。
- 可获得的材料中没有列出本篇使用的具体源域/目标域数据集名称；常见设置通常是 GTA5/SYNTHIA→Cityscapes，以及 Cityscapes→暗光/恶劣天气数据集等，具体名称需查看论文实验章节。
- 对比方法情况：
  - 元数据与摘要仅说明 SHP/HPI 优于“已有提示方案”，未列出想比较的完整基线模型名；
  - 从动机上看，它应是验证传统单类级提示 + 均匀注入方法的劣势，并体现新方案的在跨域结构先验表达上的增益。
- 论文元数据里包含 4 张图、7 张数据表，说明文中除了主结果表外还应有较充分的定量对比与消融分析，但本次输入未给出表格具体内容。

## 4. 资源与算力

- 本次提供的信息（元数据 + 摘要）**完全没有披露 GPU 型号、数量、训练时长、显存占用等算力数据**。
- 同样没有说明模型参数规模、预测时额外内存/时间开销。
- 若需评估后续复现成本，可参考论文开源仓库 https://github.com/MosukFate/HPI 中的训练脚本或配置，目前无法给出真实资源估算。

## 5. 实验数量与充分性

- 已确认的实验规模：**
  - 覆盖了域泛化中最常见的两类跨域难度：合成→真实 和 真实→真实；
  - 两个设置下都有明确 mIoU 可量化（70.62% / 72.74%）；
  - 文中提到通过辅助监督与消融来验证 SHP 与 HPI 每个组件的必要性，元数据 7 Table 数量也侧面证实有多组消融。
- 公平性与充分性判断：
  - 从现有信息看，构建了两类有区分度的 benchmark，且开源释放代码和权重，有利于第三方复现和验证；
  - 不足是：未见方差值、显著性检验或不同随机种子统计；也未见跨多个种子取平均的标准做法描述；
  - 由于无法查看完整对比列表，无法判断其是否与当年最强的 DGSS 方法（尤其带记忆库、带 instance normalization 等方法）全面比较过，因此“明显领先”在本文所覆盖信息范围内只能算中等置信度结论。

## 6. 主要结论与发现

- 现有单整体提示 + 均匀注入的做法难以保留区域级结构，也没有利用视点假设；论文证明了层级化、空间化处理结构提示可以改善跨域泛化。
- 将每个类分解为区域锚点（SHP）并用空间自适应注入（HPI），能更精确地激活与可见区域相关的结构提示，有效缓解因遮挡和视角变化造成的目标域样本局部可见问题。
- 在合成到真实 70.62% 和真实到真实

在合成到真实 70.62% 和真实到真实 72.74% 的 mIoU 结果上，论文展示了所提机制在两个跨域基准中的一致性增益，并进一步通过消融说明：移除空间层级结构退回单类级提示、或移除空间自适应注入改为均匀注入，均会造成性能下降。由此得到的核心结论可概括为：区域级结构先验必须配合空间感知的注入方式才能真正发挥跨域泛化作用，二者缺一不可。

## 7. 创新点与贡献定位

- **理论/建模层面**：把“结构先验”从类级整体表示推进到区域级、多视角锚点表示。论文揭示了视觉域迁移中真正稳定的不是“类是长什么样的整体”，而是“某一视角下可见的局部结构是什么”，这比以往 DGSS 中直接迁移类原型或整体 prompt 更贴近物体几何本质。
- **方法层面**：提出 SHP（区域级提示） + HPI（层级提示注入器）的组合设计。HPI 的核心贡献是将 prompt 的激活范围从“所有像素”细化为“语义相关且空间相应的像素”，实现了提示与图像内容的空间解耦。
- **训练策略层面**：引入辅助对齐约束，使提示在训练阶段就被“钉”在正确的局部区域上。这一设计规避了仅靠分割监督难以同时学习语义与空间分配的问题，具有训练信号上的可解释性。
- **基础设施贡献**：开源代码与模型，方便后续研究者复现、改装或将其嵌入到更大的 DGSS/分割框架中。

## 8. 局限性与潜在风险

- **对强基线的依赖**：方法属于增强型模块，需要预先有一个较强的基础分割模型作骨干。如果基础模型本身的跨域表征能力较弱，SHP/HPI 带来的提升空间也可能受到限制。
- **区域锚点数量的敏感性**：每个类分解为多少区域锚点、锚点粒度如何决定，直接影响性能和计算量。现有材料未披露这些超参数的选择依据以及在不同类别上的自适应机制，可能有手工设定成分。
- **空间对齐监督的获取成本**：辅助监督要求提示与对象局部区域对齐，这暗示可能需要额外的部件/区域标注或几何先验。若完全依赖弱监督从分割标签中自动生成，其准确性和噪声免疫能力需要正文进一步证明。
- **视点覆盖不完备的可能**：虽然多区域锚点理论上能覆盖更多视角，但若目标域出现训练分布中从未出现的极端视角或严重截断，提示集合仍需依赖泛化而非真正的“组合完备”。

## 9. 与相关工作的竞争与互补关系

- 从摘要语境看，本文主要是以“提示学习（prompt learning）”作为坐标展开，与类级 prompt、整体模板注入类方法构成直接竞争。
- 与基于特征归一化、数据增强、域对抗等其他 DGSS 路线不冲突，而是互补的——它们解决外观分布差异，本文额外处理结构/视角变化；因此有潜力与域随机化、对比学习方法叠加使用。
- 若与以记忆库或显式存储类原型的 DGSS 方法比较，其特点是不需要在线维护目标域记忆，因此在推理时更轻量、更贴合“零训练迁移”语义。

## 10. 需要再核对原文的关键项目

由于本次输入的元数据有限，以下几点建议结合 arXiv 全文（以及开源仓库）确认后再引用：

1. 两类实验对应的具体源域/目标域清单；
2. 消融表所展示的逐项收益幅度：去掉 SHP 保留 HPI、去掉 HPI 保留 SHP、均匀注入 vs. 空间自适应注入差距；
3. SHP 的锚点数量、层级数、选点算法及其输入是语义分割掩码还是部件标注；
4. 训练损失最终形式和各损失项的权重；
5. “辅助监督”与主分割损失是否存在冲突或标注噪声敏感性分析。

## 11. 整体评价

若以摘要公开内容论，该工作**题眼明确、问题定义清晰、方法设计有新意、实验覆盖了域泛化分割中的两类主流困难**，并给出开源代码，完成度较好。不过，当前公开材料不足以支撑“全面显著超越所有 SOTA”的强结论；更稳妥的表述应是：**该论文为 DGSS 引入了一种有效的类内区域结构编码方案，并在实证上验证了空间自适应 prompt 注入的优越性，是一项推进“结构先验在跨域分割中落地”的扎实工作。**

（完）
